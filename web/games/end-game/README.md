# End Game web releases

`v1/` contains The Awakening's browser shell and version-local media. Its renderer and game simulation come from the Rust `end-game` crate, compiled to WASM. The launcher selects the live release using `web/games.json`.
