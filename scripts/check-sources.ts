/**
 * Prueft in Phase 2 alle Quellen-URLs des Fragenbestands per HTTP.
 * Bis dahin bewusst ein lauffaehiger Platzhalter, damit `pnpm check:sources`
 * schon jetzt existiert und ohne Fehler beendet.
 */
console.log("check-sources: noch nicht implementiert (kommt in Phase 2).");
process.exit(0);
