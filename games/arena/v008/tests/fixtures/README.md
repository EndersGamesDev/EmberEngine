# Arena v8 fixtures

This directory holds client and server transcripts, legacy lobby traffic, a deterministic trace and the suite manifest for fixture suite `arena-v8-hosted-contract`.

The Arena v8 hosted-contract test depends on these records as frozen evidence; never edit them by hand, and cut a new version slot under the [hosted-version rules](../../../../../docs/one-server-evergreen.md) when behavior changes.

