# End Game runtime assets

Versioned optimized exports of the user's locally generated End Game asset set. The original artist/generator source remains in the separate `end-game` workspace. See `v1/README.md` for the rendering contract and provenance.
