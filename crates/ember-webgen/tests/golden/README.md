# Generated golden bytes

Each non-README file is the exact expected output for one generated artifact with the fixed source identifier `golden`. The `.golden` suffix keeps declarations and compiler inputs from becoming tracked TypeScript outputs.
