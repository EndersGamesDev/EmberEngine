# Production templates

These strict Minijinja templates render type-only boundary declarations, the exhaustive TypeScript consumer used by the release gate, the compiler-only ES2022 symbol declaration required by current wasm-bindgen output and the registry's temporary behaviour-template proof. Their names and bytes contribute to the stable template-source hash.
