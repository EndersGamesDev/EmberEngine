# Generator source

The library collects the six compiler-owned registration feeds, lowers their descriptors to template context, renders schemas and writes the SHA-scoped and fixed compiler trees. The binary only parses the output argument and resolves the source identifier.
