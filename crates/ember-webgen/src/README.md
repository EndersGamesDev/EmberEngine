# Generator source

The library collects the six compiler-owned registration feeds, lowers their descriptors to template context, renders schemas and checked behaviour templates, writes the SHA-scoped and fixed compiler trees and maps rendered diagnostics back to tracked template coordinates. The binary selects generation or diagnostic mapping and resolves the source identifier for generation.
