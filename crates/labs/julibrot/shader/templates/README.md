# Julibrot shader templates

Every file in this directory is embedded into `ember-julibrot-shader` with `include_str!` and compiled by Minijinja at runtime in the wasm context.

The interface fixtures exercise Rust-owned declarations and naga's line-bearing diagnostics during the mechanism's own tests. Julibrot's production templates join the registry as their shaders migrate.
