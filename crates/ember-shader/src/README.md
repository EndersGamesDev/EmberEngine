# Shared shader source

`lib.rs` exposes the repository-neutral runtime shader boundary. Rust-owned type and enum metadata, template rendering, native validation, and anti-drift auditing join the skeleton as independently gated structural steps.

The mechanism follows `crates/labs/julibrot/shader`; repository-wide policy lives in [`../../../docs/shaders.md`](../../../docs/shaders.md).
