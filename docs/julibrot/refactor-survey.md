# Julibrot refactor survey

## 1. Layout and quality

### Scope and counting basis

**Verified.** This round contains 30,351 physical lines in the named source set: app 12,463, worker 5,477, kernels 2,421, and present 9,990; math contributes zero scoped source lines and is represented here from its slice contract because no math source file belongs to the round-one code set. The five-crate ownership baseline is math for algebra and CPU oracles, kernels for dialect-v2 compute and heap landing, worker for reference production, transfer, credit, and publication, present for scene/reprojection/HOT records, and app for device, surface, scheduling, controls, facts, and integration (`docs/julibrot-lab.md:25-41`; verified; `crates/labs/julibrot/app/src/frame.rs:1-9`, `crates/labs/julibrot/worker/src/channel.rs:1-1463`, `crates/labs/julibrot/kernels/src/gpu.rs:1-950`, `crates/labs/julibrot/present/src/contract.rs:1-944`).

|Status|Crate and scoped line count|Scoped module map and present ownership|
|------|---------------------------|---------------------------------------|
|Verified|math — 0 scoped source lines|The out-of-scope source modules are represented only by the contract: pure object/camera/plane/screen/navigation/reprojection algebra, bignum centre and orbit, perturbation theory, drift and morph oracles; no round-one conclusion below treats a math implementation detail as inspected (`docs/julibrot/math.md:5-13`, `docs/julibrot/math.md:161-207`).|
|Verified|app — 12,463|`state.rs` 2,677 owns requested controls, input mapping, checked maps/footprints, worker-owner adaptation, reference requests, and pose construction; `saved.rs` 672 owns persisted rows and morph composition; `runtime.rs` 602 plus `surface.rs` 203 own browser device/surface and its single token; `frame.rs` 9 is the shell; `frame/loop.rs` 1,604 owns orchestration, `browser/{backdrop,facts,reference,submit}.rs` 255/275/632/422 own browser-specific arms, `schedule.rs` 954 owns refinement/fence policy, `warp.rs` 17 owns three redraw guards, and `loop/tests.rs` 4,141 owns the large fake integration harness (`crates/labs/julibrot/app/src/state.rs:1-2677`, `crates/labs/julibrot/app/src/saved.rs:1-672`, `crates/labs/julibrot/app/src/runtime.rs:1-602`, `crates/labs/julibrot/app/src/surface.rs:1-203`, `crates/labs/julibrot/app/src/frame.rs:1-9`, `crates/labs/julibrot/app/src/frame/loop.rs:1-1604`, `crates/labs/julibrot/app/src/frame/loop/browser/backdrop.rs:1-255`, `crates/labs/julibrot/app/src/frame/loop/browser/facts.rs:1-275`, `crates/labs/julibrot/app/src/frame/loop/browser/reference.rs:1-632`, `crates/labs/julibrot/app/src/frame/loop/browser/submit.rs:1-422`, `crates/labs/julibrot/app/src/frame/schedule.rs:1-954`, `crates/labs/julibrot/app/src/frame/warp.rs:1-17`, `crates/labs/julibrot/app/src/frame/loop/tests.rs:1-4141`).|
|Verified|worker — 5,477|`wire.rs` 866 owns ABI-3 bytes and four buffer identities; `channel.rs` 1,463 owns the public endpoints and same-thread queues; `endpoint.rs` 1,558 owns a second transport-neutral browser-owner state machine; `browser_owner.rs` 394 lowers it to a module Worker; `owner.rs` 1,056 owns `ViewerState`, navigation generations, staging, and drains; `registry.rs` 140 maps compact generation-tagged orbit handles to app-held values (`crates/labs/julibrot/worker/src/wire.rs:1-866`, `crates/labs/julibrot/worker/src/channel.rs:1-1463`, `crates/labs/julibrot/worker/src/endpoint.rs:1-1558`, `crates/labs/julibrot/worker/src/browser_owner.rs:1-394`, `crates/labs/julibrot/worker/src/owner.rs:1-1056`, `crates/labs/julibrot/worker/src/registry.rs:1-140`).|
|Verified|kernels — 2,421|`refinement.rs` 491 owns the three-level plan and dispatch facts; `gpu.rs` 950 owns whole-grid spans, registered shallow/perturbation kernels, immutable dispatch templates, uniform writes, scratch-copy encoding, publication, and freeing; `tile_job.rs` 980 owns wire-free stage-0 identities, demand ordering, paired-output state, reference leases, and resource arithmetic, but only its own tests call those tile types (`crates/labs/julibrot/kernels/src/refinement.rs:19-304`, `crates/labs/julibrot/kernels/src/gpu.rs:20-703`, `crates/labs/julibrot/kernels/src/tile_job.rs:1-708`).|
|Verified|present — 9,990|`contract.rs` 944 owns app-facing records/events/facts; `uniform.rs` 421 owns the 288-byte HOT ring and 160-byte scene ABI; `tile.rs` 861 owns stage-0 render/content keys, descriptor records, invalidation, and same-surface quality; `planner.rs` 2,617 owns the CPU whole-frame planner and its in-file oracle corpus; `gpu/device/{census,ledger,poll,readback,redraw,scene,scene/submit,shade,uniforms,warp}.rs` contain 197/398/206/597/169/443/200/124/110/633 lines and own the named device transactions; `gpu/device/tests.rs` contributes 2,070 more test lines (`crates/labs/julibrot/present/src/contract.rs:1-944`, `crates/labs/julibrot/present/src/uniform.rs:1-421`, `crates/labs/julibrot/present/src/tile.rs:1-861`, `crates/labs/julibrot/present/src/planner.rs:1-2617`, `crates/labs/julibrot/present/src/gpu/device/census.rs:1-197`, `crates/labs/julibrot/present/src/gpu/device/ledger.rs:1-398`, `crates/labs/julibrot/present/src/gpu/device/poll.rs:1-206`, `crates/labs/julibrot/present/src/gpu/device/readback.rs:1-597`, `crates/labs/julibrot/present/src/gpu/device/redraw.rs:1-169`, `crates/labs/julibrot/present/src/gpu/device/scene.rs:1-443`, `crates/labs/julibrot/present/src/gpu/device/scene/submit.rs:1-200`, `crates/labs/julibrot/present/src/gpu/device/shade.rs:1-124`, `crates/labs/julibrot/present/src/gpu/device/uniforms.rs:1-110`, `crates/labs/julibrot/present/src/gpu/device/warp.rs:1-633`, `crates/labs/julibrot/present/src/gpu/device/tests.rs:1-2070`).|

**Verified.** The largest scoped files are now `app/src/frame/loop/tests.rs` at 4,141 lines, `app/src/state.rs` at 2,677, `present/src/planner.rs` at 2,617, `present/src/gpu/device/tests.rs` at 2,070, `app/src/frame/loop.rs` at 1,604, and `worker/src/endpoint.rs` at 1,558; the brief's lower approximate numbers describe earlier sizes rather than this checkout (`crates/labs/julibrot/app/src/frame/loop/tests.rs:1-4141`, `crates/labs/julibrot/app/src/state.rs:1-2677`, `crates/labs/julibrot/present/src/planner.rs:1-2617`, `crates/labs/julibrot/present/src/gpu/device/tests.rs:1-2070`, `crates/labs/julibrot/app/src/frame/loop.rs:1-1604`, `crates/labs/julibrot/worker/src/endpoint.rs:1-1558`).

### Ownership quality against the slice contracts

**Verified.** The quality standard here is the written ownership split: math owns pure algebra and oracles; kernels owns GPU data-to-data work and grid spans but not scheduling or presentation; worker owns reference production, transfer, credit, owner records, and drains; present owns pixels/facts after an `EscapeGrid` exists but never the surface; app owns construction, cross-slice call order, the surface token, policy, and integration (`docs/julibrot/math.md:5-13`, `docs/julibrot/kernels.md:5-19`, `docs/julibrot/worker.md:5-11`, `docs/julibrot/present.md:5-21`, `docs/julibrot/app.md:5-17`).

**Derived.** File length and central coordination are not defects by themselves. The findings below count mixed ownership only where one type contains another slice's mutable authority, exposes reach-through around its adapter, duplicates a promised state machine, or prevents a boundary observation from being replayed independently.

**Verified.** `ViewerController` is the principal mixed owner: its fields combine app-requested intent and revision, math-produced plane/map/footprint caches, a worker-owned `ViewerOwner`, shadow HOT/MAIN records, reference-request state, navigation mirrors, and crosshair/UI state; its edit methods invoke worker navigation directly and its drains also construct the present-facing `Pose`. This is legal integration under the app contract, but it collapses J-01, J-03, and the math-to-present adapter into one type rather than exposing ports between them (`crates/labs/julibrot/app/src/state.rs:397-435`, `crates/labs/julibrot/app/src/state.rs:563-625`, `crates/labs/julibrot/app/src/state.rs:1264-1328`).

**Verified.** `BrowserFrameLoop` is the second mixed owner: one struct holds device and queue, heap executor, kernels, presenter, worker endpoints, orbit registry and accepted leases, current and spare grids, backdrop state, refinement schedule, surface-facing capture, event/facts state, and timing. App is supposed to integrate these slices, but this storage shape makes all lower boundaries fields of one browser-only coordinator and prevents the individual J-02, J-07, J-10, and J-12 transactions from being replayed through narrow ports (`crates/labs/julibrot/app/src/frame/loop.rs:672-772`; `docs/julibrot/app.md:129-145`).

**Verified.** App reaches through `ViewerController` into worker internals through public `owner()` and `owner_mut()`: scheduling mutates precision policy, reference handling accepts orbits and navigation, submit guards query pending depth, and saved-view capture reads the authoritative centre. Those callers bypass the controller's otherwise app-facing transaction surface and obstruct a replaceable J-03 owner/drain port (`crates/labs/julibrot/app/src/state.rs:548-557`, `crates/labs/julibrot/app/src/frame/schedule.rs:833-846`, `crates/labs/julibrot/app/src/frame/loop/browser/reference.rs:180-230`, `crates/labs/julibrot/app/src/frame/loop/browser/submit.rs:107-118`, `crates/labs/julibrot/app/src/saved.rs:170-180`).

**Verified.** Worker has two owner-channel implementations rather than two lowerings of one implementation: `ChannelCore` keeps queues, pending request, generation/revision, credit, facts, leases, resize, and pump logic for same-thread use, while generic `OwnerCore` repeats those fields and the submit/return/resize/facts transitions for the browser transport. The endpoint header claims both native and browser drive the identical state machine, but `WorkerChannel::new` selects `ChannelCore` for same-thread and `OwnerCore` only through `BrowserOwnerEndpoint`; this is a contract-quality defect at J-02 because equivalence depends on tests rather than construction (`crates/labs/julibrot/worker/src/channel.rs:60-136`, `crates/labs/julibrot/worker/src/channel.rs:901-1098`, `crates/labs/julibrot/worker/src/endpoint.rs:1-10`, `crates/labs/julibrot/worker/src/endpoint.rs:158-188`; `docs/julibrot/worker.md:313-333`).

**Verified.** Tile vocabulary is split across slices: kernels and present both define `TileResidency` and `TileQuality` with different fields and ordering, kernels alone defines signed `SourceScreenRect`, and present alone defines unsigned `SourcePixelRect`, `TileRung`, `DescriptorSamplePair`, and canonical chart ownership. The shipped app, worker, whole-grid kernels, planner, and device path do not call these stage-0 types; their only scoped callers are their defining modules' tests, so this is deliberately dormant scaffolding rather than a live J-06/J-08 port (`crates/labs/julibrot/kernels/src/tile_job.rs:141-172`, `crates/labs/julibrot/kernels/src/tile_job.rs:345-376`, `crates/labs/julibrot/present/src/tile.rs:90-110`, `crates/labs/julibrot/present/src/tile.rs:368-443`, `crates/labs/julibrot/kernels/src/tile_job.rs:708-980`, `crates/labs/julibrot/present/src/tile.rs:555-861`; `docs/julibrot/kernels.md:23-25`).

**Derived.** The whole-grid path otherwise respects the written slice direction: kernels own the `EscapeGrid` and its heap lifetime, present borrows/clones it into `PresentMain` and retains completed `SceneFrame` identities, and app owns surface acquisition through terminal presentation. The refactor should preserve those ownership facts while making the intervening ports explicit rather than moving policy down-slice.

### Duplicate entries, globals, stale names, and dormant code

|Status|Finding|Contract judgment and obstructed seam|
|------|-------|-------------------------------------|
|Verified|`ViewerController::pan_px` delegates entirely to public `drag_pan`, while `zoom_about_crosshair` delegates to public `wheel_zoom`; the page contract explicitly says there is no wheel handler.|The duplicate public edit entries enlarge J-01 without adding a distinct transaction, and `wheel_zoom` is stale terminology for the scale/crosshair operation (`crates/labs/julibrot/app/src/state.rs:563-625`, `crates/labs/julibrot/app/src/state.rs:702-714`; `docs/julibrot/app.md:77-91`).|
|Verified|`Presenter::poll` allocates a `Vec` from `poll_fixed`, while app's `PresenterPoll` trait still requires `Vec`.|The compatibility entry is measurable duplicate surface at J-10 and defeats the fixed two-event record already available (`crates/labs/julibrot/present/src/gpu/device/poll.rs:8-28`, `crates/labs/julibrot/app/src/frame/schedule.rs:277-282`).|
|Verified|`BrowserRuntime::start` and `start_on_canvas` are two startup entries.|This duplication is intentional and contract-compliant: the control page requires a status element while the driver page must start from a canvas alone, so round two should retain both (`crates/labs/julibrot/app/src/runtime.rs:51-81`; `docs/julibrot/app.md:630-642`).|
|Verified|`BrowserFrameLoop` exposes cloned and borrowed present facts through `present_facts`/`present_facts_ref`; present itself similarly exposes `facts`/`facts_ref`.|The cloned entries are boundary convenience, but internal fact assembly should use borrowed J-10 facts so planner-ledger clones are not hidden in refresh work (`crates/labs/julibrot/app/src/frame/loop/browser/facts.rs:10-20`, `crates/labs/julibrot/present/src/gpu/device/poll.rs:30-39`).|
|Verified|Worker startup reads `JULIBROT_WORKER_URL` and the browser clock from `js_sys::global()` rather than accepting both through the port constructor.|The URL is deployment configuration and time is an injected `OwnerPort` capability below that read, so the global access is hidden specifically at the J-02 browser adapter (`crates/labs/julibrot/worker/src/browser_owner.rs:20-21`, `crates/labs/julibrot/worker/src/browser_owner.rs:351-389`).|
|Verified|Runtime keeps panic-hook installation and the last panic in thread-local globals.|This global is allowed by the app slice's mandatory pre-device panic/error contract and is not refactor debt; it should remain outside frame state (`crates/labs/julibrot/app/src/runtime.rs:14-17`, `crates/labs/julibrot/app/src/runtime.rs:554-592`; `docs/julibrot/app.md:63-75`).|
|Verified|The settled deterministic promotion types and constants are compiled under explicit `allow(dead_code)` and the feature switch is false.|This is known dormant app scheduling, not unreachable production accidentally left behind; activation changes policy and therefore stays outside behaviour-preserving round two and J-07 extraction (`crates/labs/julibrot/app/src/frame/schedule.rs:56-111`).|
|Verified|`Warp::reproject` accepts precision and validation but names both arguments with leading underscores and never consults them.|These stale parameters make callers imply a J-11 planner distinction that does not exist; removing them is behaviour-preserving once planner-result equality is recorded (`crates/labs/julibrot/present/src/planner.rs:46-101`).|
|Verified|`frame/warp.rs` is a 17-line module containing only three boolean scheduling predicates, while the corresponding decisions and state live in `schedule.rs` and `loop.rs`.|This split hides rather than creates a port; round two can place the predicates with `FrameLoop` policy without changing the J-11 call order (`crates/labs/julibrot/app/src/frame/warp.rs:1-17`, `crates/labs/julibrot/app/src/frame/schedule.rs:352-379`, `crates/labs/julibrot/app/src/frame/loop.rs:1270-1301`).|

### Test-harness sprawl and duplicated fixtures

**Verified.** The app frame-loop harness defines `FakePresenter`, `FakeClock`, `drive_turn`, `drive_refresh`, and `drive_viewer_harness`, then adds a separate `ZoomScript`/`ZoomTurnRecord` driver and a separate `HeightDragRow`/`HeightDragStats` driver. All three reconstruct scene identities, refinement progress, warp kind, retained source, and presentation turns, so they duplicate the same fixture vocabulary without producing one comparable frame trace (`crates/labs/julibrot/app/src/frame/loop/tests.rs:685-1124`, `crates/labs/julibrot/app/src/frame/loop/tests.rs:2402-2927`, `crates/labs/julibrot/app/src/frame/loop/tests.rs:3148-3406`).

**Verified.** Present repeats pose/frame fixtures at three levels: planner's in-file `frame` and pose builders exercise pure plans, device tests define `binding_pose`, `binding_main`, `frame_on`, and `pose_on` for ledger/GPU-shape assertions, and the native `reprojection_oracle`, `steep_row`, and `steep_frame` suites independently rebuild the same source/destination pose families. The layers test different contracts, but the pose and `SceneFrame` constructors are duplicated and make a single planner-result corpus difficult to compare (`crates/labs/julibrot/present/src/planner.rs:1001-1035`, `crates/labs/julibrot/present/src/planner.rs:1610-1648`, `crates/labs/julibrot/present/src/gpu/device/tests.rs:178-257`, `crates/labs/julibrot/present/src/gpu/device/tests.rs:1090-1107`, `crates/labs/julibrot/present/src/gpu/device/tests.rs:1323-1344`, `crates/labs/julibrot/present/tests/reprojection_oracle.rs:1`, `crates/labs/julibrot/present/tests/steep_row.rs:1`, `crates/labs/julibrot/present/tests/steep_frame.rs:1`).

**Verified.** Worker has a small tuple-based `trace` over public `WorkerChannel` and a much larger `FakeSlot`/`FakePort`/`Harness` around `OwnerCore`; because same-thread public construction bypasses `OwnerCore`, those harnesses prove parallel implementations rather than replaying identical events through two ports (`crates/labs/julibrot/worker/src/channel.rs:1415-1460`, `crates/labs/julibrot/worker/src/endpoint.rs:732-1034`).

**Derived.** Moving fixture constructors into small test-support modules is useful only after each oracle has one owned trace/result record. Mere file splitting would reduce line length but would not remove duplicated semantic fixtures or protect a seam.

### Published measured facts

|Status|Publisher|Measured facts available at its boundary|
|------|---------|----------------------------------------|
|Verified|math|No mutable runtime facts publisher exists in the slice contract; math returns pure values and error/accuracy oracles, so facts belong to a caller that performed work (`docs/julibrot/math.md:5-13`, `docs/julibrot/math.md:365-409`).|
|Verified|worker|`OwnerEndpoint::facts` and `ProducerEndpoint::facts` publish a coherent `WorkerFacts` snapshot; the channel updates its fact epoch, queue depths, pool ownership, allocation events, credit, applied/stale/cancelled counts, last acknowledged/applied generations, compute wall, and overfeed as ownership events occur (`crates/labs/julibrot/worker/src/channel.rs:304-325`, `crates/labs/julibrot/worker/src/channel.rs:535-555`, `crates/labs/julibrot/worker/src/channel.rs:1032-1098`).|
|Verified|kernels|`DispatchFacts` publishes precision label, level, mode, owner epoch, requested/delivered extent and cap, page passes, copy commands, scratch/copy/reserved bytes, and optional orbit generation/length; `checked_facts` verifies planned pass/copy counts against executor facts before returning it (`crates/labs/julibrot/kernels/src/refinement.rs:78-100`, `crates/labs/julibrot/kernels/src/refinement.rs:236-304`, `crates/labs/julibrot/kernels/src/gpu.rs:650-679`).|
|Verified|present|`PresentFacts` publishes retained/pending scene identities, source generation and held partition, delivered and warp lattices, palette/view, latest scene/warp fence measurements, reprojection/redraw/hold/clear counts, texture reallocations, exposure/error/refusal results, the last-eight presentation ledger, and honest status; promotion writes completed scene identity, generation, dimensions, level, cap, census, and view after the scene fence (`crates/labs/julibrot/present/src/contract.rs:555-645`, `crates/labs/julibrot/present/src/gpu/device/poll.rs:172-205`).|
|Verified|app|`DeviceFacts` publishes adapter/backend/capability, surface extent, and frame-copy route; `BrowserFrameLoop` separately exposes present, dispatch, worker, schedule, reference, horizon, uncertainty, edge, map-condition, and timing facts, but the accessors are distributed across the browser coordinator instead of one typed trace record (`crates/labs/julibrot/app/src/runtime.rs:21-38`, `crates/labs/julibrot/app/src/frame/loop/browser/facts.rs:4-272`).|

## 2. Seam map

### Classification summary

|Status|Seam|Classification|Fragment that exists today|
|------|----|--------------|--------------------------|
|Verified|J-01 AST edit|Partial|`RequestedControls`, `SavedView`, requested revision, and typed navigation edits exist, but edits call the concrete `ViewerOwner` rather than an epoch-stamped producer adapter (`crates/labs/julibrot/app/src/state.rs:45-75`, `crates/labs/julibrot/app/src/state.rs:298-320`, `crates/labs/julibrot/app/src/state.rs:411-435`).|
|Verified|J-02 ownership transfer|Real|`OwnerEndpoint`/`ProducerEndpoint`, four trailer-identified buffers, a browser port, a same-thread lowering, and returned credit form an actual ownership cut, although its payload is Julibrot orbit-specific (`crates/labs/julibrot/worker/src/channel.rs:60-136`, `crates/labs/julibrot/worker/src/channel.rs:150-399`, `crates/labs/julibrot/worker/src/channel.rs:401-580`).|
|Verified|J-03 owner/drain|Partial|`ViewerOwner` is a versioned single consumer with HOT and MAIN drain names, but both aliases return the same complete `ViewerState`, and app callers can reach through to the owner directly (`crates/labs/julibrot/worker/src/owner.rs:63-75`, `crates/labs/julibrot/worker/src/owner.rs:177-188`, `crates/labs/julibrot/worker/src/owner.rs:615-644`, `crates/labs/julibrot/app/src/state.rs:548-557`).|
|Verified|J-04 graph compilation|Aspirational|The direct browser coordinator turns drains into kernel and present calls; there is no resource/frame node record, dirty walk, graph edge, or compiler object in the scoped path (`crates/labs/julibrot/app/src/frame/loop.rs:1120-1365`; `docs/engine-design.md:257-269`, `docs/engine-design.md:296-320`).|
|Verified|J-05 hot publication|Real|`HotSlot` checks a three-entry dynamic-offset ring and carries the owner epoch; `Presenter::write_hot` writes exactly one 288-byte record to that offset and later scene/warp draws use it (`crates/labs/julibrot/present/src/uniform.rs:7-18`, `crates/labs/julibrot/present/src/uniform.rs:139-190`, `crates/labs/julibrot/present/src/gpu/device/warp.rs:44-227`).|
|Verified|J-06 scene descriptor|Partial|`PresentMain` and `SceneUniform` are live whole-grid regional descriptors, while version-one tile keys, pose headers, and pairs exist only as stage-0 values and tests; no regional tile descriptor is emitted (`crates/labs/julibrot/present/src/contract.rs:40-73`, `crates/labs/julibrot/present/src/uniform.rs:61-84`, `crates/labs/julibrot/present/src/tile.rs:64-222`, `crates/labs/julibrot/present/src/tile.rs:227-330`).|
|Verified|J-07 kernel job|Partial|Whole-grid planning/allocation/encoding is a callable kernels-owned cut, and wire-free `TileJob` policy exists, but the app issues refinement calls directly and no graph-issued tile job reaches `JulibrotKernels` (`crates/labs/julibrot/kernels/src/gpu.rs:75-134`, `crates/labs/julibrot/kernels/src/gpu.rs:135-269`, `crates/labs/julibrot/kernels/src/tile_job.rs:254-343`, `crates/labs/julibrot/app/src/frame/loop/browser/submit.rs:300-404`).|
|Verified|J-08 value/reconstruction|Partial, stage 0 only|Kernels defines paired non-aliasing output spans and two-sided completion, and present defines the exact `S0`/`S1` pair, but no GPU path allocates, fills, fences, or publishes the reconstruction span (`crates/labs/julibrot/kernels/src/tile_job.rs:378-470`, `crates/labs/julibrot/present/src/tile.rs:313-330`).|
|Verified|J-09 geometry/projection|Partial|The live scene consumes a `DataSpan` directory index through `SceneUniform`, builds extent-specific index buffers, and has full-chain CPU projection oracles, but its mesh is a whole `EscapeGrid`, not an explicit pose-header/tile handle (`crates/labs/julibrot/present/src/uniform.rs:196-258`, `crates/labs/julibrot/present/src/gpu/device/scene.rs:156-233`, `crates/labs/julibrot/present/src/planner.rs:834-996`).|
|Verified|J-10 scene publication|Real|A scene submission reserves a monotonic id and one of two texture slots, renders, arms a scene fence, and `poll_scene` publishes a `SceneFrame` with immutable pose/source identity only after completion (`crates/labs/julibrot/present/src/gpu/device/scene/submit.rs:103-200`, `crates/labs/julibrot/present/src/gpu/device/poll.rs:41-117`, `crates/labs/julibrot/present/src/contract.rs:163-190`).|
|Verified|J-11 reprojection/composition|Partial|The live path plans a whole-frame homography/redraw/hold/clear, writes HOT, reprojects values, shades once, and fences the warp; backdrop/main use one render pass, but there is no per-tile full-target reconstruction or same-depth multi-view composition (`crates/labs/julibrot/present/src/planner.rs:46-109`, `crates/labs/julibrot/present/src/gpu/device/warp.rs:268-476`, `crates/labs/julibrot/present/src/gpu/device/scene.rs:235-303`).|
|Verified|J-12 surface|Real|App claims one generation-tagged token before acquisition, retains the `SurfaceTexture` under the returned warp id, and presents only a matching completed warp outside the measured fence interval (`crates/labs/julibrot/app/src/surface.rs:9-137`, `crates/labs/julibrot/app/src/runtime.rs:248-329`, `crates/labs/julibrot/app/src/frame/loop.rs:1307-1357`, `crates/labs/julibrot/app/src/frame/loop.rs:1465-1508`).|

### J-01 — AST edit seam

**Verified.** Concrete producers are `SavedView::capture`/`lerp` and the `ViewerController` setters for zoom, pan, object/origin, centre, iteration cap, precision, palette, VIEW, preset, and a sampled reference. `ViewerController` owns the requested record, a saturating `u64 requested_revision`, shadow HOT/MAIN records, and the concrete `ViewerOwner`; navigation creates `u32` generations and centre revisions inside the owner. Edits allocate only where bignum centres or saved `Vec` limbs are cloned/encoded, write `Cell`-backed staged HOT/MAIN and app fields, and cross no GPU fence (`crates/labs/julibrot/app/src/saved.rs:26-99`, `crates/labs/julibrot/app/src/saved.rs:170-280`, `crates/labs/julibrot/app/src/state.rs:411-435`, `crates/labs/julibrot/app/src/state.rs:530-840`, `crates/labs/julibrot/app/src/state.rs:1163-1262`).

**Proposed.** Make J-01 a real port by introducing one app-owned `ViewerEdit` plus `ViewerEditBatch { requested_revision, edit }` return from setters and one adapter method that applies a batch to J-03; keep `RequestedControls` and `SavedView` app-specific and do not put Julibrot fields into a graph core. This smallest change separates intent from owner mutation while retaining the exact setter behavior and revision sequence.

### J-02 — ownership-transfer seam

**Verified.** `BrowserFrameLoop::new` calls `WorkerChannel::new`, then `submit_pending_reference` sends `OrbitRequest` and `service_arrivals` receives/returns it; `OwnerEndpoint`/`ProducerEndpoint` own the public transfer. ABI version 3, request generation, `OwnerCore::pool_epoch`, immutable pool/slot trailer, and `WorkerFacts.epoch` guard identity. Startup allocates exactly two request and two orbit buffers at `max(644,64+8M)` and resize replaces all four; request/header/orbit/fact bytes are written before `post_message_with_transfer` or queue delivery, and credit rewrites the returned header. There is no GPU fence; detachment plus slot ownership and the bounded drain are the synchronization (`crates/labs/julibrot/app/src/frame/loop.rs:864-875`, `crates/labs/julibrot/app/src/frame/loop/browser/reference.rs:121-178`, `crates/labs/julibrot/app/src/frame/loop/browser/reference.rs:413-530`, `crates/labs/julibrot/worker/src/wire.rs:6-40`, `crates/labs/julibrot/worker/src/wire.rs:87-176`, `crates/labs/julibrot/worker/src/wire.rs:387-430`, `crates/labs/julibrot/worker/src/wire.rs:433-600`, `crates/labs/julibrot/worker/src/endpoint.rs:158-188`).

**Proposed.** The cut is already real; its smallest engine port is an `OwnedEditBuffer` transport trait implemented by existing request slots, leaving ABI-3 orbit codecs as the Julibrot adapter. Before extraction, route same-thread ownership through `OwnerCore` so the two lowerings share the state machine rather than only an equivalence assertion.

### J-03 — owner/drain seam

**Verified.** App setters and reference arrival handling are the writers; `ViewerController::drain_hot`, `drain_main`, and direct app calls are readers. `ViewerOwner` owns staged/published `ViewerState`, navigation state, latest `u32` request generation, `u32` centre revision, and checked `u64` publication epoch; both drain names call the same `drain`, copy all HOT and MAIN fields, and increment the shared epoch. The owner uses no event queue and needs no fence because its `Cell` records are same-thread, but bignum navigation inside its non-copy navigation state can allocate (`crates/labs/julibrot/worker/src/owner.rs:15-75`, `crates/labs/julibrot/worker/src/owner.rs:158-188`, `crates/labs/julibrot/worker/src/owner.rs:520-644`, `crates/labs/julibrot/app/src/state.rs:1264-1357`).

**Proposed.** Make J-03 real by replacing `owner()`/`owner_mut()` exposure with controller methods over a narrow `ViewerDrainPort` whose results distinguish an unchanged HOT observation from a MAIN publication while preserving the current full-record bytes and epoch sequence. No graph types are required for that extraction.

### J-04 — graph-compilation seam

**Verified.** No scoped owner drain produces a node, dependency, dirty bit, or stable graph identity: the frame loop drains HOT, installs `PresentMain`, services worker arrivals, calls kernels, submits a scene, then acquires/submits a surface warp in one imperative refresh. The design reference itself marks stable resource/frame graphs as missing and round one names J-04 for a future compiler (`crates/labs/julibrot/app/src/frame/loop.rs:1120-1365`; `docs/engine-design.md:257-269`, `docs/engine-design.md:296-320`).

**Proposed.** No bounded round-two or round-three change makes J-04 real. The smallest prerequisite is an app-local `ViewerWorkSet` with stable Julibrot resource keys and ordered intents. J-04 becomes real only when a later engine round defines resource/frame nodes, dependencies, dirty semantics, and a compiler that consumes that record.

### J-05 — hot-publication seam

**Verified.** Every browser refresh can call `ViewerController::drain_hot`, construct `HotSlot::for_refresh(refresh_id, stride, epoch)`, and call `Presenter::write_hot` up to three times as newly accepted state changes the prepared extent or draft decision. Present owns the fixed three-slot allocation (`hot_ring_bytes`), the opaque index/dynamic offset, and the 288-byte `HotUniform`; versions are refresh id modulo three plus owner `u64` epoch. The only GPU write is `queue.write_buffer` to that slot. It has no separate fence; queue ordering makes it visible to the following scene or warp submission, whose own fence proves consumption (`crates/labs/julibrot/app/src/frame/loop.rs:1148-1270`, `crates/labs/julibrot/present/src/uniform.rs:7-18`, `crates/labs/julibrot/present/src/uniform.rs:116-190`, `crates/labs/julibrot/present/src/gpu/device/warp.rs:190-227`).

**Proposed.** Preserve the real cut and expose only `HotPublication { epoch, slot, bytes }` behind a fixed-identity writer; callers should not learn ring offsets. The immediate debt is to record the byte payload and write order in the oracle, not to change allocation or fencing.

### J-06 — scene-descriptor seam

**Verified.** `BrowserFrameLoop::install_main` is the concrete emitter: it selects main/backdrop whole grids and calls `Presenter::set_main(PresentMain)`. App owns selection policy; kernels owns each `EscapeGrid` span; present owns `PresentMain`, `SceneUniform`, `TileContentKey::VERSION=1`, `TileRenderKey::VERSION=1`, and `DescriptorCostLedger::ABI_VERSION=1`. The live arm clones span handles and writes one or two 160-byte scene uniforms at scene submission; the tile arm allocates and writes nothing and has no publication fence (`crates/labs/julibrot/app/src/frame/loop/browser/submit.rs:25-105`, `crates/labs/julibrot/present/src/contract.rs:27-73`, `crates/labs/julibrot/present/src/gpu/device/scene/submit.rs:39-100`, `crates/labs/julibrot/present/src/gpu/device/scene/submit.rs:133-148`, `crates/labs/julibrot/present/src/tile.rs:64-87`, `crates/labs/julibrot/present/src/tile.rs:141-218`, `crates/labs/julibrot/present/src/tile.rs:333-366`).

**Proposed.** Make the partial seam real by defining one engine-neutral regional descriptor record in kernels, adding a whole-grid-to-one-region adapter, and making `set_main` consume a slice of those records. Retain `PresentMain` as the old arm until descriptor bytes, scene identity, and pixels agree.

### J-07 — kernel-job seam

**Verified.** `BrowserFrameLoop::submit_due_scene` directly chooses shallow versus perturbation and calls `JulibrotKernels::encode_shallow` or `encode_perturbation`; kernels owns plans, span/header allocations, cached dispatch templates, accepted reference identity, and the mutable `EscapeGrid`. Versions are owner epoch, refinement rung, reference generation, requested/delivered caps, and `DataSpan` generation. Allocation reserves one or an atomic pair of Final-capacity spans and three header sets; dispatch writes a kernel uniform, encodes scratch work and exact DATA copies, then app submits that encoder before present submits its scene. There is no kernel-specific fence; same-queue order places the later scene fence after the kernel copy (`crates/labs/julibrot/app/src/frame/loop/browser/submit.rs:300-414`, `crates/labs/julibrot/kernels/src/gpu.rs:20-70`, `crates/labs/julibrot/kernels/src/gpu.rs:135-269`, `crates/labs/julibrot/kernels/src/gpu.rs:271-447`, `crates/labs/julibrot/kernels/src/gpu.rs:681-702`).

**Proposed.** Make the J-07 job-record and publication-receipt boundary concrete by adding a `KernelJob` enum whose whole-grid variant carries exactly today's arguments and whose tile variant wraps the existing `TileJob`; return an encoded publication receipt rather than mutating app-selected grid state. The first adapter must call the same methods so queue submissions and scene-fence order remain byte-for-byte observable. This makes the job-record and publication-receipt boundary concrete, but J-07 remains app-issued and partial until J-04 supplies the graph issuer.

### J-08 — value/reconstruction seam

**Verified.** Stage 0 names `PairedOutputSpanPlan`, `PairedOutputCompletion`, `PublishedTileOutputs`, and `DescriptorSamplePair`; it checks equal sample lengths, non-aliasing spans, and two-sided completion, with `S0` retaining the current value lanes and `S1` carrying lifted reconstruction. No scoped call outside `tile_job.rs` and `tile.rs` tests constructs these records, and `JulibrotKernels` has only one live output span per grid. Consequently there are no live allocations, GPU writes, completion fences, or published generations for reconstruction (`crates/labs/julibrot/kernels/src/tile_job.rs:378-470`, `crates/labs/julibrot/kernels/src/gpu.rs:20-27`, `crates/labs/julibrot/present/src/tile.rs:313-330`).

**Proposed.** Complete the port by teaching `JulibrotKernels` to allocate two generation-tagged spans, encode value and reconstruction writes in one command transaction, and expose `PublishedTileOutputs` only behind the scene fence that follows both; first do this for one whole-grid descriptor view so no tiling/index policy enters stage 1.

### J-09 — geometry/projection seam

**Verified.** Scene submission is the caller: `SceneUniform::new` packages extent, refinement, cap, span directory index/logical length, plane, and source map; device scene code builds an index buffer per main/backdrop extent, binds the immutable heap group and the scene/HOT group, and draws the mesh. `Pose` supplies owner epoch and orbit generation; `DataSpan` supplies heap generation; indices are reallocated on extent changes, scene uniforms are rewritten regionally, and the enclosing scene command/fence covers the draw. CPU `project_scene_vertex_exact` is the present-owned full-chain oracle, but live geometry has no tile/header identity (`crates/labs/julibrot/present/src/uniform.rs:196-258`, `crates/labs/julibrot/present/src/gpu/device/scene.rs:156-233`, `crates/labs/julibrot/present/src/gpu/device/scene.rs:235-376`, `crates/labs/julibrot/present/src/planner.rs:834-996`).

**Proposed.** Make the port complete by changing the draw input from an `EscapeGrid` plus implicit pose to `{ mesh_handle, value_span, reconstruction_span, pose_header, active_index }`, with a one-record adapter for the retained whole grid. Keep mesh construction and full-chain projection present-owned.

### J-10 — scene-publication seam

**Verified.** `Presenter::submit_scene` owns validation, chooses a free ledger texture, advances `u64 next_scene_id`, captures pose/centre revision/plane origin/precision and a clone of the grid in `PendingScene`, rewrites scene uniforms, renders, copies the census, clears and submits the four-byte scene fence, and arms its `FenceLedger`. `poll_scene` performs one cooperative poll, maps completion/refusal, promotes only a compatible pending scene, and publishes `SceneFrame` plus `PresentFacts`; scene texture, depth, census, and index allocations are extent-keyed and `texture_reallocations` records changed scene targets (`crates/labs/julibrot/present/src/gpu/device/scene/submit.rs:13-200`, `crates/labs/julibrot/present/src/gpu/device/poll.rs:20-28`, `crates/labs/julibrot/present/src/gpu/device/poll.rs:41-117`, `crates/labs/julibrot/present/src/gpu/device/poll.rs:172-205`).

**Proposed.** The real seam needs only an engine-neutral `CompletedScene` view over the existing immutable identity and source pose, plus the fixed two-event poll return. Do not move ledger compatibility or fences into app.

### J-11 — reprojection/composition seam

**Verified.** `Presenter::write_hot` calls `Warp::reproject` against the retained `SceneFrame`, applies coverage/hold/lattice policy, and writes the plan into HOT; `Presenter::frame` chooses the retained source, allocates/resizes the transient value target as needed, uploads palette, encodes relief redraw or image warp, shades once into the app surface, optionally encodes readback, clears/submits a four-byte warp fence, and returns `FrameReceipt` under a monotonic `u64 warp_id`. `poll_warp` publishes completion/refusal; app presents only afterward. The current scene compositor draws backdrop and main with separate viewport depth ranges/stencil, so it is not the study's physical target-depth competition among independently reprojected views (`crates/labs/julibrot/present/src/gpu/device/warp.rs:44-227`, `crates/labs/julibrot/present/src/gpu/device/warp.rs:268-476`, `crates/labs/julibrot/present/src/gpu/device/poll.rs:119-170`, `crates/labs/julibrot/present/src/gpu/device/scene.rs:235-303`).

**Proposed.** Stage 1 makes this a real tile-capable port with one source: reconstruct every retained descriptor vertex, execute the complete requested target chain, write target depth and semantic value in one pass, and shade the winning value once. Preserve the whole-frame `WarpPlan` arm behind the same `FrameReceipt` and warp-fence interface.

### J-12 — surface seam

**Verified.** `BrowserFrameLoop::refresh` is the caller; `BrowserRuntime` owns the `wgpu::Surface`, configuration, device/queue, and `SurfaceState`. `SurfaceState` owns a single token keyed by `u32` app generation and at most one `PendingSurface` keyed by `u64` present warp id. Acquisition creates a `SurfaceTexture`; retention itself allocates nothing; matching completion optionally offers the texture to readback, calls `present`, and releases the token, while refusal/cancellation drops it. The warp fence is present-owned, `PresentEvent::WarpCompleted` is the handoff, and presentation lies after the measurement (`crates/labs/julibrot/app/src/runtime.rs:41-49`, `crates/labs/julibrot/app/src/runtime.rs:248-329`, `crates/labs/julibrot/app/src/surface.rs:9-137`, `crates/labs/julibrot/app/src/frame/loop.rs:1307-1357`, `crates/labs/julibrot/app/src/frame/loop.rs:1465-1508`).

**Proposed.** Keep the real ownership state intact and expose it behind `SurfacePort::acquire`, `retain`, and `resolve(PresentEvent)`; the port should return the existing present/drop/ignore action so round two can extract it without changing fence or presentation order.

## 3. Debt inventory

### Classification basis

**Verified.** The current backlog contains 81 Julibrot bullets, not 78: 14 under `julibrot-math-kernels`, 27 under `julibrot-present`, and 40 under `julibrot-app-web` (`docs/plans/backlog.md:212-300`).

**Derived.** “Cleared-by-round-two” means a behaviour-preserving edit wholly within the round-two file set; “cleared-by-round-three” means stage-0 or stage-1 tile port work within the explicit round-three file set; “stays” means the defect, policy choice, platform measurement, or later tile stage remains after these rounds; and “obsolete” means the line records no remaining work. A row names the first seam blocked, then any coupled seam.

### `julibrot-math-kernels` — 14 rows

|Status|Row|Classification|Seam|Reason|
|------|---|--------------|----|------|
|Derived|MK-01, transposed ambient basis claim (`docs/plans/backlog.md:214`)|stays|J-11|Requires the named nonzero-rotation browser reproduction, not a structural refactor.|
|Derived|MK-02, missing second horizon test (`docs/plans/backlog.md:215`)|stays|J-11|Changes or records projection acceptance semantics outside the round-two source set.|
|Derived|MK-03, unconditional Interactive level (`docs/plans/backlog.md:216`)|stays|J-07|Removing a rung changes scheduling policy; round three retains the whole-grid arm.|
|Derived|MK-04, perturbation test ignores precision mode (`docs/plans/backlog.md:217`)|stays|J-07|The test is outside both rounds' named files and fixes a correctness oracle rather than a seam.|
|Derived|MK-05, verification digits owned by kernels (`docs/plans/backlog.md:218`)|stays|J-07|The constant and duplicate literal are outside the named round-two and round-three files.|
|Derived|MK-06, duplicated `origins_share_slice` (`docs/plans/backlog.md:219`)|stays|J-01/J-11|The second implementation is in an out-of-scope present state module and the pose choice is unresolved.|
|Derived|MK-07, incomplete exact-versus-fast corpus (`docs/plans/backlog.md:220`)|stays|J-07/J-11|Both rounds consume this oracle but do not define the missing cross-product.|
|Derived|MK-08, single-record observation bound (`docs/plans/backlog.md:221`)|stays|J-07|Requires a math perturbation oracle outside both file sets.|
|Derived|MK-09, no wasm32-wasip1 lane (`docs/plans/backlog.md:222`)|stays|J-02|This is platform coverage, not code in either refactoring round.|
|Derived|MK-10, projection clamp is not model clipping (`docs/plans/backlog.md:223`)|stays|J-09|The model-space policy study remains unresolved.|
|Derived|MK-11, no mesh sensitivity sweep (`docs/plans/backlog.md:224`)|stays|J-09/J-11|The evidence needed to replace apron policy is still absent after stage 1.|
|Derived|MK-12, zoom-14 ground truth and rebasing (`docs/plans/backlog.md:225`)|stays|J-07/J-08|It combines an external measurement and undesigned numerical behavior.|
|Derived|MK-13, no GPU texel-load timing path (`docs/plans/backlog.md:226`)|stays|J-07|Neither round adds timestamp capability or changes heap addressing.|
|Derived|MK-14, browser valid-row residue proof (`docs/plans/backlog.md:227`)|stays|J-02/J-07|Requires both browser backends and is independent of port extraction.|

### `julibrot-present` — 27 rows

|Status|Row|Classification|Seam|Reason|
|------|---|--------------|----|------|
|Derived|P-01, submitted extent and pose unrelated (`docs/plans/backlog.md:231`)|stays|J-09/J-10|Adding rejection changes the submission contract and needs a correctness decision.|
|Derived|P-02, scale invariant skipped on exposure (`docs/plans/backlog.md:232`)|stays|J-11|The exposure policy is undocumented and not behaviour-preserving.|
|Derived|P-03, neutral height for refused records (`docs/plans/backlog.md:233`)|stays|J-09/J-11|The desired semantic height is undecided and the shader/mesh files are outside scope.|
|Derived|P-04, clamped vertex still counted (`docs/plans/backlog.md:234`)|stays|J-09|The inconsistency lies in out-of-scope math footprint policy.|
|Derived|P-05, zero-lift clip-w shortcut (`docs/plans/backlog.md:235`)|stays|J-09/J-11|The retained whole-grid planner still needs a separate correctness fix.|
|Derived|P-06, height refusal abandons warp corpus (`docs/plans/backlog.md:236`)|stays|J-11|Replacing fail-closed behavior needs the missing two-direction corpus.|
|Derived|P-07, exposed-fraction units unpinned (`docs/plans/backlog.md:237`)|stays|J-11|It is a missing adversarial fixture, not debt removed by extraction.|
|Derived|P-08, tautological steep-row assertion (`docs/plans/backlog.md:238`)|stays|J-09|The native test file is outside the two edit sets.|
|Derived|P-09, frame-oracle figures use its own records (`docs/plans/backlog.md:239`)|stays|J-09/J-11|Round-three parity consumes the oracle but does not make its source delivered data.|
|Derived|P-10, frame-oracle arithmetic/clear mismatch (`docs/plans/backlog.md:240`)|stays|J-09/J-11|Binary32, clipping, and depth-clear semantics remain an oracle decision.|
|Derived|P-11, no composed-backdrop frame oracle (`docs/plans/backlog.md:241`)|stays|J-11|Stage 1 retains one whole descriptor view; protected backdrop arrives later.|
|Derived|P-12, brittle dynamic-offset source census (`docs/plans/backlog.md:242`)|cleared-by-round-two|J-05|Round two replaces the hard-coded source-file census by making dynamic versus static uniform construction structural: only HOT-specific constructors may set `has_dynamic_offset`, all other constructors expose no dynamic boolean, and focused tests enumerate the resulting layouts. The app trace separately records HOT slot/write order.|
|Derived|P-13, unused planner parameters (`docs/plans/backlog.md:243`)|cleared-by-round-two|J-11|Remove the stale arguments after recording planner-result equality.|
|Derived|P-14, unpinned scene clear-colour call site (`docs/plans/backlog.md:244`)|cleared-by-round-two|J-10|A device-test command-record assertion can pin the call without changing it.|
|Derived|P-15, inverse-row only neutral (`docs/plans/backlog.md:245`)|cleared-by-round-three|J-09/J-11|Stage-1 full-target projection adds rotated and translated source self-reconstruction cases.|
|Derived|P-16, unbounded texture reallocations (`docs/plans/backlog.md:246`)|stays|J-06/J-10|A ceiling is resource policy and stage 1 deliberately retains the whole-grid arm.|
|Derived|P-17, split tile vocabulary (`docs/plans/backlog.md:247`)|cleared-by-round-three|J-06/J-08|Stage 0 establishes one engine-neutral tile-record vocabulary.|
|Derived|P-18, no depth-bearing retained view (`docs/plans/backlog.md:248`)|cleared-by-round-three|J-08/J-11|This is exactly the stage-1 deliverable.|
|Derived|P-19, narrow relief admission calibration (`docs/plans/backlog.md:249`)|stays|J-11|A broader native corpus remains necessary before policy changes.|
|Derived|P-20, final-resolution records apron (`docs/plans/backlog.md:250`)|stays|J-06/J-11|This is later backdrop/cache policy, not stage 0 or 1.|
|Derived|P-21, saturated-relief behavior undecided (`docs/plans/backlog.md:251`)|stays|J-09/J-11|The presentation policy remains an open study question.|
|Derived|P-22, no browser palette readback (`docs/plans/backlog.md:252`)|stays|J-11|The captured oracle must run it, but neither refactor itself supplies a browser device.|
|Derived|P-23, shade-only GPU time unavailable (`docs/plans/backlog.md:253`)|stays|J-11|The WebGL2 capability limit remains.|
|Derived|P-24, relief-redraw horizon ring stays Clear (`docs/plans/backlog.md:254`)|stays|J-09/J-11|A status-producing destination path is a semantic extension beyond stage 1.|
|Derived|P-25, partly-refused primitive capture (`docs/plans/backlog.md:255`)|stays|J-09/J-11|It remains a device measurement prerequisite.|
|Derived|P-26, slow-morph browser census (`docs/plans/backlog.md:256`)|stays|J-11|It remains a served-browser measurement.|
|Derived|P-27, diagonal streak device reproduction (`docs/plans/backlog.md:257`)|stays|J-09|The candidate mesh and shader files are outside both rounds.|

**Verified.** Backlog correction P-12: the backlog's embedded `device/tests.rs:848` pointer has drifted, while `every_gpu_dynamic_offset_comes_from_the_opaque_slot` now begins at line 1076 (`docs/plans/backlog.md:242`, `crates/labs/julibrot/present/src/gpu/device/tests.rs:1075-1086`).

**Proposed.** A docs-only follow-up corrects that source pointer; the drift does not change P-12's classification or this survey's code finding.

### `julibrot-app-web` — 40 rows

|Status|Row|Classification|Seam|Reason|
|------|---|--------------|----|------|
|Derived|A-01, integrated lockfile edge (`docs/plans/backlog.md:261`)|obsolete|J-11|The row states that the dependency adds no registry crate and names no remaining action.|
|Derived|A-02, clear-only stamps requested view (`docs/plans/backlog.md:262`)|stays|J-11/J-12|Changing the stamp changes finished-picture semantics.|
|Derived|A-03, freed main-grid field remains populated (`docs/plans/backlog.md:263`)|cleared-by-round-two|J-07/J-10|Make the field optional and take it on retirement, preserving the same frees and submissions.|
|Derived|A-04, backdrop replan key omits delivered extent (`docs/plans/backlog.md:264`)|stays|J-07|Adding the key can change scheduling and scene output.|
|Derived|A-05, lifted screen pan uses zero height (`docs/plans/backlog.md:265`)|stays|J-01/J-11|The correct navigation surface is a behavior decision.|
|Derived|A-06, corner zoom waits on coverage-first backdrop (`docs/plans/backlog.md:266`)|stays|J-06/J-07|Demand-ordered scheduling is stage 4, beyond round three.|
|Derived|A-07, delivered precision temporarily unavailable (`docs/plans/backlog.md:267`)|stays|J-03|Changing fact meaning is observable and needs a specified epoch source.|
|Derived|A-08, rebase counts hard-coded absent (`docs/plans/backlog.md:268`)|stays|J-07|No current producer measures the values.|
|Derived|A-09, newer present facts not forwarded (`docs/plans/backlog.md:269`)|stays|J-10/J-12|Round two requires unchanged facts, so new public fact fields remain separate work.|
|Derived|A-10, held-frame facts not forwarded (`docs/plans/backlog.md:270`)|stays|J-10|It changes the browser fact schema.|
|Derived|A-11, timing ledger lacks dispatch/transfer walls (`docs/plans/backlog.md:271`)|stays|J-07/J-10|The measurements do not exist and fence-observation semantics remain.|
|Derived|A-12, no worker admission timestamp (`docs/plans/backlog.md:272`)|stays|J-02|It requires a frozen-wire revision.|
|Derived|A-13, orbit response lacks escaped flag (`docs/plans/backlog.md:273`)|stays|J-02/J-03|It requires an ABI revision and changes acceptance evidence.|
|Derived|A-14, repeated too-long reference requests (`docs/plans/backlog.md:274`)|stays|J-02/J-07|A pre-test changes producer policy.|
|Derived|A-15, correction census not retained across navigation (`docs/plans/backlog.md:275`)|stays|J-03/J-07|Cross-navigation cache policy is not behaviour-preserving.|
|Derived|A-16, correction consumes navigation generation (`docs/plans/backlog.md:276`)|stays|J-01/J-03|A new generation class changes the owner protocol.|
|Derived|A-17, regional second-reference correction absent (`docs/plans/backlog.md:277`)|stays|J-07/J-08|This is a future rendering feature, not stage-1 reconstruction.|
|Derived|A-18, deterministic promotion disabled (`docs/plans/backlog.md:278`)|stays|J-07|Activation changes scheduling and presented precision policy.|
|Derived|A-19, presenter poll allocates a vector (`docs/plans/backlog.md:279`)|cleared-by-round-two|J-10/J-12|Change the app trait and loop to consume the existing fixed two-event return.|
|Derived|A-20, worker clears all mutable return bytes (`docs/plans/backlog.md:280`)|stays|J-02|The implementation is in `worker/src/browser.rs`, outside round two.|
|Derived|A-21, orbit uses individual byte stores (`docs/plans/backlog.md:281`)|stays|J-02|The implementation is in `worker/src/browser.rs`, outside round two.|
|Derived|A-22, crosshair bignum work every refresh (`docs/plans/backlog.md:282`)|stays|J-01|The required browser profile is still missing.|
|Derived|A-23, scalar page set reparses saved row (`docs/plans/backlog.md:283`)|stays|J-01|The JavaScript page is outside both edit sets.|
|Derived|A-24, facts coupled by JSON names (`docs/plans/backlog.md:284`)|stays|J-01/J-12|The page and page-contract test are outside scope.|
|Derived|A-25, text-based page-contract sprawl (`docs/plans/backlog.md:285`)|stays|J-01/J-12|Round-two trace work supplies evidence but cannot remove the out-of-scope harness.|
|Derived|A-26, page cap option unpinned (`docs/plans/backlog.md:286`)|stays|J-07|The page-contract test is outside scope.|
|Derived|A-27, horizon share unpublished (`docs/plans/backlog.md:287`)|stays|J-09/J-10|No current fact producer carries it and adding one changes facts.|
|Derived|A-28, page mesh fact lacks dark/refused semantics (`docs/plans/backlog.md:288`)|stays|J-09/J-10|The public meaning remains undecided.|
|Derived|A-29, drive-page census placement undecided (`docs/plans/backlog.md:289`)|stays|J-12|This is an ownership decision outside the Rust file set.|
|Derived|A-30, hidden-tab timeout policy undecided (`docs/plans/backlog.md:290`)|stays|J-12|This is page scheduling policy.|
|Derived|A-31, fake presenter does not drive capture/predicate (`docs/plans/backlog.md:291`)|cleared-by-round-two|J-10/J-12|Extend the existing in-scope fake to record armed copies and the finished predicate.|
|Derived|A-32, wasm bridge duplicates codec layout (`docs/plans/backlog.md:292`)|stays|J-02|Both implementations named by the debt are outside round two's file set.|
|Derived|A-33, plane axes frozen in worker layout (`docs/plans/backlog.md:293`)|stays|J-03|Retirement requires the deferred byte-layout revision.|
|Derived|A-34, missing live timing-ledger run (`docs/plans/backlog.md:294`)|stays|J-07/J-12|It remains an external served-browser measurement.|
|Derived|A-35, missing both-origin present runs (`docs/plans/backlog.md:295`)|stays|J-10/J-12|It remains an external browser/device campaign.|
|Derived|A-36, deep-reference admission replay (`docs/plans/backlog.md:296`)|stays|J-02|It remains a browser policy measurement.|
|Derived|A-37, shallow-bypass browser saving (`docs/plans/backlog.md:297`)|stays|J-07|It remains a browser measurement.|
|Derived|A-38, edge-on scene-id resubmission (`docs/plans/backlog.md:298`)|stays|J-07/J-10|Adding a guard changes scheduling and needs the requested reproduction.|
|Derived|A-39, saved height-refusal page path (`docs/plans/backlog.md:299`)|stays|J-01/J-12|It remains a served-page integration case.|
|Derived|A-40, offscreen frame-copy device exercise (`docs/plans/backlog.md:300`)|stays|J-10/J-12|It remains a native-device or served-build measurement.|

**Derived.** Totals are 6 cleared-by-round-two, 3 cleared-by-round-three, 71 stay, and 1 obsolete. The deliberately conservative rule is that adding an oracle in a refactoring round does not silently clear a correctness, policy, or platform item unless the bounded plan below includes the actual repair.

### Debt absent from the backlog

|Status|Unlisted debt|Seam|Disposition|
|------|-------------|----|-----------|
|Verified|The paired public edit entries `pan_px`/`drag_pan` and `zoom_about_crosshair`/`wheel_zoom` name one operation twice (`crates/labs/julibrot/app/src/state.rs:563-625`, `crates/labs/julibrot/app/src/state.rs:702-714`).|J-01|Round two retains the app-facing names and narrows the implementation helpers after call-site and trace equality.|
|Verified|Public `owner()`/`owner_mut()` lets app scheduling, saving, reference handling, and submission bypass `ViewerController` (`crates/labs/julibrot/app/src/state.rs:548-557`).|J-03|Round two replaces scoped reach-through with narrow controller methods; S-02 records the remaining crate-level decision.|
|Verified|`ChannelCore` and `OwnerCore` independently implement request, return, resize, credit, and facts transitions (`crates/labs/julibrot/worker/src/channel.rs:901-1098`, `crates/labs/julibrot/worker/src/endpoint.rs:158-730`).|J-02|Round two makes same-thread delivery a port over `OwnerCore`.|
|Verified|Browser-owner construction reads worker URL and time from JavaScript globals (`crates/labs/julibrot/worker/src/browser_owner.rs:351-389`).|J-02|Round two injects both through construction while retaining current defaults at the app edge.|
|Verified|`BrowserFrameLoop` stores every lower-slice runtime object and policy record in one browser-only coordinator (`crates/labs/julibrot/app/src/frame/loop.rs:672-772`).|J-04/J-12|Round two extracts replayable transactions, but the general graph compiler stays future work.|
|Verified|The app, worker, and present harnesses retain events in unrelated fixture shapes and publish no checked-in cross-round trace (`crates/labs/julibrot/app/src/frame/loop/tests.rs:685-1124`, `crates/labs/julibrot/worker/src/channel.rs:1415-1460`, `crates/labs/julibrot/present/src/planner.rs:1001-1035`).|J-02/J-10/J-12|Round two creates the four oracle records before structural edits.|
|Verified|Stage-0 tile records are duplicated and test-only, with no paired reconstruction allocation or live descriptor consumer (`crates/labs/julibrot/kernels/src/tile_job.rs:345-470`, `crates/labs/julibrot/present/src/tile.rs:313-443`).|J-06/J-08|Round three centralizes the records and makes one whole-view descriptor live.|
|Verified|The fixed present-event return is hidden behind an allocating compatibility method in app (`crates/labs/julibrot/present/src/gpu/device/poll.rs:8-28`, `crates/labs/julibrot/app/src/frame/schedule.rs:277-282`).|J-10|Round two clears it together with A-19.|
|Verified|The entire J-04 graph compiler has no current data product or consumer (`crates/labs/julibrot/app/src/frame/loop.rs:1120-1365`).|J-04|It stays aspirational after both rounds; a Julibrot work-set record is the prerequisite, not the compiler itself.|

## 4. Oracle for rounds two and three

### What exists now

|Status|Required observation|Existing producer|Present evidence and limit|
|------|--------------------|-----------------|--------------------------|
|Verified|Worker event trace|`logical_trace_and_browser_binding_are_mode_equivalent_contracts` drives the same public request/complete/return sequence in `SameThread` and `WebWorker` modes; the `OwnerCore` `FakePort`/`Harness` separately drives transport controls, resize, credit, cancellation, and shutdown (`crates/labs/julibrot/worker/src/channel.rs:1415-1460`, `crates/labs/julibrot/worker/src/endpoint.rs:732-1034`).|It compares one observed four-tuple and normalized `WorkerFacts`; it does not retain the ordered submit, admission, transfer, completion, return, resize, fact-epoch, and ownership events required by the design oracle.|
|Verified|App frame-loop trace|`FakePresenter`, `FakeClock`, `drive_turn`, `drive_refresh`, and `drive_viewer_harness` exercise poll-before-submit, refinement, warp, refusal, retention, and presentation; `ZoomScript` and the height-drag driver add longer scenarios (`crates/labs/julibrot/app/src/frame/loop/tests.rs:685-1124`, `crates/labs/julibrot/app/src/frame/loop/tests.rs:2402-2927`, `crates/labs/julibrot/app/src/frame/loop/tests.rs:3148-3406`).|The fakes keep separate counters and vectors, and each test asserts a subset; no one value records the complete per-turn HOT writes, accepted epochs, scene identities, fence observations, surface actions, and facts.|
|Verified|Present planner result|`Warp::reproject` is pure, `WarpPlan` is `PartialEq`, and planner tests cover homography, relief, lattice, exposure, refusal, and direct full-chain projection; the native `reprojection_oracle`, `steep_row`, and `steep_frame` tests provide independent numerical and pixel-shaped oracles (`crates/labs/julibrot/present/src/planner.rs:45-109`, `crates/labs/julibrot/present/src/contract.rs:219-257`, `crates/labs/julibrot/present/src/planner.rs:1001-2617`, `crates/labs/julibrot/present/tests/reprojection_oracle.rs:1`, `crates/labs/julibrot/present/tests/steep_row.rs:1`, `crates/labs/julibrot/present/tests/steep_frame.rs:1`).|Tests assert selected fields and tolerances but do not serialize a named input/result corpus or compare old and extracted planner arms.|
|Verified|Whole-grid pixels and facts|`final_conformance` compares exact and picture-fast records; `steep_row` and `steep_frame` provide CPU placement/frame evidence; the device test `two_palettes_recolour_one_completed_scene_through_the_offscreen_route` reads a native rendered frame; `drive.html` settles a row, calls `lab.frame`, checks copied and completed scene ids, computes a census, and publishes facts (`crates/labs/julibrot/app/tests/final_conformance.rs:1`, `crates/labs/julibrot/present/tests/steep_row.rs:242-492`, `crates/labs/julibrot/present/tests/steep_frame.rs:1452-1871`, `crates/labs/julibrot/present/src/gpu/device/tests.rs:385-458`, `web/labs/julibrot/drive.html:70-97`).|`lab.frame` returns the raw RGBA bytes, route, extent, and scene id, but the drive event retains only facts and four census totals; no checked-in runner stores or compares the raw frame and stable fact subset (`web/labs/julibrot/lab.js:417-450`, `web/labs/julibrot/drive.html:82-97`).|

### Availability verdict and formats round two must establish first

**Derived.** Measurement unavailable for the required worker event trace. Before the J-02 state-machine edit, round two must add a deterministic `WorkerTraceEvent` enum in the `channel.rs` test module and make a shared scenario return `Vec<WorkerTraceEvent>` plus `WorkerFacts`; events must include call/result, buffer identity and owner, generation, pool epoch, fact epoch, credit, queue depths, and allocation count, with elapsed microseconds normalized only where the test deliberately supplies them.

**Derived.** Measurement unavailable for the required app frame-loop trace. Before extracting J-03/J-12 ports, round two must add one `FrameTraceTurn` record to `frame/loop/tests.rs` containing input time, drained HOT/MAIN epochs, HOT-write count/order, due rung, submitted/completed/refused scene id, submitted/completed warp id and kind, retained source id, `SurfaceAction`, fence-observation order, and the stable facts snapshot; existing short, zoom, and height drivers must all emit that record.

**Derived.** Measurement unavailable for a captured, directly comparable planner result. Before deleting a planner argument or routing through a port, round two must define a named input corpus in `planner.rs` tests and assert equality of complete `WarpPlan` values from the current and extracted entries; floating fields use exact equality because both arms call the same current implementation during extraction, while independent numerical oracles retain their existing tolerances.

**Derived.** Measurement unavailable for a repeatable pre/post whole-grid pixel-and-facts artifact. Round two must first define a versioned capture record containing scenario id, extent, capture route, completed/copied scene ids, raw RGBA bytes or a lossless file plus a cryptographic digest, and a normalized facts object; normalization may remove only explicitly nondeterministic wall times and device strings, never counts, epochs, identities, extents, modes, fence order, or pixel bytes. The current Rust and browser harnesses can supply the fields, but an allowed location for the browser runner/golden is an open scope question because round two otherwise edits only the named Rust files.

**Proposed.** Use line-delimited JSON with a schema number for externally captured browser records and ordinary strongly typed Rust values for in-process traces. JSON makes a before/after artifact inspectable without imposing a serialization dependency on production records; the Rust comparisons keep NaN and enum behavior explicit. A whole-grid comparison passes only when worker and app traces are equal, `WarpPlan` values are equal, scene ids and fence order are equal, stable facts are equal, raw RGBA digests are equal for deterministic device/backend captures, and existing cross-device pixel tolerances still pass where exact bytes are not promised.

### Exact verification commands

**Verified.** These are the current native/unit harness commands; package names come from the three crate manifests, and each filter names an existing test/module (`crates/labs/julibrot/worker/Cargo.toml:2`, `crates/labs/julibrot/app/Cargo.toml:2`, `crates/labs/julibrot/present/Cargo.toml:2`).

```text
cargo test -p ember-julibrot-worker --lib logical_trace_and_browser_binding_are_mode_equivalent_contracts
cargo test -p ember-julibrot-app --lib 'frame::loop::tests::'
cargo test -p ember-julibrot-present --lib 'planner::tests::'
cargo test -p ember-julibrot-present --test reprojection_oracle
cargo test -p ember-julibrot-present --test steep_row --test steep_frame
cargo test -p ember-julibrot-app --test final_conformance
cargo test -p ember-julibrot-present --lib two_palettes_recolour_one_completed_scene_through_the_offscreen_route
```

**Verified.** The served proof uses the repository's release build/bindgen sequence, then the driver page's own settle/capture path (`deploy/deploy-pages.sh:88-109`, `web/labs/julibrot/drive.html:70-97`).

```text
cargo build --target wasm32-unknown-unknown --release -p ember-julibrot-app --lib
wasm-bindgen --target web --no-typescript --out-dir web/labs/julibrot/pkg target/wasm32-unknown-unknown/release/ember_lab_julibrot.wasm
python3 -m http.server 8000 --directory web
```

**Derived.** With that server running, the exact existing interactive operation is to open `http://127.0.0.1:8000/labs/julibrot/drive.html`, post a `JulibrotRow` message or supply the page's encoded `row` query, and wait for `julibrot-settled`. It proves a visible settled frame and census, but because no repository command drives the page and saves raw RGBA plus normalized facts, the automated browser comparison remains measurement unavailable until the first round-two bite.

**Derived.** None of these commands is to be run in round one: the lane has no compiler, the deliverable is a read-only source survey, and this section records commands for the later execution environment rather than claiming fresh results.

## 5. Plan for rounds two and three

### Round two — behaviour-preserving debt clearing

**Verified.** Round two is limited to debt in the round-one files and must preserve worker events and accepted epochs, whole-grid descriptor bytes and scene identities, planner results, fence order, facts, and existing pixel tolerances (`docs/engine-design.md:298-302`).

**Proposed.** R2-04 extracts worker service, kernel submission, present-event handling, and surface resolution as four separately reviewed sweeps, each passing the complete app trace before the next begins.

|Status|Order and bounded bite|Files|Required oracle|Estimate|
|------|----------------------|-----|---------------|-------:|
|Proposed|R2-01a, freeze the worker trace as an ordered typed ownership-event record.|`worker/src/channel.rs`, `worker/src/endpoint.rs`|Same-thread and browser-mode scenarios return equal event sequences and normalized `WorkerFacts`, including buffer ownership, generations, fact epochs, credit, queue depths, and allocations.|2–3 worker-hours|
|Proposed|R2-01b, freeze the app frame trace and extend the fake presenter through armed copy and the finished-picture predicate.|`app/src/frame/loop/tests.rs`|Short, zoom, height, capture, and finished-picture scenarios return complete equal per-turn records with HOT/MAIN epochs, scene/warp ids, fence observations, surface actions, and stable facts.|2–3 worker-hours|
|Proposed|R2-01c, freeze a named paired planner corpus over complete `WarpPlan` values.|`present/src/planner.rs`|Current and extracted entries return exactly equal plans for every named input while the independent numerical oracles keep their existing tolerances.|2–3 worker-hours|
|Proposed|R2-01d, freeze native and served whole-grid captures, including stable fact normalization and one current served row with raw RGBA. Pin the scene encoder's clear operation to `scene_load_color()` at the render-pass call site, not only the helper's returned value.|`present/src/gpu/device/tests.rs` plus the S-01-approved runner/artifact location|Native pixels pass; the served artifact includes raw RGBA, route, extent, matching copied/completed scene ids, and stable facts; the call-site clear pin passes. No structural bite starts until all four R2-01 baselines pass.|4–5 worker-hours|
|Proposed|R2-02, put same-thread and browser transfer over one `OwnerCore`: adapt the same-thread port, remove `ChannelCore`'s duplicate transitions, inject browser URL/clock construction inputs at the adapter, and retain ABI-3 bytes, four buffers, credit, and public endpoint behavior.|`worker/src/channel.rs`, `worker/src/endpoint.rs`, `worker/src/browser_owner.rs`, `worker/src/wire.rs`|Exact worker trace, normalized `WorkerFacts`, allocation count, buffer-owner sequence, accepted generations, and wire-layout tests are unchanged.|12–18 worker-hours|
|Proposed|R2-03, narrow the viewer/owner boundary: make drag/wheel implementation entries internal behind the existing app-facing pan/zoom entries, add controller methods for precision, pending-depth, navigation acceptance, and reference state, and migrate every in-scope `owner()`/`owner_mut()` caller without changing requested revisions or worker generations.|`app/src/state.rs`, `app/src/saved.rs`, `app/src/frame/schedule.rs`, `app/src/frame/loop.rs`, `app/src/frame/loop/browser/reference.rs`, `app/src/frame/loop/browser/submit.rs`, `app/src/frame/loop/tests.rs`|Worker and app traces are identical, including HOT/MAIN epochs, accepted generations, reference dispositions, scene ids, and saved-row round trips.|8–12 worker-hours|
|Proposed|R2-04a, extract worker service as an app-local replayable transaction owner.|`app/src/frame/loop.rs`, `app/src/frame/loop/browser/reference.rs`, `app/src/frame/loop/browser/facts.rs`, `app/src/frame/loop/tests.rs`|The complete app trace and worker trace pass before R2-04b, with identical accepted generations, reference dispositions, event order, and facts.|4–6 worker-hours|
|Proposed|R2-04b, extract kernel submission as an app-local replayable transaction owner.|`app/src/frame/loop.rs`, `app/src/frame/loop/browser/backdrop.rs`, `app/src/frame/loop/browser/submit.rs`, `app/src/frame/loop/tests.rs`|The complete app trace passes before R2-04c, with identical dispatch facts, span generations, scene ids, and queue order.|4–5 worker-hours|
|Proposed|R2-04c, extract present-event handling as an app-local replayable transaction owner.|`app/src/frame/loop.rs`, `app/src/frame/loop/browser/facts.rs`, `app/src/frame/schedule.rs`, `app/src/frame/loop/tests.rs`|The complete app trace passes before R2-04d, with identical completion/refusal order, retained source, fence observations, and stable facts.|3–4 worker-hours|
|Proposed|R2-04d, extract surface resolution as an app-local replayable transaction owner and move the three warp scheduling predicates beside `FrameLoop`; retain acquire, warp submit, matching completion, present/drop, and fence order.|`app/src/frame.rs`, `app/src/frame/loop.rs`, `app/src/frame/schedule.rs`, `app/src/frame/warp.rs`, `app/src/runtime.rs`, `app/src/surface.rs`, `app/src/frame/loop/tests.rs`|The complete app trace, served whole-grid capture, matching `SurfaceAction`, scene/warp identities, and fence order pass before R2-05.|3–5 worker-hours|
|Proposed|R2-05, remove measurable compatibility debt: consume `PresentEvents` directly instead of allocating `Vec`, remove unread precision/validation planner parameters through the paired entry, and make dynamic versus static uniform construction structural so only HOT-specific constructors can set `has_dynamic_offset` and all other constructors expose no dynamic boolean.|`app/src/frame/schedule.rs`, `app/src/frame/loop.rs`, `app/src/frame/loop/tests.rs`, `present/src/planner.rs`, `present/src/gpu/device/poll.rs`, `present/src/gpu/device/uniforms.rs`, `present/src/gpu/device/tests.rs`|Complete planner equality, app trace equality including poll and HOT slot/write order, focused enumeration of every resulting uniform layout, and unchanged native pixels/facts.|5–8 worker-hours|
|Proposed|R2-06, make freed grid ownership structural and consolidate semantic test fixtures: change the retired main-grid pair to an explicit absent state, preserve restoration/failure paths, and make short, zoom, and height scenarios reuse the trace record and fixture constructors.|`app/src/frame/loop.rs`, `app/src/frame/loop/browser/submit.rs`, `app/src/frame/loop/tests.rs`|App trace, dispatch facts, grid span generations, scene identities, fence order, capture route, and whole-grid pixels/facts are unchanged.|6–10 worker-hours|
|Proposed|R2-07, parity closeout: replay all named trace corpora through the retained entries, run every current crate/native oracle command, and compare a fresh served capture with the R2-01d artifact before deleting only now-unreferenced compatibility code.|All files touched by R2-01a–R2-06; no new scope|All four oracle classes pass together; any mismatch keeps the old entry and returns the responsible bite for correction.|4–6 worker-hours|

**Derived.** Round two totals 59–88 worker-hours. R2-03 can remove all reach-through in the named survey files, but not the `app/src/facts.rs` reader or the public pan/zoom callers in `app/src/lib.rs`; treating “app reach-through” as a crate-wide requirement therefore needs the scope answer recorded below.

### Round three — engine-neutral records and ports, whole-grid arm retained

**Verified.** Round three may edit `math/src/reprojection.rs`, kernels tile-job/GPU files, present tile/planner/device files, and app frame files; it must prove whole-grid parity before tile behavior and retain the previous arm. The tiled study defines stage 0 as policy/math at 20–28 lane-hours and stage 1 as one depth-bearing whole-screen descriptor view at 36–52 lane-hours, explicitly keeping the old homography path switchable (`docs/engine-design.md:302-306`, `docs/julibrot/tiled-reprojection.md:347-355`).

#### Stage 0 — rendered-view policy and math

|Status|Order and bounded bite|Files|Required oracle|Estimate|
|------|----------------------|-----|---------------|-------:|
|Proposed|R3-01, define one engine-neutral record vocabulary: canonical content/render/source identities, signed source rectangle, mesh handle, versioned pose header, paired value/reconstruction span identities, residency/quality, invalidation, and exact descriptor ABI; keep present re-exports as adapters so the whole-grid API remains intact.|`kernels/src/tile_job.rs`, `present/src/tile.rs`|Existing tile tests plus exact version, 512-byte header-slot, `S0`/`S1` lane, non-aliasing, key equality, invalidation, and descriptor pack/unpack tests; whole-grid traces remain equal.|6–8 worker-hours|
|Proposed|R3-02, implement pure descriptor packing, source-sample reconstruction, source self-round-trip, and requested-pose full-chain projection, sharing the current five-dimensional order rather than copying planner algebra.|`math/src/reprojection.rs`, `present/src/tile.rs`, `present/src/planner.rs`|Descriptor round trip, exact-in-f32 lanes, compensated-split tolerance, source-pixel/depth return, direct-f64 target comparison at or below 1.0 px, poles, and all existing planner results.|6–8 worker-hours|
|Proposed|R3-03, finish stage-0 policy values: derived chart footprint/density, slice validity, same-surface ownership independent of input order, demand-key tie breaking, reference leases, and exact resource arithmetic; no live scheduler or GPU arm yet.|`kernels/src/tile_job.rs`, `present/src/tile.rs`, `app/src/frame/schedule.rs`, `app/src/frame/loop/tests.rs`|Invalidation matrix, cost totals, stable demand permutations, same-surface quality permutations, lease lifetime, footprint conservatism, and unchanged whole-grid app trace/facts.|8–12 worker-hours|

**Derived.** Stage 0 totals 20–28 worker-hours and changes no served rendering path.

#### Stage 1 — one depth-bearing retained whole view

|Status|Order and bounded bite|Files|Required oracle|Estimate|
|------|----------------------|-----|---------------|-------:|
|Proposed|R3-04, make paired outputs live for one whole grid: allocate generation-tagged `S0` and `S1` spans together, encode both in one kernel transaction, and publish neither until both completions are represented by one receipt.|`kernels/src/tile_job.rs`, `kernels/src/gpu.rs`, `app/src/frame/loop/browser/submit.rs`, `app/src/frame/loop/tests.rs`|Paired allocation/non-aliasing, exact logical/reserved bytes, value-record parity with the old span, reconstruction readback, two-sided completion, stale-generation refusal, worker/app traces, and scene-fence order.|8–12 worker-hours|
|Proposed|R3-05, upload one whole-screen descriptor-map view and bind its header, active index, paired spans, source pose, and mesh as a single regional input; add a capability switch while leaving `PresentMain` submission available.|`present/src/tile.rs`, `present/src/gpu/device/scene.rs`, `present/src/gpu/device/scene/submit.rs`, `present/src/gpu/device/uniforms.rs`, `present/src/gpu/device/ledger.rs`, `present/src/gpu/device/tests.rs`, `app/src/frame/loop/browser/submit.rs`, `app/src/frame/loop/tests.rs`|GPU descriptor readback equals CPU packing, source identity/generation survives the fence, N=1 descriptor bytes are stable, and switch-off reproduces the entire whole-grid oracle.|8–12 worker-hours|
|Proposed|R3-06, add the descriptor arm's vertex path: reconstruct the source-visible five-dimensional point for every mesh vertex, run the complete independently requested target chain, reject invalid poles, and write physical target depth so draw order cannot substitute for geometry.|`math/src/reprojection.rs`, `present/src/tile.rs`, `present/src/planner.rs`, `present/src/gpu/device/scene.rs`, `present/src/gpu/device/tests.rs`|GPU self-reprojection, material camera/height deltas, deterministic interior/edge/pole lattice, direct target projection at or below 1.0 px, finite max/p95 error, and two-record depth-order permutations.|10–14 worker-hours|
|Proposed|R3-07, retain semantic value through depth and shade the winning value once with current palette, exposure, tone, and output encoding; display-only changes must not invalidate spans or rerun escape kernels.|`present/src/gpu/device/scene.rs`, `present/src/gpu/device/shade.rs`, `present/src/gpu/device/warp.rs`, `present/src/gpu/device/readback.rs`, `present/src/gpu/device/tests.rs`|Palette and HDR immediacy, unchanged tile/source ids and dispatch counts across display edits, native readback tolerances, and no pre-shaded seam state.|4–6 worker-hours|
|Proposed|R3-08, integrate the switch into the frame loop and close parity: run current presets through old whole-grid homography and new one-view descriptor arms, compare traces, scene/warp identities, fence order, stable facts, pixels, reconstruction receipts, and refusal behavior; retain both arms at completion.|`app/src/frame/loop.rs`, `app/src/frame/loop/browser/facts.rs`, `app/src/frame/loop/browser/submit.rs`, `app/src/frame/loop/tests.rs`, `present/src/gpu/device/poll.rs`, `present/src/gpu/device/ledger.rs`, `present/src/gpu/device/tests.rs`|All four round-one oracle classes, descriptor round trip, source self-reconstruction, full-target projection, palette immediacy, current-preset parity, and a served capture from each switch arm.|6–8 worker-hours|

**Derived.** Stage 1 totals 36–52 worker-hours and round three totals 56–80 worker-hours. It deliberately stops before multiple retained views, chart tiles/indexing, protected backdrop, and demand-ordered per-tile scheduling; those are stages 2–4, so the old whole-grid/homography arm remains available and no J-04 graph compiler is introduced.

### Tiled-study backlog alignment

|Status|Study backlog|Round-three disposition|
|------|-------------|-----------------------|
|Derived|`JB-TILE-001` (`docs/julibrot/tiled-reprojection.md:364`)|Completed by R3-01/R3-02.|
|Derived|`JB-TILE-002` (`docs/julibrot/tiled-reprojection.md:365`)|Completed across R3-02 and R3-05/R3-06.|
|Derived|`JB-TILE-003` (`docs/julibrot/tiled-reprojection.md:366`)|Completed by R3-04/R3-05.|
|Derived|`JB-TILE-004` (`docs/julibrot/tiled-reprojection.md:367`)|Deferred to stage 2; round three retains exactly one descriptor view.|
|Derived|`JB-TILE-005` (`docs/julibrot/tiled-reprojection.md:368`)|Policy completed by R3-03 and depth-separation evidence added by R3-06.|
|Derived|`JB-TILE-006` (`docs/julibrot/tiled-reprojection.md:369`)|Completed by R3-07.|
|Derived|`JB-TILE-007` (`docs/julibrot/tiled-reprojection.md:370`)|Paired ownership is completed by R3-04; physical tile subdivision is deferred to stage 3.|
|Derived|`JB-TILE-008` (`docs/julibrot/tiled-reprojection.md:371`)|Conservative footprints are completed by R3-03; hierarchy, traversal, deduplication, and exhaustive index comparison are deferred to stage 3.|
|Derived|`JB-TILE-009` (`docs/julibrot/tiled-reprojection.md:372`)|Deferred to stage 3.|
|Derived|`JB-TILE-010` (`docs/julibrot/tiled-reprojection.md:373`)|Deferred to stage 4.|
|Derived|`JB-TILE-011` (`docs/julibrot/tiled-reprojection.md:374`)|Deferred to stage 4.|
|Derived|`JB-TILE-012` (`docs/julibrot/tiled-reprojection.md:375`)|R3-05–R3-08 publish the stage-1 subset; complete tile/index/scheduler/eviction facts and seven browser proofs remain for stages 3–5.|
|Derived|`JB-TILE-013` (`docs/julibrot/tiled-reprojection.md:376`)|Deferred beyond strict MAIN equality in version 1.|
|Derived|`JB-TILE-014` (`docs/julibrot/tiled-reprojection.md:377`)|Deferred to hardening after multi-view behavior exists.|

## 6. Open questions

### Tiled-study questions and recommendations

**Verified.** The unmerged 4D target reinforces four constraints used in these recommendations: same-thread delivery is the cheapest lowering of one ownership abstraction, the Julibrot owner is a miniature rather than the general graph compiler, rendered and presentation state need a structural boundary, and quality ownership cannot transfer to the arena without stable surface or primitive identity (`origin/docs/4d-arena:docs/plans/engine-chain-backlog.md:13-16`, `origin/docs/4d-arena:docs/presenter-architecture.md:22-24`, `origin/docs/4d-arena:docs/state-model.md:159-161`, `origin/docs/4d-arena:docs/4d-first-engine.md:95-100`). The branch is evidence for the destination only; none of its files or commits belongs in these rounds.

|Status|Question restated|Recommendation|
|------|-----------------|--------------|
|Verified / Proposed|Q-01, which descriptor-map ABI? (`docs/julibrot/tiled-reprojection.md:407`)|Freeze version 1 exactly as specified: RGBA32F, 32 header texels/512 bytes, `S0` as the current value record, `S1=(a_F,b_F,zeta_F,validity)`, 16-byte alignment, and no cached quantized presentation depth. Any incompatible change becomes a new version.|
|Verified / Proposed|Q-02, is 256×256 physical with a 254×254 core and one-sample apron the first geometry? (`docs/julibrot/tiled-reprojection.md:409`)|Yes for the first proof because it is one heap page and one mesh. Treat 128 and 512 as later measured source-screen alternatives, not chart hierarchy levels.|
|Verified / Proposed|Q-03, how are same-surface quality ties resolved? (`docs/julibrot/tiled-reprojection.md:411`)|Use canonical chart-cell ownership before depth, then low-to-high quality with `LessEqual` only for exactly coincident remnants. Quality never biases physical depth between distinct points.|
|Verified / Proposed|Q-04, which cache profile is the product floor? (`docs/julibrot/tiled-reprojection.md:413`)|Start with the 64-page/28-rendered-tile constrained profile; expose 120-page/56-tile as a delivered higher-memory tier until weak-device evidence supports a different floor.|
|Verified / Proposed|Q-05, how many transformed candidates may a frame submit? (`docs/julibrot/tiled-reprojection.md:415`)|Keep a constructor-validated 64-entry active list, enumerate by direct chart walk and coverage-first order, admit every intersection up to the ceiling, and publish overflow/holes rather than adding set-cover ranking.|
|Verified / Proposed|Q-06, does reprojection need an HDR color transient? (`docs/julibrot/tiled-reprojection.md:417`)|Shade directly into presentation for the Julibrot proof. If later arena accumulation requires it, allow one frame-lifetime RGBA16F canvas target with published bytes and no retained-cache identity.|
|Verified / Proposed|Q-07, may certified values survive reference regeneration? (`docs/julibrot/tiled-reprojection.md:419`)|No for version 1. Preserve strict MAIN equality until an independent-value proof also accounts for regeneration or retention of the source-depth receipt.|
|Verified / Proposed|Q-08, may a tile rendered at one height be reused at another? (`docs/julibrot/tiled-reprojection.md:421`)|Yes: retain source height in the render key, validate source depth at that height, reconstruct base height from the value, and recompute target position/depth at requested height.|
|Verified / Proposed|Q-09, does version 1 use every sample as a vertex? (`docs/julibrot/tiled-reprojection.md:423`)|Yes; the complete 254×254 core is the honest baseline. Coarser mesh classes wait for interior error witnesses and a measured payoff.|
|Verified / Proposed|Q-10, how does eviction value different render angles? (`docs/julibrot/tiled-reprojection.md:425`)|Protect visible quality and backdrop first, then unique chart coverage/view direction ahead of redundant nearby poses; publish pose buckets so diversity is measurable.|
|Verified / Proposed|Q-11, what arena claim and identity follow from the lab? (`docs/julibrot/tiled-reprojection.md:427`)|Claim pose-stamped value/depth retention, full-chain per-view reprojection, physical-depth intersections, chart-backed same-surface ownership, and honest holes. Transfer quality ownership only when the arena supplies stable surface or primitive identity; do not claim hidden-surface recovery or complete 3D-slice reprojection.|
|Verified / Proposed|Q-12, do rendered tiles persist across reloads? (`docs/julibrot/tiled-reprojection.md:429`)|Keep them session-local until pose, bignum, value, depth, ABI, corruption, quota, and privacy are versioned as one persistence contract.|
|Verified / Proposed|Q-13, what deep-zoom precision is exposed? (`docs/julibrot/tiled-reprojection.md:431`)|Keep the absolute bignum anchor in the CPU exact intern table; store exact-in-f32 `anchor_id` and revision; upload the source-to-request delta as compensated high/low `H17`; refuse placement when the independent f64 oracle cannot certify at most 1.0 px.|

### Questions raised by this survey

|Status|Decision sought|Recommendation|
|------|---------------|--------------|
|Verified / Proposed|S-01, where may the missing browser capture runner and golden live when round two otherwise edits only the named Rust files? The current driver drops raw bytes from its settled event (`web/labs/julibrot/drive.html:82-97`).|Allow a narrow test-only scope addition for one browser runner and versioned fixture under `web/labs/julibrot/`; without it, treat the pre/post capture as an external review artifact and keep the survey's “measurement unavailable” verdict. Production page behavior remains untouched.|
|Verified / Proposed|S-02, is round-two app reach-through acceptance scoped-file-clean or crate-clean? `app/src/facts.rs` also reads `ViewerOwner` directly, outside the named set (`crates/labs/julibrot/app/src/facts.rs:295-303`, `crates/labs/julibrot/app/src/facts.rs:394-400`).|Prefer crate-clean and allow that single file as a scope exception; otherwise record J-03 as still partial after round two and retain `owner()` only for facts. The existing `app/src/lib.rs` pan/zoom API can remain while drag/wheel helpers become internal (`crates/labs/julibrot/app/src/lib.rs:550-566`, `crates/labs/julibrot/app/src/lib.rs:638-652`).|
|Verified / Proposed|S-03, does round three's `present/src/gpu/device/**` include the module root `present/src/gpu/device.rs`? `Presenter` and `GpuState` store all scene pipelines, textures, ledger, fences, and new-construction initialization there (`crates/labs/julibrot/present/src/gpu/device.rs:297-358`, `crates/labs/julibrot/present/src/gpu/device.rs:360-430`).|Explicitly include `gpu/device.rs`; add any new descriptor shader as a child of `gpu/device/` and avoid changing the retained `shader.rs`, `uniform.rs`, or public contract until parity demonstrates a necessary interface delta.|
|Verified / Proposed|S-04, where is the canonical engine-neutral tile vocabulary owned while J-06 is present-facing and J-08 is kernel-produced? Both slices currently define residency/quality independently (`crates/labs/julibrot/kernels/src/tile_job.rs:345-470`, `crates/labs/julibrot/present/src/tile.rs:313-443`).|Own content/render identities, paired span publication, geometry, residency, and cost in kernels; keep composition-only ownership masks and target-depth policy in present; re-export compatibility names from `present::tile` during migration.|
|Verified / Proposed|S-05, which facts may normalization ignore? Worker and present include observed wall measurements alongside deterministic ownership and identity fields (`crates/labs/julibrot/worker/src/credit.rs:241-285`, `crates/labs/julibrot/present/src/contract.rs:555-645`).|Ignore only explicitly identified wall durations and environment strings. Compare event order, epochs, generations, ids, extents, modes, counts, queue depths, bytes, fence outcomes, and all policy results exactly; preserve existing numerical tolerances only for independent pixel/projection oracles.|
|Verified / Proposed|S-06, does either refactoring round create the general J-04 compiler? The current refresh is imperative and the target branch calls the Julibrot owner only a miniature (`crates/labs/julibrot/app/src/frame/loop.rs:1120-1365`, `origin/docs/4d-arena:docs/plans/engine-chain-backlog.md:15`).|No. Round two emits a replayable Julibrot work-set record; round three consumes it through tile ports. General resource/frame DAGs wait for a separate engine round with synthetic multi-producer and multi-class adapters.|
|Verified / Proposed|S-07, how is the stated 78-line backlog count reconciled with 81 current bullets? (`docs/plans/backlog.md:212-300`)|Treat the 81-row source inventory as authoritative for this survey. A later backlog-only change can remove the obsolete lockfile note and annotate items cleared by implemented rounds; round one leaves the backlog untouched.|
