# Julibrot presentation audit

Status: evidence collection in progress against `eb5ce23e`; measurements are run only on sokol and browser-only conclusions remain explicitly undecided.

## 1. Per-control trace

The completed audit will trace every page control from input state through rapid-sequence planning, interim presentation, per-refresh CPU work, and scene-resubmission behavior.

## 2. Clear colour and partial-frame paths

The completed audit will enumerate every clear-colour and partial-frame path, its fast-input reachability, visible result, and the tile design's replacement behavior.

## 3. Open-line disposition

The completed audit will confirm, refute, or mark browser-dependent each scoped backlog, PF, and PL line with repository evidence.

## 4. Reprojection speed

The completed audit will account for warp mesh work, uniform traffic, texture reads, fences, poll limits, deadlines, fallbacks, and measured native costs.

## 5. Tile readiness

The completed audit will compare present and app mechanisms with the stage-0 foundations required by the tiled-reprojection design without implementing tiles.

## 6. Ranked findings and recommendations

The completed audit will rank user-visible findings and provide lane-ready recommendations with fix shape, risk, tile relationship, and estimate.
