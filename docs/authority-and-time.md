# Authority and Time

Authority is a calculation, not a resemblance. One server owns the game history; clients estimate that history so input can feel immediate, then accept correction when the estimate and the calculation differ. The useful contract is therefore bounded convergence to server state, not equality between executions.

Authority is also independent of processing frequency. Inputs and outcomes belong to timestamps on one history. A server may wake, batch, integrate, substep, and broadcast on schedules chosen for load and numerical stability, but none of those schedules gives an event its meaning. Presentation is the sole exception because a display must be fed at its refresh cadence.

## 1. Authority Is the Server's Calculation

### 1.1 One writer of truth

Each game has one authoritative server for a running game. `pong-server` keeps connections, lobbies, and running simulations on one hub thread, calls the shared `pong_core::shooter::Sim`, and emits the resulting state (`crates/pong-server/src/lib.rs:368-450`). `fire-server` likewise gives one hub thread every lobby, runs `fire_core::sim::Race`, and states the essential division directly: clients send intents, never positions (`crates/fire-server/src/lib.rs:1-11`, `crates/fire-server/src/lib.rs:348-417`). The single writer is valuable because it leaves exactly one place where an ambiguous fact must become unambiguous; a cluster may later implement that logical role, but it must still expose one ordered authority per game.

The shared-core crates do not create shared authority. They create shared rules and a good predictor. Both the Pong client and server depend on `pong-core`, and both the Fire client and server depend on `fire-core` (`crates/pong/Cargo.toml:14-17`, `crates/pong-server/Cargo.toml:6-8`, `crates/fire/Cargo.toml:14-17`, `crates/fire-server/Cargo.toml:6-8`). `pong-core` deliberately has no engine or platform dependency (`crates/pong-core/src/lib.rs:6-12`), while `fire-server` and the Fire client both instantiate the same race model (`crates/fire-server/src/lib.rs:25-31`, `crates/fire/src/online.rs:25-29`). Reusing code lowers prediction error and makes rules easier to review; it does not turn a client execution into a vote.

The client's simulation is a predictor. Its output is provisional by construction, including when it runs the same source with the same inputs. Fire's online client already states the right reason: predicted car arithmetic may drift in its last bits, the car is corrected against server state, and the local position never decides an outcome (`crates/fire/src/online.rs:1-17`). On every authoritative state it replaces the local car fields, discards acknowledged inputs, and reapplies the pending suffix (`crates/fire/src/online.rs:224-260`). The arena client performs the same kind of rebase and replay for movement (`crates/pong/src/online.rs:918-990`). These are examples of eventual consistency in game networking: the server is authoritative throughout; the client is not a competing replica awaiting consensus.

### 1.2 A hit exists only on the server

A hit exists when, and only when, the authoritative server evaluates the interaction and commits its outcome. A client may immediately draw recoil, a tracer, or a predicted response, but those are presentation claims until authority reports what happened. The arena makes this boundary unusually clear. `pong-server` supplies accepted inputs to `Sim::step` and broadcasts the resulting bullets and player state (`crates/pong-server/src/lib.rs:409-507`); the shared simulation creates bullets, sweeps them against rewound bodies, resolves shields and head zones, and records damage inside that step (`crates/pong-core/src/shooter.rs:711-714`, `crates/pong-core/src/shooter.rs:784-831`, `crates/pong-core/src/shooter.rs:945-1106`). The client receives bullet heights from the server instead of reconstructing their paths, then derives its hitmarker from authoritative bullet disappearance and health loss (`crates/pong/src/online.rs:818-847`, `crates/pong/src/online.rs:909-919`).

The hit volume is an authority contract, not a renderer convenience. Its standing and crouched heights are tied to the silhouette the client actually draws, and the constants live in `pong-core` because both sides must agree where the body is (`crates/pong-core/src/shooter.rs:65-104`). The finite cylinder and head boundary are defined beside the authoritative hit test (`crates/pong-core/src/shooter.rs:112-130`), and the sweep uses those stance-dependent bounds for the final body and head decision (`crates/pong-core/src/shooter.rs:962-1030`). The state-model account confirms why the change is semantic: visible head geometry previously lay outside the authoritative volume, so pitch, tracer, and hit could disagree (`docs/state-model.md:82-84`). This model-aligned work matters precisely because authority must answer the question the picture invites: a player who aims at visible body geometry must not be judged against an unrelated hidden volume.

Server-only adjudication makes target-native floating point safe where replay equality would make it suspect. The bullet sweep uses transcendental and approximate arithmetic, but no client-calculated bullet can damage anyone; the repository records that boundary explicitly (`CLAUDE.md:15`, `crates/pong-core/src/shooter.rs:1064-1067`, `crates/pong-core/src/shooter.rs:1118-1123`). SIMD, fused multiply-add, target math libraries, and fast approximate math are therefore admissible on authority and prediction paths when they preserve the exact decisions and quantified budgets below. A faster calculation does not weaken authority; an ambiguous decision predicate does.

### 1.3 The determinism that survives

Consistency-determinism survives. For a fixed accepted event set, the authoritative server must have a well-defined order, exact decision rules at ownership boundaries, stable identities, seeded world selection, and enough recorded state to reproduce its own adjudication. Seeded generation still selects the world; a rewind still restores the same accepted events; an equal-time tie still has one result; an input sanitizer still has exact acceptance conditions. These properties make authority fair, auditable, and locally reproducible without constraining another machine's continuous trajectory.

Equality-determinism is dropped. The architecture does not require identical floating-point intermediates or state bits across native and wasm, scalar and SIMD, builds, targets, optimization levels, or contraction policies. It does not ban FMA, pin a software transcendental, preserve one expression tree for its rounding, or compare cross-target full-state hashes. Input-only bit-exact replay may be built as a separately costed product profile, but it is not a hidden prerequisite of ordinary authority.

The distinction prevents two common category errors. Seeded randomness is not a demand for equal floating trajectories: it is a demand that the same stable identity selects the same discrete choice. A fixed update order is not a demand for one universal instruction stream: it is a demand that the server's adjudication cannot change with hash-map traversal, packet arrival, or worker scheduling. The former governs what fact is selected; the latter governs how continuous estimates approach it.

### 1.4 What the player is promised

“Eventual” has a deadline. On receipt of an authoritative position correction, the predictor first restores the timestamped server state and reapplies only still-pending inputs. The displayed error then obeys a monotone envelope: it may not grow because of the correction, it is at most half the initial error after 50 ms, at most the greater of 10 percent of the initial error or 0.02 world metres after 120 ms, and at most 0.02 metres after 200 ms. A wrong discrete state, a path that would cross authoritative collision geometry, or an initial position error above 0.50 metres snaps on the next presentation opportunity instead of being disguised by smoothing.

Those numbers are engineering defaults, not measurements. They make the promise testable while leaving a title free to publish a stricter or looser supported envelope before testing. The default balances a short visual blend against the requirement that prediction cannot remain visibly wrong for several interactions; the 0.50 metre snap threshold is below the arena's standing hit diameter, so smoothing cannot preserve a body-sized lie. Orientation, velocity, and title-specific phase values require corresponding declared envelopes before they can gate a release.

Hit reporting has two clocks. The default server-work budget is p95 at or below 4 ms and p99 at or below 8 ms from the close of the eligible reorder cohort through adjudication, revision, and outbound enqueue. The default first-report budget at the client is p95 no worse than the measured round-trip p95 plus 25 ms and p99 no worse than measured round-trip p99 plus 50 ms, measured end to end from the timestamped trigger sample. These are defaults rather than benchmark claims; a release must measure them in each supported network envelope. A first report may be revised while its timestamp remains inside the rewind horizon, but every revision is an explicit correction and finality is bounded by that horizon rather than left implicit.

## 2. Authority Is Frequency-Free

### 2.1 Timestamped history

The server's monotonic session timeline is the time authority. Wire inputs carry a client input sequence, an event identity, and the client's estimate of the sample's server timestamp; authoritative state carries the server timestamp at which it is true, an increasing authoritative event sequence, a history revision, and the latest accepted input sequence for each relevant actor. Timestamps are integer durations in a declared epoch and scale. Their precision describes representation, never a processing period.

Clients estimate an affine mapping from their local monotonic clock to the server timeline through timestamp exchanges and retain offset, skew, round-trip time, and uncertainty. The server validates a claimed time against that exchange history, receipt time, sequence progression, and the rewind horizon. A future claim cannot gain future authority, a causally regressing claim cannot gain extra rewind, and an implausibly old claim receives no historical credit. Client time is evidence constrained by server observations, not authority smuggled onto the wire.

Every accepted event receives an effective server timestamp. Movement intent changes take effect there; trigger edges, boosts, jumps, melee, joins, and other semantic transitions occur there; cooldowns and lifetimes are durations from timestamps rather than counts of updates. An implementation may integrate between event times using any stable method that meets its gates. It may also choose a uniform internal step, but the step number cannot enter a message, an event identity, a gameplay rule, or an acceptance condition.

### 2.2 Bounded reordering and simultaneity

Arrival order is network noise, so it cannot be a gameplay tiebreaker. The server holds accepted events for a bounded reorder window, closes a cohort when its watermark passes, and adjudicates that closed event set in timestamp order. The default reorder window is 12 ms. This is an engineering default, not a measured jitter percentile: it is short enough to fit inside the 25 ms first-report allowance while giving ordinary packet reordering a chance to stop influencing outcomes. A title changes it only together with its latency and fairness gates.

Events with the same representable timestamp are simultaneous wherever the rule can be commutative. Each independent attack reads the same pre-cohort authoritative state, all valid outcomes are collected, and their effects are combined as sets or associative reductions; one attack does not erase another merely because its packet was dequeued first. If a resource can have only one owner or another commit is inherently noncommutative, the stable tie key is `(event_class, actor_identity, input_sequence)`.

Each tie component earns its position. `event_class` comes first because semantic dependency is a rule of the game rather than a privilege a sender can race for: a class table can state, for example, whether a membership transition makes an action invalid before interaction outcomes commit. It is versioned and server-owned. `actor_identity` comes next because a stable session identity removes traversal and arrival bias among different actors; any fairness policy that rotates priority must derive that rotation from an earlier authoritative event, not receive order. `input_sequence` comes last because it orders repeated events from one actor and makes their identities unique without allowing a later packet to precede an earlier one at an equal timestamp. The complete order is effective timestamp followed by that key; batch size, thread, connection, and packet arrival are absent.

The commutative rule and tie key serve different cases. Commutative adjudication preserves genuine simultaneity, such as two otherwise valid lethal shots. The key resolves only the irreducible remainder, such as two simultaneous acquisitions of one exclusive object. Sequentially applying the key to every interaction would turn a deterministic implementation detail into a gameplay advantage, while pretending every rule commutes would leave exclusive ownership undefined.

### 2.3 Event-keyed randomness

World generation remains keyed by immutable world identity and a session seed. Runtime authoritative randomness is keyed by event identity rather than a mutable stream: `(session_seed, subsystem_domain, event_class, actor_or_source_identity, input_sequence, draw_purpose, draw_index)`. Every component is accepted event data or a versioned domain constant. A retry or rewind of the same event therefore selects the same choice even when batching, substeps, worker assignment, or arrival order changes.

Tick number, packet order, worker index, presentation sequence, and internal substep are forbidden random inputs. A subsystem that needs several values gives each a stable purpose and index; it does not advance a global generator whose later values depend on which branch happened to draw first. This preserves the reason for the repository's seeded-RNG rule while removing its tick-indexed prescription (`CLAUDE.md:14`). The existing arena generators demonstrate why shared seed identity matters (`crates/pong-core/src/shooter.rs:288-315`, `crates/pong-core/src/shooter.rs:370-405`); §5 narrows how their shared world decisions must eventually be represented.

### 2.4 Rewind and revision

Late but admissible evidence may revise recent authority. The server keeps timestamped checkpoints and accepted events for a bounded rewind window, restores the latest checkpoint no later than the event, advances to the exact event time, adjudicates it, and replays later accepted events in the stable order. The default rewind depth is 300 ms, expressed as a duration. This default is grounded in the arena's existing product scale of eighteen 60 Hz history steps (`crates/pong-core/src/shooter.rs:194-197`) without retaining those steps as the contract.

When re-adjudication changes a previously emitted state or outcome, the server increments the history revision and sends a correction naming the affected authoritative sequence interval. Old messages remain forensic records of an earlier revision but cease to describe current authority. Evidence older than the rewind horizon is final and cannot silently rewrite history; invalid, future, or over-age timing receives no rewind credit and is handled by the title's explicit current-time or rejection rule.

The server may checkpoint, integrate, and replay at internal boundaries chosen for numerical stability. Fast projectiles, contacts, stiff constraints, and long intervals often justify bounded substeps or fixed solver iterations. Such choices may be pinned within a server release when needed to reproduce its own adjudication, yet remain unobservable implementation law rather than wire frequency. A change is safe when exact verdicts remain stable, continuous state stays inside declared tolerance, and the recorded authority remains interpretable.

## 3. The Clock Lives at Presentation

Exactly one pipeline stage may promise a frequency: the stage that faces the display. The adopted rendering policy puts scene output in offscreen `SceneFrame` color and depth, forbids the scene renderer from touching the swapchain, and makes a cheap warp pass present the newest finished scene every display frame (`docs/atw-first-rendering.md:3-8`, `docs/atw-first-rendering.md:73-86`). The presenter architecture makes that ownership structural: `presenter.rs` owns the surface and its entry point consumes a finished scene plus a newer view pose (`docs/presenter-architecture.md:13-22`, `docs/presenter-architecture.md:71-81`).

Presentation samples a timeline; it does not advance authority. The scene may render variably and offscreen, while the presenter reprojects and paces the newest complete image at rAF or the native display opportunity. A 60 Hz, 120 Hz, or adaptive-refresh promise belongs here because failure to meet it is a pacing failure visible at the display. A scene scheduler may have a local target or budget, but only the presenter may expose a frequency as a pipeline contract. The late input read, presenter-side UI, independent scene resolution, and ATW invariants survive unchanged (`docs/atw-first-rendering.md:90-122`, `docs/presenter-architecture.md:62-67`).

Snapshot broadcast cadence is a bandwidth and quality-of-service knob. Sending every current server step is merely today's implementation in `ember-server` (`crates/ember-server/src/lib.rs:336-357`); the arena and racer currently gate state on `STATE_EVERY_TICKS` (`crates/pong-server/src/lib.rs:448-507`, `crates/fire-server/src/lib.rs:447-461`). Under this law a broadcaster may adapt to queue pressure, loss, observer interest, or state change. Each snapshot remains true at its source timestamp and revision, so changing broadcast rate changes freshness and bandwidth, never which event the server accepted or what happened.

Client input sampling is also local scheduling. A device callback may sample more often than prediction, a client may coalesce held intent, and an edge may be sent immediately; the sample retains its timestamp and sequence. Prediction stepping is equally local. Fire may keep its current fixed-step accumulator because it reduces reconciliation error (`crates/fire/src/online.rs:134-160`, `crates/fire/src/online_game.rs:191-202`), and another client may use variable or adaptive integration. Neither schedule binds the authority, and neither appears as a promised frequency on the wire.

The boundary is absolute: gameplay duration, event order, rewind, random choice, snapshot identity, and authority never count presenter frames; presentation never authors a server event from its frame-relative late pose. The input-latch design already protects the latter half by forbidding the cosmetic view read from reaching anything sent, stored, or ticked (`docs/input-latch.md:106-112`). The result is one clocked consumer of time and a frequency-free history upstream of it.

## 4. Disposition of Existing Contracts

This table reprojects the contract-bearing matches in the main-tree census. The first pattern, `60[[:space:]]*hz|tick_hz|fixed[ -](time)?step|fixed[ -]dt|fixed[ -]rate|per[ -]tick|tick[ -](rate|loop|clock|cadence|budget|interval|index)|server tick|sim tick|fixed_dt|state_every_ticks|msgs_per_tick|max_msgs_per_tick|max_rewind_ticks|ack_age_ticks|view_tick`, matched 200 lines. The second, `determinism|deterministic|lockstep|replay|rollback|bit[ -]identical|state[ -]hash|cross[ -](target|machine|build)|fused multiply|fma|suboptimal_flops|imprecise_flops`, matched 119 lines. The catch-all `(^|[^[:alnum:]_])(tick|ticks|ticked|ticking)([^[:alnum:]_]|$)|tick_|_tick` matched 436 lines. Each count is from `git grep -I -n -i -E` at `502414c` over `CLAUDE.md`, `README.md`, `docs`, and `crates`; the sets deliberately overlap.

The table groups adjacent lines that express one clause and gives each group one disposition. `SURVIVES` means the clause already states a law this document keeps. `SUPERSEDED` means the current fact may remain in grandfathered code but cannot govern new work. `SPLIT` means a local technique or consistency purpose survives after its authority or equality claim is removed. `DROPPED` means the named field or requirement has no contractual successor.

|Source clause at `502414c`|Verdict|Reason|
|---|---|---|
|`CLAUDE.md:13`|SURVIVES|Shared-core changes remain protocol questions because they can change authority semantics and prediction quality, even though shared execution no longer proves equality.|
|`CLAUDE.md:14`|SPLIT|Seed identity and stable server order survive, while fixed 60 Hz and tick-indexed runtime randomness become timestamps and event-keyed randomness.|
|`CLAUDE.md:15-17`|SURVIVES|Server-only bullets and model-aligned shared body geometry already put adjudication and world meaning on the correct side of the boundary.|
|`CLAUDE.md:21-22`|SURVIVES|Compatibility is judged by what an old peer does, and semantic disagreement still requires an exact protocol gate.|
|`README.md:14-20`|SPLIT|Reproducible server adjudication and recorded replay survive, but fixed-timestep cross-execution reproducibility and free input-only replay do not.|
|`README.md:31-37`, `README.md:48-53`|SUPERSEDED|The listed 60 Hz server and snapshot behavior describes the current implementation, not the authority or network contract.|
|`README.md:72-80`, `README.md:87-108`|SPLIT|Current tick diagnostics and roadmap items remain useful implementation observations, while display-facing frame budgets are the only frequency promises.|
|`README.md:150-158`|SPLIT|The match server remains authoritative and shared Pong code remains useful, while “deterministic 60 Hz” and 30 Hz state cease to define the game.|
|`docs/ARCHITECTURE.md:11-31`|SUPERSEDED|Its inventory of deterministic cores, 60 Hz loops, `TICK_HZ`, and tick-gated state is current topology, not future law.|
|`docs/ARCHITECTURE.md:57-64`|SPLIT|Replayability, seeding, and bounded rewind survive, but exact client/server physics equality and an eighteen-tick horizon become recorded authority and a duration.|
|`crates/ember-net/src/lib.rs:1-2`|DROPPED|Protocol-wide preservation of one floating expression order has no successor; exact wire fields and server-owned acceptance replace it.|
|`crates/ember-net/src/lib.rs:17-24`|SUPERSEDED|`TICK_HZ` is neither an authority nor a shared protocol constant under the new wire.|
|`crates/ember-net/src/lib.rs:71-79`|DROPPED|`Welcome.tick_hz` exposes server scheduling and is removed rather than defaulted in the protocol bump.|
|`crates/ember-net/src/lib.rs:89-92`|SUPERSEDED|`Snapshot.tick` becomes source timestamp, authoritative event sequence, and history revision.|
|`crates/ember-server/src/lib.rs:1-11`|SPLIT|The single simulation writer and channel ordering survive; preserving one floating expression order and the fixed 60 Hz description do not.|
|`crates/ember-server/src/lib.rs:281-392`|SUPERSEDED|The tick deadline, per-tick integration, unconditional broadcast, and tick-overrun budget become internal scheduling plus elapsed-time state and adaptive broadcast.|
|`crates/ember-server/src/lib.rs:92-104`, `crates/ember-server/src/lib.rs:135-137`, `crates/ember-server/src/lib.rs:430-433`|SPLIT|The token-bucket defense and its eventual-consistency rationale survive, but refill and reporting become per-second rather than per-tick.|
|`crates/ember-server/src/lib.rs:617-635`|SURVIVES|Exact version mismatch refusal is the required behavior for old `ember-net` peers after the non-additive wire change.|
|`crates/game/src/net.rs:18-30`, `crates/game/src/world.rs:29-45`|SUPERSEDED|Client liveness and snapshot identity use elapsed receipt time, source timestamp, event sequence, and revision rather than a promised rate or last tick.|
|`crates/game/src/world.rs:79-138`|SPLIT|Rejecting stale state and interpolating snapshots survive, while tick order and `TICK_HZ`-derived interpolation become revision-aware timestamp sampling.|
|`crates/game/src/main.rs:1-4`, `crates/pong/src/lib.rs:1-4`, `crates/fire/src/lib.rs:1-2`|SPLIT|Local arithmetic-preservation lints may remain for grandfathered predictors, but they no longer express cross-target correctness.|
|`crates/pong-core/src/lib.rs:1-8`|SPLIT|A shared headless core survives, but lints and comments that preserve one floating expression tree for equal trajectories no longer state a release requirement.|
|`crates/pong-core/src/sim.rs:1-5`, `crates/pong-core/src/sim.rs:70-108`|SPLIT|The simple Pong integrator may retain a fixed local step, while authority and client contracts use elapsed timestamp intervals.|
|`crates/pong-core/src/sim.rs:278-287`|SPLIT|Same-build same-input reproducibility remains a server-local regression gate, not evidence of cross-target authority equality.|
|`crates/pong-core/src/shooter.rs:1-5`, `crates/pong-core/src/shooter.rs:610-714`|SUPERSEDED|`FIXED_DT`, `Sim.tick`, and tick-indexed rewind history become internal integration, timestamped checkpoints, and revisions.|
|`crates/pong-core/src/shooter.rs:288-405`|SPLIT|Seeded world and pad identity survive, while their floating generator cannot be the final cross-peer existence predicate once touched.|
|`crates/pong-core/src/proto.rs:99-102`, `crates/pong-core/src/proto.rs:213-257`, `crates/pong-core/src/proto.rs:293-301`|SUPERSEDED|Tick-gated broadcast, `view_tick`, tick-consumed edge wording, and `State.tick` migrate together to event timestamps and sequences.|
|`crates/pong-core/src/proto.rs:10-95`|SURVIVES|Its old-peer analysis and protocol bumps correctly treat changed gameplay meaning as incompatible even when fields decode.|
|`crates/pong-server/src/lib.rs:368-450`|SUPERSEDED|The hub's tick clock, post-step rewind arithmetic, and broadcast modulus become a timestamped event adjudicator and independent broadcaster.|
|`crates/pong-server/src/lib.rs:74-76`, `crates/pong-server/src/lib.rs:112-116`, `crates/pong-server/src/lib.rs:514-519`, `crates/pong-server/src/lib.rs:627-628`|SPLIT|Flood control and RTT-bounded rewind survive, re-expressed as elapsed-rate tokens and timestamp uncertainty rather than counters per tick.|
|`crates/pong-server/src/lib.rs:902-935`|SPLIT|Server validation of claimed historical view and preservation of input edges survive, while the clamp target becomes credible timestamp and event identity.|
|`crates/pong/src/online.rs:918-983`|SPLIT|Authoritative rebase and pending-input replay remain the predictor, but replay duration and local subdivision are approximation choices checked by convergence.|
|`crates/fire-core/src/lib.rs:1-11`|SPLIT|Fire remains a separate shared core and protocol domain, while preserving established floating rounding is no longer an equality gate.|
|`crates/fire-core/src/car.rs:10-22`, `crates/fire-core/src/car.rs:184-205`|SPLIT|Stable semantic update order survives for adjudication; agreement tick for tick and fixed `TICK_HZ` do not.|
|`crates/fire-core/src/car.rs:537-577`, `crates/fire-core/src/sim.rs:469-497`|SPLIT|Same-build reproducibility remains a regression check, while bit-identical client/server state and whole-race equality cease to be release or authority gates.|
|`crates/fire-core/src/track.rs:1-15`|SPLIT|Stable course identity and query semantics survive, while equal sampled `f32` geometry is grandfathered rather than a cross-peer existence contract.|
|`crates/fire-core/src/sim.rs:30-75`|SPLIT|`FixedStep` remains a valid local predictor and server integrator helper, but it cannot define authority or wire time.|
|`crates/fire-core/src/sim.rs:78-108`, `crates/fire-core/src/sim.rs:165-248`|SUPERSEDED|Countdown ticks, finish ticks, `Race.tick`, and tick-based finish ordering become timestamped transitions with the same stable actor tie.|
|`crates/fire-core/src/proto.rs:1-30`, `crates/fire-core/src/proto.rs:126-141`, `crates/fire-core/src/proto.rs:178-186`|SPLIT|Exact old-peer gating and input sequence survive, while tick-consumed boost, tick broadcast cadence, and state tick become timestamped events and state provenance.|
|`crates/fire-server/src/lib.rs:348-461`|SUPERSEDED|Accumulator-driven ticks, one-tick edge consumption, and tick-modulus broadcasts become frequency-free adjudication plus QoS scheduling.|
|`crates/fire-server/src/lib.rs:45-46`, `crates/fire-server/src/lib.rs:379-384`, `crates/fire-server/src/lib.rs:589-599`|SPLIT|Flood protection survives as an elapsed-time token bucket, not a counter reset by simulation progress.|
|`crates/fire/src/online.rs:1-21`, `crates/fire/src/online.rs:134-160`, `crates/fire/src/online.rs:224-260`|SURVIVES|Fire already treats fixed prediction and replay as a local closeness technique corrected by server state rather than an adjudicator.|
|`docs/atw-first-rendering.md:35-69`, `docs/atw-first-rendering.md:90-101`|SPLIT|The scene and presenter clocks survive, the presenter owns display cadence, and the fixed deterministic sim row and fixed-tick input wording are superseded.|
|`docs/presenter-architecture.md:26-81`, `docs/presenter-architecture.md:172-174`|SURVIVES|Scene timestamps, ring-global sequence, surface ownership, and a scene clock above rendering already keep presentation provenance separate from authority.|
|`docs/state-model.md:19-40`|SPLIT|The five levels and server ownership survive, while network-state tick identity becomes timestamp, event sequence, revision, and acknowledgement.|
|`docs/state-model.md:60-64`, `docs/state-model.md:131-151`|SURVIVES|Override, rebase, and the documented mismatch between server integration and approximate client replay are direct evidence for a convergence contract.|
|`docs/state-model.md:92-102`, `docs/state-model.md:122-125`|SPLIT|Shared seeded world and bandwidth-aware state encoding survive; fixed broadcast cadence and floating generation as existence authority do not.|
|`docs/state-model.md:106-116`|SPLIT|Held-state intent, sequence acknowledgement, and semantic protocol versioning survive, while application on each tick becomes application at an effective timestamp.|
|`docs/input-latch.md:18-34`, `docs/input-latch.md:106-112`|SPLIT|Two independent input consumers and the cosmetic late-read firewall survive; its 60 Hz sim premise becomes an arbitrary local prediction schedule.|
|`docs/latency-observability.md:93-116`, `docs/latency-observability.md:124-177`|SPLIT|Causal sequences, same-process timestamps, and honest stage metrics survive, while tick join keys and tick-described server stages become timestamp and revision fields.|
|`docs/dynamic-scene-timing-and-scaling.md:3-29`|SURVIVES|Display classes, unconditional presentation, and adaptive scene cadence are presentation policy and cannot actuate authority.|
|`docs/dynamic-scene-timing-and-scaling.md:120-130`|SPLIT|The controller remains forbidden to alter gameplay or networking, but the protected object is timestamp semantics rather than a fixed sim or broadcast cadence.|
|`docs/dynamic-scene-timing-and-scaling.md:142-156`|SURVIVES|Byte-identical replay of the pure presentation controller is a local policy test, not cross-machine simulation equality.|
|`docs/weak-device-performance.md:139-157`, `docs/weak-device-performance.md:213`|SURVIVES|Its observed prediction divergence with render rate motivates convergence gates and does not establish a fixed authority requirement.|

The remaining census hits are excluded for explicit reasons rather than carried forward silently. Tests and examples contain fixture tick values, loop counters, and old cadence assertions; they are migration evidence governed by the production row for the same crate, not independent contracts. Identifiers and plumbing such as `finish_tick`, `last_tick`, `predict_tick`, trace-span names, and test helper names describe the grandfathered representation and are covered by their owning module row. Current-state measurements and arithmetic examples in `docs/weak-device-performance.md` remain true descriptions of `502414c`, but only its prediction conclusion has architectural force. Files under `docs/plans/` and the backlog are dated work records or superseded plans rather than adopted law; relevant open work is restated in §7 and appended as new backlog lines. Renderer animation callbacks named `tick`, editor determinant prose, track-ribbon “lockstep,” worker execution-order prose, and linter text about backtick delimiters are lexical false positives unrelated to simulation authority.

## 5. World Agreement Uses Exact Decisions

Eventual consistency can correct where a client draws a moving body; it cannot repair a client and server that disagree about whether an obstacle, road, pad, or body part exists. Shared world identity is therefore stricter than continuous-state closeness. A seed and a versioned world recipe must select the same obstacle records and pad identities; Fire's circuit description and sector identities must denote the same course; stance and model geometry must name the same body. The arena already depends on seed-on-join rather than transmitting obstacles (`docs/state-model.md:92-94`), Fire currently shares a densely sampled floating course (`crates/fire-core/src/track.rs:1-15`, `crates/fire-core/src/track.rs:57-100`), and the hit volume already lives beside the server decision rather than in renderer-private tuning (`crates/pong-core/src/shooter.rs:65-130`).

Contract-bearing world data uses exact integers, rational fixed point, or a versioned algebraic representation. A generated item has a stable identity independent of enumeration order. Boundary conventions are one-sided and explicit: lower-inclusive and upper-exclusive intervals, stable actor or feature ties, and canonical ownership of equality. Floating-point closeness may place render geometry and predict motion, but “within epsilon of an obstacle” can never decide whether that obstacle exists or which side owns its boundary.

The acceptance algorithm is filter, then widen. A cheap scalar, SIMD, GPU, or approximate-math filter may reject a candidate when a conservative error bound proves it outside, and it may accept when the same bound proves the exact sign. If the bound overlaps zero, the implementation widens to the exact integer or algebraic predicate. Overflow, non-finite input, or an unproved sign cannot choose a side; it invokes the wider path or rejects malformed world data. Approximation is free to be sloppy only while it cannot change acceptance.

Dynamic interaction has two legitimate final boundaries. A cross-peer existence or ownership fact uses the exact predicate above. A continuous collision or hit that need not be independently recreated by clients may instead be server-owned: the server's finite, sanitized acceptance predicate commits the fact and clients consume the verdict. The arena bullet sweep follows this second model. Its float geometry need not match a wasm predictor because no client adjudicates it, but its ordering, finite-input rules, target identity, stance selection, and inclusive/exclusive comparisons must be stable on the server and recorded with the event.

Current shared generation is grandfathered, not exemplary. `generate_arena` and `generate_pads` derive cross-peer layout through `f32` trigonometry (`crates/pong-core/src/shooter.rs:288-315`, `crates/pong-core/src/shooter.rs:370-405`), while Fire track location uses continuous geometry in shared code. Untouched sessions remain compatible with their existing protocol versions. The next change to those recipes must either cook exact world records, transmit a versioned exact description, or introduce filtered exact predicates and prove identity round-trips; it may not strengthen the existing floating coincidence into new law.

## 6. Gates Enforce Convergence, Not Equality

### 6.1 Convergence-budget gates

A predictor fixture records timestamped authoritative states, accepted input sequences, network delivery conditions, raw correction error, post-replay error, displayed error, and discrete state. For every correction it asserts the monotone default envelope from §1.4 at 50, 120, and 200 ms, retains censored failures, and fails immediately if a wrong ownership, alive/dead, collision-side, lap, or world identity survives correction. Separate cells report p50, p95, p99, maximum, sample count, snaps, and unsupported conditions; pooling an unhealthy network cell into a healthy average is forbidden.

The fixture perturbs client integrator step, SIMD width, FMA availability, optimization, render cadence, and snapshot cadence without expecting equal trajectories. It passes only when every claimed cell stays inside the declared continuous budgets and exact identities converge to zero difference. A client that oscillates across successive corrections, hides a snap as animation, or misses the deadline fails even if its final state eventually wanders close.

### 6.2 Adjudication-determinism gates

Construct one accepted event set containing unequal timestamps, equal timestamps, competing exclusive acquisition, simultaneous independent lethal interactions, invalid future and regressing claims, and at least one random event. Feed every relevant arrival permutation, batch partition, queue-drain size, and permitted internal substep schedule to the adjudicator. The authoritative event order, discrete verdicts, random choices, revision graph, and final exact identities must match; continuous state compares only within the server release's declared tolerance.

A rewind fixture inserts a late-but-in-window event, restores the prior checkpoint, and requires the same event-keyed random values and stable ties as an on-time run. It then inserts the same evidence one unit beyond the 300 ms default and requires zero rewind credit. Server work and end-to-end hit reports are measured against §1.4, with reorder hold, adjudication, queueing, and return path reported separately; no client timestamp is subtracted from a server timestamp.

### 6.3 Protocol gates

The new schema is tested in both directions against captured old messages and explicit peer versions. For `ember-net`, removal of `tick_hz` and reinterpretation of `tick` are non-additive semantic changes, so the next version is 3 if no intervening bump occurs; old peers receive the existing mismatch rejection and the connection closes (`crates/ember-net/src/lib.rs:17-21`, `crates/ember-server/src/lib.rs:617-635`). No default may make an old snapshot index masquerade as a timestamp.

Pong and Fire batch their corresponding timestamp, event-sequence, revision, acknowledged-input, and duration fields into each game's next deliberate `PROTO_VERSION` bump. Their old clients may still issue the ungated lobby-list request, but create and join remain exact-version operations, so frozen builds become list-only and receive the existing human-readable refusal (`CLAUDE.md:21-22`, `crates/pong-server/src/lib.rs:674-703`, `crates/fire-server/src/lib.rs:612-663`, `crates/fire-server/src/lib.rs:779-800`). Tests must ask what an absent field makes the old peer do, not merely whether Serde decodes it.

### 6.4 Presentation gates

Presentation keeps the ATW invariants: the scene renderer cannot reach the surface, the presenter owns swapchain configuration and every display opportunity, scene and surface sizes are independent, UI composites after warp, a re-presented frame keeps its own source timestamp and input mark, and identity warp reduces to the blit (`docs/presenter-architecture.md:71-83`, `docs/presenter-architecture.md:108-122`). A display-rate test varies scene rate and authority traffic independently and requires unchanged event outcomes, no presenter-frame value on the wire, and continued presentation at the available display opportunities.

### 6.5 World-agreement gates

For every certified world recipe, serialize and deserialize the exact operands, enumerate boundary, corner, overflow, and near-zero cases, and compare accepted stable identity sets across scalar, SIMD, native, and wasm implementations. Run the approximate filter beside the exact evaluator on adversarial inputs: every conclusive filter sign must equal the exact sign, every inconclusive case must widen, and no overflow may fall through to approximate acceptance. Model fixtures round-trip stance, body, head, track, obstacle, and pad identities; server-owned hit fixtures separately prove that visual geometry constants and authoritative acceptance use the same versioned body description.

No gate computes a cross-build full-state hash. Forensics records authoritative timestamps, event identities, accepted order, revisions, outcomes, server build, world recipe, and periodic checkpoints. A re-run demands exact event and discrete verdict agreement while comparing continuous fields to named absolute and relative tolerances. That evidence tests every promise in §§1–5 without reintroducing equality-determinism under another name.

## 7. Migration Posture

This law governs new work immediately. Existing tick-shaped code is compliant by grandfathering until it is touched; grandfathering permits operation and compatible fixes, not new dependencies on tick identity. A touched gameplay duration becomes seconds, a touched random choice becomes event-keyed, a touched authority message becomes timestamped, and a touched rate limiter becomes elapsed-time based. A local fixed-step helper may remain or be introduced when it is only an implementation choice.

Wire-visible dissolutions move together rather than through misleading defaults. `ember-net` removes `TICK_HZ` from shared protocol meaning, removes `Welcome.tick_hz`, and replaces snapshot indices in one deliberate version bump. Pong and Fire replace `view_tick`, `ack_age_ticks`, `State.tick`, tick-gated edge semantics, and tick-valued finish or rewind provenance in coordinated per-game bumps. Until those batches are ready, current fields keep their current meanings; no half-migrated peer interprets a tick as a duration.

Server loops may migrate independently behind the old wire when behavior remains compatible. Per-tick rate limits become token buckets refilled from elapsed monotonic time and expressed in messages per second with burst capacity. Snapshot selection becomes an independent bandwidth/QoS scheduler. Internal fixed steps may remain while timestamps are the source semantics, allowing the protocol migration to remove observable frequency without requiring a physics rewrite in the same change.

The migration is complete when authority can change its batching, substeps, and broadcast cadence without a protocol or gameplay change; when client prediction can change math path without claiming equality; when old peers fail or remain list-only exactly as their protocol family specifies; and when the gates in §6 pass on the promises above. Until then the disposition table is the map: preserved mechanisms keep their value, superseded clauses receive no new dependants, and dropped fields wait for the deliberate wire break rather than accumulating compatibility fiction.
