# Killshot v31 — Breach-12

User request: introduce a shotgun in v31. Work stays on `codex/killshot-v31`, incorporating main's host repair `70ffba2a` and the v30 release handoff. Preserve the unrelated dirty original checkout, all frozen releases, and peer game changes. No all-game updater or new Cloudflare tunnel is part of this task.

## Ready-to-publish checkpoint — not yet live

Final runtime source is `a33c5ce445af68acb83d2db58530d0317d0d514b`, r1509, protocol 24. The clean server/WASM/bindings build passed in 3.208s. WASM SHA256: `5ea5d8a11e7cad5092615208875848307ca7afffcf07309d2ef20fe43eee4ff2`; server SHA256: `0f370d272f0349d8ca42d718f2cf10ab9d43300e10237755a4e0a2b28ab7c501`. The final clean WASM exactly matches the visually reviewed reload/ADS candidate. Documentation after this source commit is a handoff, not a different tested runtime.

Exact final-source branch CI [34048622973](https://github.com/EndersGamesDev/EmberEngine/actions/runs/34048622973) passed Linux cores/servers and deployment shim suites. This workflow does not build the client; the separate native/WASM/browser gates below cover it. No main promotion or production deployment is implied by green branch CI.

Verified: 434 native tests across the unchanged final core/server and final client suites (169 client, 243 core, five server unit, 17 WebSocket; one existing ignored core fixture). Strict Clippy and format checks passed. Final pure settings/HUD 134 plus publisher/projectile-validator 12 tests and asset reproduction passed together in 0.304s. The exact r1509 final server and WASM passed the full actual browser's 115 checks in 43.217s (43.486s lifecycle); report `target/killshot-v31-browser-final/results.json`. The exact r1509 eight-peer FFA/TDM/Hill plus shotgun gate passed in 26.977s (28.775s lifecycle), 6,127 states, no errors and eight identical pellet endpoints on all peers; report `target/killshot-v31-network/network-results.json`. Five dedicated ADS/reload captures were inspected after the pose correction, in addition to the six earlier first-/third-person captures; details below.

Not verified or performed: v31 public deployment, production-load/human playtesting, trusted physical fullscreen/pointer-lock/controller input, or any fleet update. New v31 Windows-Arena-only publication/restart authorization remains unanswered. Therefore main and live v30 have not been replaced. A fresh read-only check confirmed v30 Arena PID 43400, UTC start ticks 639243104889265139, sole port-7780 listener, unchanged binary SHA256 `fb4a0faf5d0afbbe1a177da8463c2c13410cf6205fcb818279b29d8e84c5a93f`, and its running `ember-arena-server-v30` task. Original cloudflared PID 4072 and UTC start ticks 639242972163706587 are unchanged. That check took 1.410s. No live service was stopped during v31 work.

Next concrete step: obtain the v31 Windows-Arena-only restart decision, then fresh-fetch main/Pages, coordinate with League and Barza, preserve peer changes, promote a tested clean source only after CI, perform the hash-pinned Arena-only binary/task replacement without touching its tunnel, and publish via the guarded `tools/v31` server-first release/public-byte checks. Rebuild if promotion changes source provenance. Current last fetched main/ci-passed is `70ffba2a`; Pages moved independently to `2a6af28d` (host lundi publication), so never reuse an earlier Pages base. Barza messages through 112 were read. Knecht's fleet timer remains intentionally paused; do not resume it or change Fire/Kings/League as a shotgun-release side effect. Repository policy leaves PR comments to a human while WSL/gh is unavailable; the pushed branch, this checkpoint and Barza are the durable handoff.

Implemented contract: Breach-12 is weapon 8, a magazine-fed shotgun with six shells loaded, 24 in reserve, a 0.85-second firing cycle and a 2.8-second reload. One shell launches eight server-authoritative deterministic pellets. Intrinsic pellet spread stays nonzero while ADS/crouch improve aim, and isolated pellets do not inherit the other weapons' instant-headshot kill. Each pellet deals one HP; effective damage decreases through physical dispersion, not fractional damage falloff. Pellets have a hard 25-metre horizontal travel limit. Weapon 8 participates in loot, retained per-life inventory, resupply and Custom starting loadouts; slot 9 remains reserved. Protocol 24 rejects incompatible v30 clients cleanly.

Root owns runtime integration, serial Idle-priority builds, multiplayer/browser verification and release coordination. Core worker owns core/server; web worker owns new v31 page/settings/catalog/tests and scoped publisher; presentation worker owns feel/reload/sound plus a separate shotgun GLB and sidecar. Original weapon and glove assets remain unchanged. A new gun can reuse the existing AK glove frame only if its geometry actually fits those sockets and is visually checked in first and third person.

Required gates: existing and new native/core/server tests, strict all-target Clippy, pure v31 web/publisher tests, clean server/WASM builds, actual private browser custom-shotgun/HUD/reload checks, pellet authority/cover/shield/range regressions, first/third-person asset captures and public byte/version checks if deployed. Report wall times and explicitly distinguish rendering fixtures from gameplay tests. Never synthesize workstation input or take its foreground. Publication/restart must be coordinated against fresh live state; v30's prior Windows-only approval does not authorize a Knecht fleet update.

Barza message 98 was read: main/ci-passed now contain the host-only refinement; Knecht's timer remains paused, its current Arena/Fire servers remain live, and the Kings tunnel is absent. Do not resume its timer as a side effect of the shotgun work.

## Candidate verification — 2026-09-06

Source `faa5ba09c7e2c99ec115f517a9486ab40841f260`, r1506, passed branch CI [34047890545](https://github.com/EndersGamesDev/EmberEngine/actions/runs/34047890545): Linux cores/servers and deployment shim suites. The clean server/WASM/bindings build took 10.234s. Candidate WASM SHA256 is `7e1bed08ed4812b4ec5960d646c3d1943f110f1dfa53ef50ddeeb43bc2f9310d`; server SHA256 is `7f6171548e7743307b9d064eef179f22584b79c76c2961002a198cf932a653aa`. These hashes identify this candidate, not a later final runtime or a public release.

Native core/server gates passed 243 core tests, five server unit tests and 17 real-WebSocket tests (265 total, one existing ignored fixture writer) in 11.747s. Shotgun checks cover eight deterministic pellets and one shell spent, ID uniqueness and JavaScript-safe 52-bit bounds, event capacity, range clipping, close versus distant effectiveness, cover, shields/reflection identity, friendly fire, isolated head pellets, inventory, loot and Custom starts. Historical handling fingerprints remain pinned. The client suite passed all 165 tests in 11.771s after correcting new round-table expectations and fixing a real iron-sight occlusion: the front bead now ends two millimetres below the optical ray. Existing source weapon/glove assets are unchanged. Strict Clippy passed; a final formatting/Clippy/asset gate took 3.732s.

Pure JavaScript gates passed 134 settings/HUD checks plus 12 publisher/projectile-validator tests in 0.200s combined at Idle. The private eight-peer network test passed FFA/TDM/Hill, four-versus-four, rejoin/capacity, previous-protocol rejection and movement/parkour, then verified all eight observers received identical eight-pellet paths for one spent shell. Slot 8→1→8 retained ammo; real reload restored six shells with 23 reserve. It processed 6,127 states without errors in 27.074s (29.596s including its own hidden server lifecycle). Report: `target/killshot-v31-network/network-results.json`.

The actual private browser passed 115 checks in 41.739s (41.993s including lifecycle): inherited controls and seven-weapon reloads, Custom Breach-12 start, LIFE/ammo/hotbar at 390/1600 pixels, slot 8 remapping, shell expenditure, the visible 2.8-second countdown and reserve conservation. Report: `target/killshot-v31-browser/results.json`. Six first-/third-person WebGL2 grip views rendered without errors in 11.199s; root inspected first-person, side and the actual reload/HUD image. Rendering fixtures prove import and appearance, not hit balance. Browser input is authored DOM input with permission APIs stubbed; trusted fullscreen, physical controls, production-load performance and human playtesting are not claimed.

Independent review identified a client audio regression after these candidate gates: muzzle-flash deduplication also suppressed valid near-miss cracks on reflected/piercing segments or later nearby pellets. Commit `5ca25be2` decouples the crack cache and resets all lobby-local projectile caches on GameJoined, with three new regressions. All 433 native tests passed (168 client, 243 core, 22 server; one existing ignored fixture) and strict Clippy/format checks passed in 15.585s after fixing lint-only empty-vector assertions. The server greeting now correctly identifies v31/Breach-12, and its real-WebSocket test plus the server suite, Clippy and formatting passed in another 12.487s. A post-fix clean build and final-source provenance are required; the candidate hash above must not be substituted for them.

Publication remains pending fresh v31 restart authorization. No live process, tunnel or public v30 file was changed by these tests. League workers own `ember-league` / `ember-league-fable`; read Barza through message 104 and coordinate fresh main/Pages state before any promotion. Preserve their work. Do not resume Knecht's paused fleet timer, restart other games, or mint a tunnel. PR comments remain a human action while WSL/gh is unavailable under repository policy; this file, commits and Barza provide the explicit handoff.

## Final visual correction

The audio-fix candidate `f25def0d`, r1508, passed branch CI [34048390091](https://github.com/EndersGamesDev/EmberEngine/actions/runs/34048390091), another clean build in 9.076s, and the actual eight-peer test in 26.612s (29.094s lifecycle; 6,033 states, eight identical pellet events for every observer). The five dedicated ADS/reload captures passed in 10.606s, but root's image inspection found the original magazine exchange dropped outside the first-person view and drove the remote weapon too high. A passed rendering harness alone was not accepted as visual approval.

The shotgun-only reload pose now rolls the magazine inward, moves the gun forward during exchange and shortens the synchronized withdrawal to 14 cm. No asset, gameplay timing, legacy weapon pose or renderer was changed. Its geometric test bounds both measured palms and all magazine corners within the hip view through the exchange. All 169 client tests and strict Clippy/format checks passed; tests plus the candidate WASM rebuild took 24.979s. Five new captures passed in 9.825s (`target/shotgun-pose-captures/results.json`), and root inspected ADS, the three requested first-person phases and third-person midpoint. The magazine and support hand now remain visible, with the trigger grip attached and the remote weapon lowered. These are authored low-poly assets, not a photoreal/scanned asset claim; human playtesting and broader animation polish remain outside this gate. Exact clean final-source build and actual browser checks still follow this checkpoint.
