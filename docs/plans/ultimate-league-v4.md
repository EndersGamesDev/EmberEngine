# UltimateLegue V4 — crystalforge visual feedback and interface parity

The owner approved a focused V4 pass for UltimateLegue to improve map readability and feedback clarity without changing core rules. The scope is additive presentation and UI polish only, with protocol compatibility retained.

## Owner and integration model

This release is coordinated from the Codex seat (`feature/codex/league-environment-feedback`) using V4 scene and client work prepared on shared branches. Fable owns environmental art and placement details, OpenCode owns read-only review of `web/games/league/v4` feedback behavior, and the release guard stays in Codex-owned `league` root wiring. The V4 work is designed to be a single branchable change with no cross-game process impact.

## V4 scope

- `scene.rs` changes from `e8631c96` rebuild the Crystalforge objective and lane silhouette: updated court rings, core silhouettes, objective geometry and non-trivial lane floor detail.
- `scene/art.rs` and `tools/league/art/bake_environment.py` changes from `39390fa5` replace the V4 ground, lane and court textures.
- `cb8a7198` and `0d4c374d` are the second ground and scene pass, made after the first integrated captures (below).
- `league-core` and `league-client` feedback contracts from `49a33c17` add session-scoped, monotonic feedback events with optional normalized screen coordinates, source/unit IDs and a compact `impact` envelope.
- UI iteration `70d4263f` updates the V4 overlay contract: target readout, ready/impact cues, unavailable action toast, reduced-motion fallback and gesture-gated sound cues.
- `a3e5e6ea` routes the launcher and landing path to V4 runtime files.
- `18f707eb` keeps the existing release guard in place so scoped V4 publishes do not overwrite unrelated game trees.

## Compatibility and constraints

No gameplay protocol bump is part of V4. Core movement, damage, cooldowns, wave timing and economy rules remain unchanged.

No new server features or network fields are introduced in this cycle. No host process or tunnel changes are part of this branch; this is a scene/UI pass in the existing V4-compatible runtime.

## Deliberately excluded from V4

- No combat formula, XP, or economy changes.
- No champion ability tuning or mechanics changes.
- No capture-time input automation.
- No launch story, trailer, or landing narrative changes.
- No protocol migration.

## The second ground and scene pass (fable, 2026-09-07)

Codex ran out of tokens after `70d4263f`; the owner handed the cycle to fable to finish with Spark (the Codex seat) and OpenCode, coordinated on Barza. The first integrated captures (`target/league-visual-v4`, root's 12:28Z run) showed three things the visual brief (Barza 227) had asked to avoid: the lane's 768 mirrored paving block at 14 × 1.4 tiles folded the large slabs into butterfly figures every 10 units; the garden's near-uniform olive picture read as a flat plane; and the objectives were small at mid-lane. What the captures also settled: nothing may stand above 16 cm inside the walkable rectangle (the scene test enforces it because the sim has no obstacles), so the brief's balustrade posts and reed band were dropped rather than faked.

`cb8a7198` re-bakes the two ground pictures without new picture jobs, except one: the accepted paving picture was offset by half in both axes and its cross seam regenerated by a single SDXL inpaint on specht (`SetLatentNoiseMask`, denoise 0.82, seed 6090742, job `fa485dea-c4f7-4126-a6e0-a8e43b46c65b`), then rolled back so the tile's edges are the picture's continuous centre. The lane ships as a plain 1024 repeat at 26 × 2.6 tiles with a warm ivory tint. The garden ships as a 768 mirrored block at 0.78 brightness with a jade tint, multiplied after the mirror by periodic low-frequency mottling (a random grid tiled on a torus, upsampled, centre-cropped), so it has patches a few champions wide, no seam and no mirror symmetry. `tools/league/art/bake_environment_pass2.py` reproduces both from the durable job records and carries the manifest forward; embedded art is 7,794,272 of 8,388,608 bytes.

`0d4c374d` cools the sun to (1.0, 0.90, 0.76) at 0.96 so the moss stays green, and grows the hearth obelisk from 0.60 to 0.72 (crystal at 4.15) and the Court obelisk from 0.40 to 0.46 (crystal at 2.85) with unchanged footprints; the pick-radius test still walks every vertex of both silhouettes.

Verified: `cargo test -p league --lib scene::` 10/10, native build, four hands-off captures at 72 s and 96 s (`tools/league/review/p2-*.png` in the fable worktree, `tools/league/review.ps1 -Mode squad -Cam <x,z[@zoom]>`), compared against root's first captures. Known remaining: a faint soft band recurs along the lane every 5.4 units where the seam was regenerated. A second inpaint at full denoise on a narrower band (`VAEEncodeForInpaint`, job `8e4d8b6a-bb53-402e-a333-5bd9db3aa004`) was tried and came out softer, not sharper, so it was not shipped; the fix, if wanted later, is a fresh seamless source rather than more inpainting. `rustfmt --check` reports pre-existing drift in `scene/art.rs` and `scene.rs` tests at `70d4263f`, not introduced by this pass. `cargo clippy --tests -D warnings` at `70d4263f` failed on 13 findings in `feedback.rs` (12 in library code, 1 in tests); fixed mechanically in `38e58be2` before release, and the Escape menu's Tab cycle (OpenCode's F1) in `bc7baa80`.

## OpenCode's read-only review of the V4 interface (Barza 242)

OpenCode read `web/games/league/v4/{ui.js,index.html,ui.css}` at `70d4263f` against the feedback contract (Barza 228) with the Rust emitters (`feedback.rs`, `world.rs`) as ground truth, by reading only. The contract passes with line evidence: audio is created only inside a trusted user gesture and never at load; retained events are never replayed (session and id cursor, hidden-tab discard); dedupe is by session and id; the local unit is `me.uid`, never a slot; `sx`/`sy` are used directly; damage numbers are the server's net HP loss and crit carries no amount; the unavailable toast, target panel, order status, impact envelope, edge-triggered ready cue, six-voice and eight-per-second sound caps, saved mute and volume, reduced-motion handling and pointer-events pass-through are all present. Three findings: F1, the Escape menu's Tab trap selects only buttons, links and summaries, so the two sound inputs are unreachable by keyboard (fixed before release, see the record); F2, the heal cue for the healed player is unreachable because heal events carry unit 0 (recorded as a V4 limit: cueing the recipient means the emitter must carry the recipient, a widening left for a later cycle); F3, the bottom bar overflows below about 410 px because of the fixed 0.57 scale, identical in V3 (pre-existing, not touched). Observations without action: cast labels are deliberately not floated, the score and feed rebuild their markup every poll without animation, floats freeze at first position under reduced motion, no skip link.

## Released 2026-09-07

[Play UltimateLegue V4](https://endersgamesdev.github.io/EmberEngine/games/league/v4/), reached from the [story and trailer page](https://endersgamesdev.github.io/EmberEngine/games/league/) and the hub.

- Source: main `bc7baa803052582985f30b44af32a4339fdb9c24` (r1799), fast-forwarded from `feature/fable/league-v4`; main CI run 34126155702 green (https://github.com/EndersGamesDev/EmberEngine/actions/runs/34126155702).
- Game publication: Pages `68b6eea83bfb950f8f1be39b4f6a978776d7c82f` via `tools/league/publish.cjs --game-version=v4 --server-commit=cf4a95f0…`, eleven staged files (catalogue entry, `games/league/v4/` page, style, script, art previews with manifest, `pkg/league.js`, `pkg/league_bg.wasm` sha256 `0e31cf22e8bd7990ef874622bbef202a18a8446cdc20441aedf332b3a54acc6c`, `version.json`). Landing publication: Pages `b11fdf7399fa9dca2ad559949c3f4cb7667936c9` via `tools/league/publish-landing.cjs --game-version=v4`. Pages deploy 34126489361.
- Server: the V3 runtime `dusky-osprey-league-v3` on port 7784, protocol 2, commit `cf4a95f0`, reused unchanged; `league-core` and `league-server` are byte-identical between `cf4a95f0` and the source, which the publisher proves before staging. No service, tunnel or task was touched; V1, V2 and V3 trees stay frozen.
- Verification before the push: CI-scope tests (eight core and server crates, `--locked`) green; `wsprobe` example built; league lib tests 51/51; league clippy with tests clean; wasm32 check clean; `visual-v4.cjs --version=v4 --require-ui` five champions × 8 checks; `browser.cjs` with `LEAGUE_GAME_VERSION=v4` clean; native hands-off captures of the second ground pass. After the push: prove-live passed (11 public files byte-identical, 30 frozen files unchanged, real public 1v1 and 3v3 through wsprobe healthy on protocol 2 in 0.58 s and 0.42 s, 14.1 s total); prove-landing passed (CDN bytes of the landing match the source, 4 s); Pages deployment 34126489361 success at 13:19:04Z.
- Not verified: the whole-workspace `cargo test` (fails to compile `tools/linter` tests under `-D unused`, pre-existing on main and outside CI's scope); real audio playback; the 390 px layout beyond OpenCode's reading.

## Planned release record

Landing route and scope are set by this cycle, and the plan is to publish once V4 visual passes are green and browser captures confirm map readability and UI usability on desktop and 390px widths.

This plan documents the design and integration target; the final proof matrix and published hash list are recorded at release time.

## Release checklist intent

- Preserve V3 and keep all non-League pages intact.
- Run V4 smoke/builder checks in the League tooling with `--version=v4 --require-ui`.
- Record commit IDs, branch tip, and wall-time around publication.
