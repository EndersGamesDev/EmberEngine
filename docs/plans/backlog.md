# ember backlog

Follow-ups and known gaps, one line each; pull into a milestone plan before working. Frictions with the orchestration itself go to the orchestration repo's backlog instead.

Presenter and input work is planned in `docs/presenter-architecture.md` and `docs/input-latch.md`, not here and not in `docs/plans/milestone-1.md` — those two documents are the plan of record for the presenter split, the `SceneFrame` contract, the two-consumer input latch, and the capture policy, restated against the v7 renderer. Items below that they now own are marked.

## Architecture (from the v7 re-framing)

- The scene and present command buffers reach the queue in one `submit` call (`crates/ember-engine/src/renderer.rs:798-799`) under a comment claiming the ATW sliced-submission rule (`crates/ember-engine/src/renderer.rs:673-675`). Splitting the call is a one-line fix worth making for the stage boundary, but it does **not** deliver sliced scheduling — on one in-order queue the present still runs behind the scene work either way, and real slicing needs the scene emitted as several separately submitted chunks (roadmap step 7). Fix the comment along with the code (`docs/presenter-architecture.md` §5).
- Shader hot-reload rebuilds both the scene and the present pipeline from one clock (`crates/ember-engine/src/renderer.rs:807-835`) and needs re-homing before the presenter split can be written (`docs/presenter-architecture.md` §8.1).
- The scene-Hz throttle lives inside the renderer, so the renderer decides whether the renderer runs (`crates/ember-engine/src/renderer.rs:608-612`); the clock belongs above both GPU stages (`docs/presenter-architecture.md` §8.2).
- `cursor_ndc()` and `aspect()` have no callers anywhere in the workspace, and the shooter's head comment claiming cursor unprojection (`crates/arena/src/online.rs:2`) is stale — it aims with relative deltas. The API is worth keeping; nobody should assume a shipped game is exercising it (`docs/input-latch.md` §7).
- Pointer capture fires on any mouse press with no button filter (`crates/ember-engine/src/app.rs:195-203`), so in the one game that opts in the fire button is also the capture gesture; and `cursor_ndc` goes stale rather than `None` under pointer lock (`docs/input-latch.md` §7).
- `docs/state-model.md` names the five levels of state the engine holds (abstract scene, sent/received network, local, rendered, presented) and is the conceptual root above the other architecture documents; its open items are whether level 1 gets a checkable artefact at all (no schema exists — the scene concept is hand-written three times, in the sim structs, the protocol enums and the instance builder) and whether the arena client's prediction gets a fixed-step accumulator rather than integrating on the render clock (`crates/arena/src/online.rs:731-740`).
- Weak-device performance is assessed in `docs/weak-device-performance.md`, which owns the prioritized levers and the measurement items: backface culling is off (`crates/ember-engine/src/renderer.rs:963`), `scene_scale` is pinned at 1.0 against an uncapped device-pixel-ratio backing store (`crates/ember-engine/src/app.rs:233-234`), scene depth is stored every frame and read by nothing (`crates/ember-engine/src/renderer.rs:701-704`), the ATW rig is compiled out on the only target that is actually weak (`crates/ember-engine/src/renderer.rs:611-612`), and the workspace defines no `[profile.release]` for the wasm payload.

## Infrastructure

- Server lane: no `ember` loop account exists on any server; provisioning on adler (designated CI/build box) is blocked pending owner action — until then no builds/tests/gates can run (compute never runs on the workstation).
- The live specht services (ember-server, arena-server + Cloudflare tunnel) run under the `ender` human account via nohup and do not survive a reboot; migrating them to the ember loop account with systemd user units is future work.
- ~~The Cloudflare quick-tunnel domain changes on every restart with no credential-safe republish path~~ — DONE 2026-09-04: `host.sh` writes its proven three-game entry to `$RUN/host.json`, `republish-host.sh` lets a credentialed workstation merge it without pushing an unchanged entry, and a host-owned mirror remains the recommended credential boundary. A named tunnel would still remove address rotation itself.
- Add a restart-on-death supervisor for `host.sh` services on pods without systemd, using a supervised tmux session or a dedicated `host.sh supervise` loop.
- Run the off-host deployment watchdog on durable automation that is independent of the developer workstation.
- A protocol bump has a two-sided deploy window with no zero-downtime ordering: page-first leaves the live page unable to join until the server moves, server-first the reverse. `deploy-pages.sh` now warns at ship time (27c20d6) and the refusal is an accurate sentence rather than a mystery, so this is socially mitigated, not solved. The real fix is the server publishing its own protocol version into `server.json` so the hub can warn BEFORE a join is attempted — deliberately not built, because it needs a server change and the server redeploy is itself blocked, and shipping a field nothing populates is worse than not shipping it. Revisit when the redeploy unblocks.
- Retire the root `pkg/arena.js` and `pkg/arena_bg.wasm` compatibility copies after cached pre-rename pages no longer need the Arena bundle under its maiden artifact names.

## From the adoption survey (at b1ef9af)

- ~~ember-server pre-Hello `Ping` slot-parking hole~~ — DONE 2026-08-28, commit 3332f55 (guard originally from d3c6f48's arena-server, not 224bbd2; e2e test with negative control). Residual hardening divergences from arena-server, found in the same lane:
- ember-server has no per-IP connection cap (arena-server: MAX_CONNS_PER_IP = 6) — one host can still occupy the global admission cap for 10 s windows.
- ember-server has no per-connection message-rate cap (arena-server: 30 msgs/tick) — a post-Hello peer can dominate the shared event channel.
- ember-server has no socket read timeout or handshake watchdog — a byte-dribbling client holds two threads until the 10 s sweep reaps it.
- ~~The README "## Pong" section still describes online play as paddle pong over `sim.rs`~~ — DONE with arena v13: the README carries a `## Arena Shooter (v13: Trench City)` section (the WebSocket stack over `shooter.rs`, the Trench City map, what the proto bump does to frozen builds, the runbook), and `## Arena v0: the pong classic` describes the local two-player game only (there is no online pong — the paddle sim runs entirely in the client).
- `Instance` carries a full `rot: Quat` (not a bare yaw — `with_yaw` is just the common-case helper), and normals are rotated by the same matrix as positions; under non-uniform scale that breaks lighting. Now exercised more: bullet tracers are non-uniformly scaled rods.
- Frozen hub game versions become unjoinable on every protocol bump; the hub needs a compatibility story (`old_proto_may_list_but_not_join` covers the server side only).

## From README known limitations

- Snapshot interpolation degenerates to snap-to-latest at ≤60 fps; a proper ~100 ms interpolation delay buffer is future work.
- The arena client connects before the window opens; an unreachable server pauses launch ~4 s before the offline fallback.
- Do not ship a REBUILT bundle from the new home until arena-server is redeployed: main is PROTO 9, the running server is 7. Page-only edits (index.html, games.json) are safe. The redeploy gates jumping, aim elevation, authored levels and the Q shield. The gap widened rather than opened — the freeze already stood at 8 vs 7 — and the shield rides the same window rather than needing its own. The v11 page entry is STAGED on main (games.json, web/games/arena/v11/, hub fallback, ARENA_LIVE): once the server runs proto 9, one `deploy-pages.sh` run ships the shield build; nothing ships until that run.
- Level is produced but never consumed: Sim still takes a seed and nothing reads a Level off the wire (bite 12). Closing with arena v13 — see `docs/plans/arena-v13-trench-city.md` §2 and the section below.
- Web editor picking: aspect()/cursor_ndc() measure winit inner_size while the wasm surface is sized from canvas.client_width()*dpr. Latent today (only the native-only editor reads them); must be measured at three window sizes and fixed WITH the web shell, not before.

## From the Q shield (lane/q-shield-reflect)

- ~~The shield has never been SEEN~~ — DONE 2026-08-30 same day: both draws looked at against a local v9 server via the wasm bundle, and a live reflection (owner flip, mirror heading, kill credit) watched on the wire; see the 9941bfd run in `gate-q-shield-reflect.md`. Still unseen: the plate under real mouse-look (the harness has no pointer lock), and any second human's impression of the sizes.
- A denied `requestPointerLock` kills the wasm client: the first canvas click in a context that refuses pointer lock (sandboxed iframe) surfaces an uncaught `WrongDocumentError` and the game dies with "failed to start". Real tabs grant it, so nobody is hurt today; the capture path (`app.rs:195-203`) should survive rejection.
- A reflected round changes owner, which the client's audio diff reads as the reflector having fired: it plays a remote-shot cue on a reflection, and can raise a false hitmarker for the original shooter (their bullet count dropped while someone else lost hp). Both are cosmetic; a reflect cue of its own would fix them properly and needs a new `Sfx` variant.
- Reflected rounds count against the reflector's `MAX_BULLETS_PER_PLAYER`, so catching ten inside 1.6 s briefly caps their own fire. Deliberate (see the comment at the constant); telling caught rounds from fired ones needs a flag on `Bullet`.
- The shield is judged in the present while the body hit test is lag-compensated, so a shot rewound onto a target's old position can be reflected off empty air where they no longer stand. Same family as every other lag-comp artefact, but newly visible because the round survives to show it.
- `cargo fmt --all -- --check` is not green on main: 13 files across ember-editor, ember-engine, ember-server, game and pong disagree with rustfmt, mostly compact one-line asserts the house style prefers. Either adopt the compact form in rustfmt.toml or format the tree once; today the check cannot be a gate.
- `arena-server` can lose its main event loop while the listener stays up: connection threads still complete the WebSocket handshake, then `events_tx.send(Ev::Connected ..)` fails and each returns silently (`lib.rs:252-260`), so every peer gets `101` followed by an immediate close, and nothing is logged. Observed live on specht 2026-08-30 (PID 116476, confirmed on loopback with the Cloudflare tunnel out of the path). Either supervise the process or let a main-loop panic take it down, so a silent outage becomes a restart.
- ~~`deploy-pong-online.sh`'s health check passes on HTTP `101` alone~~ — DONE 2026-08-31 (11adb49): the arena now runs `wsbot`, which creates a real lobby and counts states, and fire runs `crates/fire-server/examples/probe.rs`, which sends `Hello` and requires `Welcome` plus a matching `PROTO_VERSION`. Neither can pass against a listener whose hub loop is dead.
- ~~Both deploy scripts `pkill -u ender`~~ — DONE 2026-08-31 (11adb49), though not as expected: there is no handover to wait for. specht carries BOTH accounts, and the arena legitimately runs as `ender` deployed from a second workstation, while this one lands as `end`. So the fix is not to rename the user but to stop hardcoding one: both scripts kill only `$(id -un)`'s own processes and then check the port, aborting with the holder's row if it belongs to someone else. Fire additionally unpacks to `~/ember-src-fire`, because the shared `~/ember-src` is the arena's tree and `rm -rf`-ing it would delete the binary the live arena-server is running from. The tunnel pkills are now anchored on their own `--url`; the old bare `cloudflare[d] tunnel` pattern meant redeploying either game killed the other's tunnel.
- ~~The server deploy tarball is 544 MB~~ — DONE 2026-08-31 (11adb49): both scripts use `git archive HEAD Cargo.toml Cargo.lock crates/`, which is ~0.5-1 MB depending on the checkout and contains all 11 workspace members. Because that ships the committed tree, both scripts now refuse to run against a dirty one.

## From the Fire Racer upgrade (castle circuit, drift/boost, online)

- `deploy/deploy-fire-online.sh` had never been run as of 74d554b; the `ender` session on the second workstation is running it from 11adb49. Until it reports a tunnel domain, `fire_ws` is absent from server.json and the v2 page shows "no online server is published right now" and offers practice mode only. The port guard, the toolbox build path and the remote probe invocation are all still untested against specht.
- Everything about the netcode has only been exercised on loopback, where the round trip is ~0 and reconciliation has the easiest possible job. The 25 m convergence bound in `online_e2e` is meaningless until someone runs it across real latency; expect to need an interpolation delay buffer for remote cars, exactly as the arena already wants above.
- `crates/fire/tests/online_e2e.rs` is flaky on the Windows workstation (seen 2026-09-02 during the arena v13 gate, no fire code change involved): in five runs `two_clients_see_each_other_move` failed once ("alice never learned bob had joined", both sockets Open, roster of one) and `a_refused_join_surfaces_a_reason` once, each passing on rerun. Four tests share one process and each starts its own server thread; the 5 s waits race the thread scheduler under a parallel `cargo test`. Suspect the roster broadcast timing, or serialise the suite with `--test-threads=1` and see whether it disappears.
- The `Track` half-width is a single scalar, so kerbs, walls, off-track grip and the wall clamp are all constant-width. A variable-width circuit (narrowing into the hairpin, opening onto the straight) is a `fire-core` change and therefore a protocol question — both peers must bake the same widths.
- `Car::step` takes one `surface_grip` scalar, which cannot express a surface that has different lateral grip, engine multiplier and drag (grass vs kerb vs gravel). Widening it to a `Surface` descriptor is likewise a shared-sim change.
- Generated props are POSITION-only, so `crates/fire/src/meshes.rs` fakes normals per face and projects planar UVs. Faceted is fine for stone; it is wrong for the car, whose curved bodywork wants smoothing groups. Hunyuan's `texgen` pipeline exists in `C:\hy3d\venv\...\hy3dgen\texgen` and was not used — a painted car would remove the planar-UV fudge entirely.
- Nobody has driven this with a controller or on a phone; the input path is keyboard-only and the page's start screen assumes a pointer.
- The handling numbers are argued from the model and checked by tests, not tuned by feel. `GRIP` vs `GRIP_DRIFT` (9.0 vs 1.6) sets how far the car rotates in a slide, and `MAX_SLIP` 0.95 rad caps it; those three are where to start when it feels wrong.
- The lobby has no chat, no kick, and no way to change laps or car count without a redeploy — `ServerConfig` is built from `Default` in `main.rs`.

## From the os-error-997 hunt (2026-08-31)

- The same faulty read guard is still live in FOUR places I did not touch, all `matches!(e.kind(), WouldBlock | TimedOut)` with no raw-os-error check, all of which exit their read loop permanently when Windows returns 997: `crates/arena/src/online.rs:1753` (the native arena client), `crates/arena-server/src/lib.rs:331` (harmless on specht, which is Linux, but wrong whenever the server is run on Windows for testing), and `crates/ember-server/src/lib.rs:219` plus its `tests/e2e.rs`. `fire_core::proto::is_transient_read` is the predicate; arena-server cannot depend on fire-core, so either lift it somewhere shared or inline the `raw_os_error() == Some(997)` check as `wsbot.rs` now does. Left alone deliberately: the arena is live, deployed from another workstation, and this was not the change to make while its page deploy was frozen.
- `send_to` in fire-server returns a bool every caller ignores. It now logs, which is how the 997 hunt got its evidence, but an undeliverable control message is still only a warning. One-shot events — `PlayerJoined`, `PlayerLeft`, `Results` — have no redelivery, so a peer that misses one stays out of sync until it rejoins. A sequence number on lobby events, or resending the roster with each `Phase`, would close it.

## From the specht decommission (2026-08-31)

- `deploy/deploy-specht.sh` (the ember-server deploy) has had none of the treatment the other two got: it still `pkill -u ender`, still tars the whole working tree, still hardcodes the host in its name and its body, and has no health check at all. `ender` goes away with specht, so this script cannot work after the migration. Left untouched because it is unclear whether `ember-server` is still wanted at all — decide that first, then either bring it to the shape of the other two or delete it.
- Both online deploys mint a Cloudflare QUICK tunnel, which takes a new random domain on every restart. That is why an unplanned reboot silently breaks both games until someone redeploys: `ws` and `fire_ws` in server.json still name the dead domain, and the pages keep pointing at it. A named tunnel with a stable hostname would make a restart self-healing and remove the redeploy-after-every-reboot step entirely. Worth doing as part of standing up the new host rather than after.
- Neither online script has been run against a host other than specht, and the `EMBER_HOST` knob added today is therefore untested. The remote side also assumes a `toolbox` container called `ember-build` with a rust toolchain, `~/bin/cloudflared`, and `ss` — all present on specht by history, none guaranteed on a fresh box. Expect the first migration run to fail on one of those and read the error rather than assuming the script is wrong.

## The residual join failure — what has been ELIMINATED (2026-08-31)

- Roughly 1 run in 45-60 of `two_clients_see_each_other_move` still loses a single message after the connection is established. It surfaces as whichever message was next — `PlayerJoined`, `Phase`, `Joined`, or `Welcome` — so it looks like four different bugs and is one. Do not re-diagnose it from the symptom.
- EVERY application-level drop path is now instrumented and has been observed SILENT across ~250 runs while the loss still occurred: the server's frame parse (`undecodable frame`), its hub-channel send, `handle_event`'s unregistered-connection branch, the per-drain rate limiter, `version_ok`'s refusal, `send_to`'s queue-full and connection-gone arms, and the client reader's undecodable-frame and thread-death paths. `hello conn=N proto=1` is logged for both peers on every failing run, so the connection is fully established before the loss. The loss is therefore BELOW the application layer.
- The untested hypothesis is the 5 ms `SO_RCVTIMEO` on both read loops interacting with tungstenite's framing — the same family as os error 997, which was proved. Testing it means lengthening the timeout, which costs outbound latency (up to one tick of the 30 Hz broadcast), so it is a tradeoff rather than a free fix and was not taken unilaterally. The clean answer is async I/O (tokio-tungstenite), where no timeout is needed at all.
- Blast radius, and the reason this did not block shipping v2: Windows-only and tungstenite-only. Browser clients use `web_sys::WebSocket` with no read timeout; the deployed server is Linux, where the timeout is a handled `EAGAIN`. It affects native Windows clients and Windows-side tooling, INCLUDING the deploy probe — retry a failed probe once before concluding a host is broken.
- Measurement note: `cargo test --workspace` STOPS at the first failing test binary, so a tripped flake yields a short run (e.g. suites=10 passed=135) that reads like a mass regression. Use `--no-fail-fast`, and always report the suite count: 31 means complete, fewer means truncated.

## Reboot durability is blocked by SELinux, not by a missing env var (2026-09-01)

- `systemctl --user start|restart|stop|is-active` returns **Access denied** on specht from a non-interactive ssh session, for BOTH accounts. Measured directly, not inferred: as `end` (uid 1008, `Linger=yes`, `/run/user/1008/bus` present) a throwaway unit could be written and `enable`d — the symlink was created — but `start` was refused. Setting `XDG_RUNTIME_DIR` and `DBUS_SESSION_BUS_ADDRESS` changes nothing; the peer session saw the same as `ender`.
- The split is read-vs-write, not env-vs-no-env: `is-system-running`, `show`, `list-units` and `is-enabled` all succeed, while every state-changing verb is denied. `getenforce` is **Enforcing**. This is the same SELinux policy that already denies several ports on this box (see the port note in deploy-pong-online.sh). So the units the watchdog installs cannot be controlled over ssh, and reboot durability via `systemd --user` is unavailable until either the policy is adjusted or the services move to system-level units with a sudo rule.
- Consequence already fixed in the deploy scripts: `is-enabled` is NOT a sound test for "systemd owns this". It reads a symlink and succeeds precisely where control fails, so predicting from it takes the systemd path and then dies on the restart. Both scripts now ATTEMPT the restart and fall back to `pkill`+`nohup` when it is refused.
- Until this is resolved, both games run under bare `nohup` and a reboot takes them offline exactly as before. The off-host `watchdog.sh` is the only thing standing between a reboot and a manual redeploy.

## The vendored linter does not build its tests on Windows (2026-09-01)

- `cargo build --workspace` PASSES on Windows. It is `--all-targets` — and therefore `cargo test --workspace` — that fails, because the breakage is entirely in `tools/linter`'s TEST targets. Anyone reporting "the workspace does not build on Windows" has run the second command; the distinction matters when triaging.
- The scope is larger than the three errors rustc stops at. `std::os::unix` is used in TEN places across six files: `fs::symlink` in `interchange.rs`, `plan.rs` and `spdx.rs`, and `OsStrExt`/`OsStringExt` in `finding.rs`, `plan.rs`, `reference.rs`, `snapshot.rs`, `spdx.rs` and `tests/partition_and_universe.rs`. The suite is Unix-only by construction, not by oversight, so this is a port rather than a patch — the symlink tests in particular have no Windows equivalent without `SeCreateSymbolicLinkPrivilege`.
- OWNERSHIP: `tools/linter` is Wild Sky Maker's (`wildskymaker@proton.me`, the `wildsky` remote). Every commit under `tools/` bar the 9mm conversion pipeline is theirs. It has been misattributed at least once; check `git log -- tools/linter` before anyone "fixes" it, because a local patch over vendored code is lost on the next re-vendor.
- Immediate unblock, and what the other sessions are already using: `cargo test --workspace --exclude linter --no-fail-fast` — 320 passed across 36 suites. Combine with `--no-fail-fast`, since a truncated run otherwise reads like a mass regression.
- Scope correction (2026-09-01, owner audit): the ten `std::os::unix` occurrences are almost all already handled — every helper carries a `#[cfg(not(unix))]` counterpart (`finding.rs`, `plan.rs`, `reference.rs`, `snapshot.rs`, `spdx.rs`, `cli-common`'s `publication.rs`, and the test-support `git.rs`), and the symlink tests are individually `#[cfg(unix)]`-gated so they compile out on Windows by design. The one bare site was the symlink in `interchange.rs`'s link-carries-no-document test, now gated in the sibling pattern. So the suite is ported, not Unix-only by construction; a patch, not a port.
- Remaining verification: `cargo check --workspace --all-targets --target x86_64-pc-windows-msvc` (needs no Windows linker) to enumerate any residual errors — e.g. imports left unused under the denied-warnings law once the unix tests compile out. Queued for the build server, currently unreachable.

## From the many-hosts build

- ~~NO DEPLOY HAS BEEN RUN AGAINST A REAL HOST~~ — PARTLY DONE 2026-09-02: `host.sh up` ran for real on the developer PC (build r437, both servers proved through their tunnels, published as `brass-heron`, hub verified live), and its first run found one bug (no DNS settle before the public probe, fixed in f9c40d6). Still unproven live: `deploy-pong-online.sh`, `deploy-fire-online.sh`, `install-watchdog.sh` and `watchdog.sh`. `host.sh` had before that run end to end only on loopback against the stub tunnel. The first real run is the acceptance test for the whole feature; expect it to fail on something environmental and read the error before assuming the script is wrong.
- The two ssh deploys are exercised ONLY through the PATH shims in `deploy/tests/shims` (`ssh`, `scp`, `cargo`, `sleep`), which log argv and answer what the scripts read back. The git side of those tests is real; nothing about the remote side is.
- `deploy-pong-online.sh`'s public health check runs `wsbot` from the WORKSTATION's checkout while the host runs the ref `EMBER_REF` names, and the join gate is exact equality, so pinning a host to a commit whose `PROTO_VERSION` differs from the workstation's fails the check and never publishes — the package names now resolve per ref, but the protocol the local probe speaks does not. Either run the probe on the host out of `~/ember-src` (which is built from the ref) or build wsbot from a temporary worktree of the ref.
- The browser smoke test over the three pages against a named server is still owed: `9a2a618` promised it "in the next commit" and `78314d6` then recorded `node --check` only, noting the in-game path needs a wasm bundle the deploy builds. So the host chip, the hosts strip, the picker and the cross-host lobby list have been reviewed by reading and unit-tested, never seen working in a browser.
- ~~No commit records a build or test run of the Rust server changes~~ — DONE 2026-09-02 at `dd2a361`: clippy, `cargo test` (19 suites, 168 passed), fmt and the wasm check are green on the six touched crates inside claude-sdk, and the loopback suite (35/35) ran both servers built from that commit.
- Hosts do not talk to each other: no lobby sharing, no player migration, no cross-host matchmaking. The client-side merge covers discovery instead. Deliberate (`docs/hosts.md` §10), and the thing to revisit only if the fleet grows past what one page can probe in parallel.
- No automatic pruning of the address book. A host that vanishes is dropped by the probe, never by the book, so dead entries accumulate and every page pays a probe for each; `publish-host.sh --remove <name>` is the only cleanup and it is manual. Same for `mirrors`, which only a writer can add or remove.
- Quick tunnels still take a new random `*.trycloudflare.com` name on every restart, which is the sole reason republishing and the off-host watchdog exist. A Cloudflare NAMED tunnel per host would make a reboot self-healing and delete most of `deploy/README-watchdog.md`; it needs an account, a domain and a one-time browser consent flow per host.
- Frozen arena pages older than v11 carry no `proto` in `games.json` (v10 and below have none), so the hub cannot route a lobby to them: a lobby on a host speaking an older protocol lists with "needs a proto N build" and no Join. Filling those in means establishing each old build's protocol from history rather than guessing, since a wrong number routes a join to a build that cannot make it. The `handover` flag now makes that safe to do: a version entry gains a `proto` without becoming a routing target until it also carries the flag.
- A name-bound mirror can still be `chosen` on every page by self-reporting an absurd `version` in its `Welcome` — the binding stops it publishing *another* host's address, not out-ranking every honest one, because `viewOf` lets the live value win. Fixing it means ranking mirror-sourced entries below book-sourced ones, or clamping a mirror's `versionNum` to the best book-sourced one; both need the source tag carried from `mergeBook` through `viewOf` into the sort, which is a wider change than the binding was.
- The hub's Join gating (`proto` plus `handover`) and the hosts strip's one-row-per-machine merge are asserted only through `games.json` invariants and by reading: `node --test web/hosts.test.mjs` has no DOM, so nothing checks that a non-handover lobby renders the badge and no button. It is part of the browser smoke test already owed above.
- `feature/one-server` folds the three server binaries into one process behind one tunnel. It changes what runs ON a host, not how many hosts exist, so it must publish the SAME entry with the same per-game keys pointing at its legacy selectors — otherwise a one-server host is invisible to the book.
- The Four Kings game on `feat/four-kings` publishes `kings_ws`/`kings_proto` at the TOP LEVEL of `server.json` through its own `deploy/merge-server-json.py`, so a kings host writes no `hosts[]` entry and cannot be ranked, pinned or shown in the hosts strip. Switch `deploy/deploy-kings-online.sh` to `publish-host.sh --name <host> --game kings --url <wss> --proto <n>`; the key derivation already produces `kings_ws`/`kings_proto` with no code change anywhere.
- The Four Kings page (`web/games/kings/v1/`) reads only the legacy `kings_ws` from server.json and honours `ember-pending` for action/lobby/password but not `pending.ws`, so its games.json entry carries no `"handover"` flag and the hub renders "needs a proto 1 build" instead of a Join for kings lobbies — honest, not a misroute. The upgrade is small: a shim on that page that prefers `pending.ws` over `cfg.kings_ws` before the first connect, plus `"handover": true` on the entry; the fuller version imports `hosts.js` and calls `chooseHost(ROOT, {game: 'kings', proto})` so kings hosts rank and fail over like the others. Live at the time of writing: host `brass-heron`, kings proto 1, build r383 (2268ba1), published at the top level only.
- ~~`host.sh` omitted Kings from its hardcoded game list~~ — DONE 2026-09-04: it manages arena, fire and Kings as three server/tunnel pairs. `watchdog.sh` still hardcodes arena and fire because its deploy-per-game path targets the two older workstation scripts; deriving both lists from `games.json` remains future work.
- Installing the credentialed workstation's `republish-host.sh sokol` scheduler is an operator action: the repository supplies and tests the idempotent command but deliberately does not install a workstation cron or copy the upstream key onto the host.
- The watchdog's never-over-players guard greps `~/pong-server.log` only, so a full Fire Racer race does not defer a redeploy of that host, and a `host.sh`-managed host (which logs to `$EMBER_HOME/log/arena-server.log`) always reads as empty. Both make the guard silently absent exactly where it is needed.
- `install-watchdog.sh` writes units hardcoded to the ssh-deploy layout (`~/ember-src`, `~/ember-src-fire`, ports 7780/7781), so they do not match a `host.sh`-managed host and would start the wrong binaries there. `host.sh` has no unit installer of its own; a reboot takes a self-service host offline until someone runs `host.sh up` again.
- ~~`host.sh`'s header claimed it needed unused `curl`~~ — DONE 2026-09-04 while adding Kings. `deploy-pong-online.sh` still has the same class of comment drift: it says the server binds `127.0.0.1:7778` while `PORT=7780`.
- `publish-host.sh` enters MIRROR mode on any `--file` whose basename is not `server.json`, not just `host.json`, so a typo'd `--file serve.json` silently writes a one-entry mirror file instead of a book. Cheap guard: accept `host.json` explicitly and reject the rest.
- The legacy top-level address key for a game is recomputed only when the book carries that game's top-level PROTOCOL key, so a new game's `<id>_ws` is never re-pointed until `deploy-pages.sh` has written its `<id>_proto` at least once. Correct as designed, but it means the first deploy of a new game must ship a pages deploy too or its legacy key goes stale forever.
## From Four Kings (feat/four-kings)

- Card variants (alternative rule texts per piece) and cross-player card trading: a peer that does not know a card plays a different game, so both are a protocol bump.
- The pawn capture set (`PAWN_CAPTURES`, three diagonals vs the documented two-diagonal fallback) and the joker step (`JOKER_STEP`) are playtest knobs to be settled by playing, not by argument.
- Underpromotion: promotion is automatic and always to Queen; a choice needs a field on the wire.
- Reconnection and spectators: v1 treats a lost socket as a lost seat during Playing and admits nobody who is not seated.
- AFK escalation beyond three consecutive timeouts (a kick from the lobby, a per-player timeout budget) is not designed.
- Move animation keyed on piece ids: pieces jump between tiles today; ids are stable for the whole game so a slide is possible.
- 3D picking: the wasm canvas is a view only, the page's 2D grid is the pointer input; a ray pick against the tiles would make the canvas clickable.
- `net.rs` and `is_transient_read` are copied from fire-core because kings cannot depend on it; lift them to a shared crate.
- The wasm client processes incoming messages only on animation frames; a hidden tab now keeps its seat alive (Hello and Ping left the render loop) but sees the board only when it is shown again.
- The faceted mesh primitives in `crates/kings/src/meshes.rs` (and fire's) belong in `ember-engine`.
- `deploy-pages.sh` still runs cargo, wasm-bindgen and python on the host; routing them through `wsl -d claude-sdk` with `CARGO_TARGET_DIR=$HOME/targets/ember` at idle priority is a separate, non-kings change.
- A named Cloudflare tunnel with a stable hostname would make a restart of the kings pair self-healing and remove the republish step.
- A Windows-side watchdog for the kings pair: there is no systemd in `claude-sdk`, and a `wsl --shutdown` (the documented fix for the WSL boot wedge) kills the server; `deploy/watchdog.sh` is not ported to this game.
- The hub's live-lobby showcase stays arena-only; kings lobbies are listed on the kings page, not on the hub.
- The `hosts[]` entry for this machine, with `kings_ws`/`kings_proto`, once `publish-host.sh` lands from `feat/multi-host`; `deploy-kings-online.sh` step 10 becomes `deploy/publish-host.sh --game kings`.
- `deploy-kings-online.sh` has no "is anyone mid-game" guard (fire's `--require-empty` probe step); a redeploy drops every seated player. Needs a probe flag or a `players` read from `Welcome`.
- `deploy/wsl-detach.ps1` prints a PID and leaves the wsl.exe alive, but its first launch (22:24:07 on 2026-09-01) was followed within 3 s by every wsl.exe call on the machine hanging, including three `wsl --shutdown`s; the hidden session's script never ran, and killing the `wslhost.exe` that wslservice had spawned at the same second released every queued call at once. A second wedge followed the first boot after those shutdowns and resolved, after four minutes, into `Wsl/0x8007000e` "Not enough memory resources are available to complete this operation": the host was out of memory with several lanes and the VM competing, which is a plausible common cause for both and means causation is not settled either way. Before the first real deploy: re-test the hidden launch once with `wslhost.exe` under watch, and if it wedges again switch the helper to redirected stdio (`-RedirectStandardOutput`/`-RedirectStandardError` to files) or a minimized rather than hidden window.
- `wsl.exe ... > file 2>&1` from Git Bash writes the distro's stdout and stderr at separate file offsets, so a later stderr line overwrites the head of the file (measured: a one-line stderr write replaced "stdout line 1" of a ten-line stdout). Any script that logs a `wsl` run to a file must go through a pipe (`2>&1 | tee`); the kings deploy header says so, the other deploy scripts and the cargo helper do not yet.
- Four Kings is not registered in `games/hosted.toml` and has no `games/kings/v001` hosted-contract crate: it runs as its own `kings-server` process behind its own tunnel, which the multi-host contract allows; folding it into the one-server hosting needs a fixture suite and a limits profile first.
- One-server evergreen: recover and audit the historical Arena v7-v11 source and frozen bundles, then admit only versions with complete wire and deterministic fixtures.
- One-server evergreen: assign delisting approval, player-notice ownership, supported-version publication, and the emergency security-removal path.
- One-server evergreen: measure and set Arena and Fire CPU, memory, frame, message, connection, lobby, outbound-byte, and step-time limits before shared hosting.
- One-server evergreen: provision one stable public origin and verify that its tunnel and proxy preserve the legacy game-selector query through WebSocket upgrade.
- One-server evergreen: inventory retained client artifacts and record their immutable hashes beside each hosted version's frozen-client fixtures.
- Arena v12: reconcile `level_fire_is_not_a_free_headshot`'s stale `0.22` rationale with the shipped `HEAD_H = 0.30` without changing the frozen balance rule.
- what-is-this: add a durable report-sink capability only when a second hosted consumer needs it; v1 deliberately emits accepted reports through the host's structured log stream.
- what-is-this: expose a fallible offscreen calibration seam from `ember-engine`; its current winit-owned surface loop and panicking renderer initialization cannot satisfy an optional stage's continue-on-failure contract.
- what-is-this: a real GPU-complete report serialized to 53,088 B against the page's 53,248 B report cap — 160 B of headroom; devices that expose richer adapter identity strings may overflow and lose Submit, so trim GPU notes or renegotiate the cap against the frame budget.
- The specht servers (arena proto 11, fire) still run under the `ender` account but are no longer in the book: `deploy-pages.sh` shipped the v12 hub and the legacy `ws` follows the live protocol, so they serve nobody unless published as a host entry (`publish-host.sh --name <name> --game arena --url … --proto 11 …`) or redeployed from main with `EMBER_HOST=specht`.

## From arena v13 (Trench City)

- Trenches are walls above the floor, not corridors cut below it: `y = 0` is the floor in a dozen places (landing, the bullet floor test, eye height), so a sunken trench needs a `Level` with a heightfield — a different project, and the raised-wall version plays the same.
- Per-face detail beyond one picture per mesh, and normal/roughness maps: the scene pass has one sampler and one base-colour texture per mesh, so "a detailed picture on every polygon" ships as atlas boxes plus box projection (`docs/asset-pipeline.md`, Path E). A second texture binding is the change that would lift it.
- OBB collision for rotated cover: containers are AABBs, so a container along z is a different box rather than a rotated one. `overlaps`, `blocked`, `support_height` and the bullet test all assume axis alignment, and the editor's 90° yaw snap exists because of it.
- `ARENA_HALF` is a shared constant baked into `blocked`, so a bigger or differently shaped arena is a `move_circle` signature change on both peers. Making it per-`Level` was not needed for eight players and was not done.
- The picture-generator connector's gallery URL needs a key this repo does not hold, which is the only reason `tools/v13/fetch_pictures.py` exists — it scrapes `/history` and `/view` over ssh instead. Put the key somewhere the runbook can read it and the scraper can be deleted.
- adler's 4090 holds one generator at a time: the Qwen-Image ComfyUI on `:8188` and the Ideogram 4 ComfyUI on `:8288` cannot both be resident, so v13 ran everything through the second. Nothing enforces or even detects this — someone will start both and wonder why jobs stall.
- The factory backdrop (`assets/models/level-backdrop.glb`, Path C) is no longer drawn by any game — v13's arena draws the sky cylinder and the generated city instead, and its `include_bytes!` went with 1.4 MB of bundle. Decide: delete it, or keep it as the Path C worked example the pipeline doc points at.
- Fog is now per-frame (`Frame::fog: Fog { color, density }`, default = the old shader constants, uploaded in the scene uniform). Every shipped game still passes `Fog::default()`; the arena is the first consumer to set its own haze. Nobody has yet tried animating it per frame (dusk transitions, damage tint); the uniform write is per scene pass so it is free to.
- Two editor gaps from the v13 sim lane: `Obj::from_kind` in `crates/ember-editor/src/lib.rs` places every palette entry at `scale.y * 0.5` and ignores `Kind::base`, so a placed roof lands on the floor until lifted (one line: `pos.y = kind.base + kind.scale.y * 0.5`); and `Ammo`, `Sandbag`, `Rubble` and `Plinth` have no palette entries, so those kinds survive a load-then-export only as geometry, not as kind.
- The generated props' facing is unverified: Hunyuan's "front" view maps to some mesh axis and `Decor.yaw = 0` faces +Z, and nobody has stood in front of the cathedral to confirm the two agree. Overview screenshots looked plausible; an eye-height shot at a facade is the test.
- The v13 cover boxes tile their pictures at a nominal size baked into the mesh UVs, so a box drawn at another size stretches them. A per-instance UV scale in the renderer is the fix if it ever shows.
- `assets/textures/floor_basalt.png` and `wall_basalt.png` (3.9 MB) are no longer embedded by `arena`, and `crates/game` — their last reader — went with the cube demo, so nothing in the workspace reads them; delete them or keep them as the v8 look. `tools/cli-common/src/publication.rs` carries three lint attributes so the Windows gate is green — it is Wild Sky Maker's vendored code, so the patch is lost on the next re-vendor; upstream it or expect the gate to go red again.
- The night coder (opencode against specht's Qwen3.8-27B) was given the v13 layout audit as a read-only task and produced nothing in 25 minutes before its timeout; it is not a reviewer that can be counted on for a whole-file reasoning task at 16 tok/s. Use it for narrow, file-scoped asks.
- The v13 server is undeployed (2026-09-02). ssh to specht stalled at publickey auth for the whole v13 session (TCP connects, `hello` never answered; the WireGuard TCP forwards to :9089 worked throughout), and the one host in the book, `brass-heron`, runs arena protocol 12 built from `origin/main` before this merge. With v13 on main the next `bash deploy/host.sh up` or `EMBER_HOST=<ssh name> bash deploy/deploy-pong-online.sh` produces a proto-13 host; run one BEFORE `deploy-pages.sh`. Shipped page-first, the v13 page is honest but playable for nobody — its chip says "brass-heron is on protocol 12 and this build speaks 13" — while the frozen v12 page keeps finding brass-heron until it is redeployed. adler has no cargo, no toolbox and no cloudflared under `end`, so standing a server up there is an install job, not a deploy.
- The one-server registry (`games/hosted.toml`, `games/arena/v012`, `crates/ember-server`) still lists arena 12 as `latest` and carries no `games/arena/v013` crate: `ember-server` hosts protocols 1–12 and the live `arena-server` speaks 13, the same split the frozen v12 lived with while brass-heron ran 12. Freezing v13 into `games/arena/v013` — wire fixtures, a deterministic trace on `Level::trench_city()`, the legacy lobby projection, a `hosted.toml` entry — is its own lane; nothing in the tree requires it to build, test or deploy the live v13, and it was deliberately not invented in the merge.
- `deploy/tests/run.sh host-loopback` fails on Windows: `host.sh` starts the arena server with `nohup ... &` and its `alive()` pid/start-time check rejects a server that is provably listening (the gate saw `listening on 127.0.0.1:17780 (proto v13)` followed by `arena server did not stay up`). host.sh is a Linux-host script; the Windows workstation hosts through `deploy/deploy-arena-local.sh` instead, which starts its processes through PowerShell.
- v14's web fullscreen (F or the button fullscreens `#stage`) is verified only as far as an embedded browser pane allows: the handler runs and the Fullscreen API is refused there, and the same pane has no WebGL2/WebGPU adapter so the wasm client panics at start. Nobody has yet pressed F on the public page in a real browser; do that once and record it. Native F11 is proven by the engine's own resize log, because GDI screen capture cannot see a flip-model fullscreen swapchain (the capture shows the previous frame in the corner of a black screen).
- v15 viewmodel: the hands are one static pose each, baked from the rigged hand's finger chains; the support fist sits a little right of where a two-hand grip would put it and neither hand animates with the reload dip or the recoil beyond riding the gun. The rig is in the source zip, so a reload or a cocking motion is a posing job, not a modelling one. The moving gun parts were verified by unit test and by reading, not by a captured shot: the synthetic click in the capture harness did not fire.
- v15 viewmodel: 4.5 MB of bundle (two 1024² pictures and ~23k hand triangles), pushing the arena wasm to ~28 MB. Sharing one picture across the five gun parts would need the renderer to share textures between mesh ids.
- `tools/9mm_convert.py` never ran (its FBX was never in the tree) and is superseded by `tools/v15/`; delete it or keep it as the annotated Path D draft it is.
- v16 melee: the butt-strike is first person only. Remote players show nothing on a swing because `PState` carries no melee state; a cosmetic `melee_t` with `#[serde(default)]` would decode across versions and needs the server to set it from `melee_cd`.
- v16 operator: the third-person rig still swings both arms while the rifle rides at the hand; an aiming arm pose (hands on the grip and foregrip, as the FBX's own pose has them) would close the last gap between what the player holds and what others see.
- v16 forearms are tubes in the sleeve's colour; the operator's real sleeves are too coarse to cut. A higher-resolution arm sleeve from the picture generator, or a decimation-aware cut, would replace them.
- v15's revolver runbook (`tools/v15/`) still works and its viewmodel is a weapon pickup waiting for a weapon-model table; the client animates by node name and would need nothing new.
- v17 sword hand: the operator's posed right index is a trigger finger, straight along the trigger, so on the Murasama it lies along the grip rather than around it. Curling RightHandIndex1..3 on a second evaluation of the body (not the shared armature, or the rifle hands curl too) is the fix.
- v17 slash is first person only, like the v16 strike: remote players show nothing on E until `PState` carries a melee timestamp.
- v17 shield: the scutum's protected arc is the sim's `MELEE_ARC`-independent shield rule, not the board's outline; a player may read cover the rules do not give at the board's edges.
- v17 VRAM: five viewmodel meshes now clone five pictures (2048 + four 1024); a renderer that shares one texture between mesh ids would let the sword hand reuse the hands' picture.

## From arena v18 (Freight Yard)

- The M4 is a table row with no mesh: its FBX and base colour are compressed entries in a RAR5 archive (`assets/m4/source/source/M4 Carbine by Umang Rank.rar`) and this workstation has no extractor, so `LOOT_POOL` excludes id 4 and the client draws the sidearm mesh for it. The unblock is `winget install 7zip.7zip`, `7z x` into `assets/m4/source/`, a `build_m4()` in `tools/v18/build_weapons.py` (one material, like the AK), and 4 added to `LOOT_POOL` on the server. No bump: clients never derive from the pool.
- Block tops are perches: a loot block is an ordinary solid box to all four `base` rules, so a train-zone block's top (3.30) is a 0.35 m step from any roof within a hop, 1 m wide and exposed on every side, and a bonk from below still pays while someone stands on it. Accepted in `docs/plans/arena-v18-freight-yard.md` section 5.1 and placed nowhere near a spawn; if play proves the perches wrong the fix is a support-skipping rule for `Cover::Loot` in the shared sim, which is a protocol bump, so it is not done blind.
- A roof-only reward beyond the two blocks over C2 (a second-storey loop of its own) is not designed; the roof block is the only reward the floor cannot reach.
- The sniper's bolt-cycle keyframe and the landing dip are not in the v18 feel pass; the sniper settles on its 0.9 s recoil curve alone and a landing plays nothing.
- Remote melee and remote bonk animation on `PState` are still missing (the v16 and v17 lines above): a cosmetic `melee_t` and a bonk timestamp with `#[serde(default)]` would decode across versions; the remote bump today comes only with `S2C::Loot`.
- `deploy-pages.sh`'s `copy_pkg` copies only `arena.js` and `arena_bg.wasm`, so a wasm-bindgen `inline_js` snippet never reaches the live page; that is why rumble goes through `window.emberRumble` on the page. A `copy_pkg` that carries the `snippets/` directory would let the engine own the actuator call once web-sys stabilises it.
- The freight yard numbers are argued and tested, not played: every sightline is decided by `no_yard_spawn_sees_another`, and the knobs that move first are Q2's length, the sandbag positions and `LOOT_RESPAWN_SECS`.
- Gamepad input and rumble are verified by compile and unit test only: no pad is attached to this workstation (gilrs listed none on 2026-09-03). gilrs on Windows is Windows.Gaming.Input (WGI, the crate's default backend there: rumble only on pads it classes as a Gamepad, no DirectInput force feedback), browsers expose a pad only after a button press, Firefox and Safari have no actuator, and native Linux is untested. The v18 page's status line names the probe result once a pad is seen.
- The v18 viewmodel GLB is 17.4 MB against the plan's 16 MB line even with the Vityaz and sniper atlases at 768 (the pre-decided first fallback), and the wasm bundle measured 39.1 MB after `wasm-bindgen` (29.6 before v18). The next levers are the hands' 2048 picture (2.7 MB), the shield's 1.6 MB, the AK and revolver-frame pictures at 1024, and the 15 000-triangle budgets; a project decision, not taken blind.
- The Freight Yard train-zone alley (z 7.4..14) is a 48 m level sightline as authored: its blocks hang at 2.30 and are transparent to eye-level fire, and nothing else in the alley rises above 1.45. `the_yard_has_a_long_sightline_and_the_train_zone_does_not` pins the alley being sealed from the yard and the backlot instead. Cutting it needs a new container in the alley, not a named knob.
- The bullet sweep resolves bodies in player order, not along the segment: with pierce, a body 0.9 m behind a raised plate on one sniper tick segment can be hit before the plate reflects the round (both bodies within a 1 m segment, which needs interpenetrating bodies since players do not collide). Pre-v18 the same order chose which of two stacked bodies took the round; deterministic on both peers. The fix is to collect the tick's contacts with their parameter and resolve them sorted, stopping at the first reflect.
- A rocket's TTL exit is unreachable in play: at 24 m/s horizontal speed a rocket meets the wall or the floor inside a 48 m arena before its 2.5 s, so `ttl` is a safety net that only `a_rocket_detonates_at_its_ttl_at_the_floor_and_at_the_wall` reaches by hand.
- `Bullet.hit_mask` is a bit per player id: a round that pierced a player who then leaves keeps their bit for up to its `ttl` (1.0 s for the sniper), so a joiner allocated the same id inside that window cannot be hit by that one round; `remove_player` could clear the bit.
- The wsbot's `--bonk` hunt has no pathfinding (a straight line, a hop a second, an 8 s budget per block): on the yard it reaches the two blocks nearest the spawns and gives up on the rest, which is enough to count Loot events on the wire and no claim about every block.
- The RPG's hold point: the tube reaches 0.78 m behind the grip, so at the v16 hold offset it sits beside the eye; a per-weapon hold offset for a shoulder-fired tube is the fix once play says where it should sit.
- gilrs's own default dead-zone filter stacks under the engine's 0.18 radial zone on native (a little extra dead travel); winit's web `Focused` events gating the pad are unverified in a browser; the web status takes the shim's own answer (whether a pad with a `vibrationActuator` was found), which no browser has exercised yet.
- `EMBER_WEAPON=1..7` on the native client draws every player with that weapon id (a review aid for `tools/v18/capture.ps1`, cosmetic like `EMBER_CAM`); there is still no way to hand a chosen weapon to a player for a capture other than a lucky roll.
- Deploying proto 15 freezes the v13 to v17 pages list-only and needs the local host rebuilt (`deploy/deploy-arena-local.sh up`) before `deploy-pages.sh`; the v18 page and `games.json` are staged on the branch on `proto: 15` exactly as v13 was staged before its server moved, and moving the host is the user's separate decision (the specht ssh stall means this workstation is the only host that can move).
- Trench City's four v13 pads are gone in favour of four `?` blocks at the tunnel mouths (`Cover::Loot` at `[-0.5, 9.3]..[0.5, 10.3]`, base 2.30, quarter-turned): the first lobby created there got the pads rolling loot and no blocks at all, and the Mario mechanic has to exist on both maps. That is the second bump of v18 (`PROTO_VERSION` 15): the map changed under a name that did not, so a first-cut v18 bundle would predict the pads-and-no-blocks map, be paid from a block it cannot see and run across pads that never pay; the name gates a new map, never a changed one. Only the seeded arena carries pads now, and the frozen fixture is `trench-city-v18.json`.

## From arena v19 (modes)

- Teams balance only on join: `remove_player` leaves the survivors where they are, so a 4/4 lobby that loses three blues plays 1/4 until someone new lands on blue. A rebalance on leave has to move a living player mid-fight, which is a design call (who moves, and does the move respawn them), not a fix.
- No vote or rotation between modes: a lobby plays the mode it was created with for its whole life, round after round. A rotation is a `C2S` message and a lobby-level list; a vote is that plus a tally on the pause.
- Capture the flag is the natural fourth mode, with the loot blocks as flags, and is a bigger design than the three that shipped (a carried object on `PState`, a drop on death, a base per team) rather than a limit and a rule.
- Block respawn (`LOOT_RESPAWN_SECS`) is one number for every mode; king of the hill in particular may want the blocks nearest the hill on a longer clock so the hill is fought for with what a player brought, not what hangs beside it.
- No team voice or ping: a team in team deathmatch has its colours and nothing else to coordinate with. A ping is a cosmetic `C2S` message the server rebroadcasts to the team.
- The Tab scoreboard does not show the hill: in king of the hill it lists hill points in `SCORE` and frags in `FRAGS`, but nothing there says who holds the hill now; that is on the status line only.
- The seeded arena's hill (`[-2, -2]..[2, 2]` at `top: 0`) is a placeholder square on the open floor so the seeded tests can drive the mode, not a place to fight over; the authored maps have the dock and the plinth.
- Deploying proto 16 freezes the v18 page list-only alongside v13 to v17 and needs the local host rebuilt (`deploy/deploy-arena-local.sh up`) before `deploy-pages.sh`; the v19 page and `games.json` are staged on the branch on `proto: 16` exactly as v18 was staged before its server moved.
- The v19 page is verified by reading and by `games.json` parsing: the embedded browser pane has no WebGL2, so nobody has yet created a team or hill lobby from the page in a real browser and seen the pill on the row.
- `PState` carries only `score`, so the Tab scoreboard in king of the hill shows hill points instead of frags; a `PState.frags` (`serde(default)`) would let it show both, additively and without a bump.
- The Tdm status line names my team first but cannot colour it: `#status` is plain text by design (no innerHTML); a page-side team element is the fix.
- arena-core's `free_for_all_is_bit_identical_to_v18_until_the_limit` pins a fingerprint computed on this Windows workstation (x86_64 MSVC libm); the launch goes through cos/sin/tan, so another libm could differ in the last bit and the pin would need regenerating from the v18 code the same way (the constant's comment says how).

## From arena v20 (realism)

- No additive blending: the tracer glow, the muzzle flash and the plume smoke are opaque boxes because the scene pass is `BlendState::REPLACE`. The fix is a renderer change, a second pipeline with `BlendState::ADDITIVE` drawn in a depth-read-only pass after the opaque scene, recorded in `docs/minimum-requirements.md` first since it is a project decision.
- No decal atlas: an impact mark is a 0.10 m near-black box on the surface, not a picture, because the renderer samples one texture per mesh and a mark's mesh id is shared by every mark. A decal atlas (one texture, per-instance UV rectangles) is a renderer change.
- The recorded-sample slot `crates/arena/assets/sfx/` ships empty: every cue in v20 is synthesised. Filling it needs licensed 16-bit mono 44.1 kHz recordings this repo does not have; the reader and its test are in place, and a WAV in the slot replaces the synth at build time with no code change.
- Remote bodies have no hit reaction and no death animation: a hit is the flash and the sparks at the contact, a death is the body vanishing. A reaction needs a cosmetic timestamp on `PState` (`serde(default)`, no bump) and a pose on the split body.
- No penetration through thin cover: a round stops on the first box face it meets whatever the box is, so a sandbag and a container stop a sniper round the same way. Penetration with damage falloff is a table column per cover kind and a second segment past the exit face, a sim change on both peers.
- No wind and no drop table for the sniper beyond 120 m: its round expires at its range, and the arena is 48 m across so nothing reaches that far today; a longer map would want both.
- Deploying proto 17 freezes the v19 page list-only alongside v13 to v18 and needs the local host rebuilt (`deploy/deploy-arena-local.sh up`) before `deploy-pages.sh`; the v20 page and `games.json` are staged on the branch on `proto: 17` exactly as v19 was staged before its server moved.
- The v20 page is verified by reading and by `games.json` parsing: no browser has yet seen a tracer, heard the panned shot or the crack from the page, and the page's hint describes what the plan and the client tests pin, not what an ear has confirmed. The sound check in the plan is a plot of every cue's envelope and centroid, not a listen.
- kings e2e: `two_clients_create_join_start_move_and_time_out` (crates/kings/tests/online_e2e.rs) is flaky under the full parallel workspace test run (bob's start window closes while the creator's start is in flight); passes alone. Widen the window or serialise the online_e2e tests.
- The rounds (`crates/arena/src/rounds.rs`) draw the sidearm's round as a 9x19 because the spec grouped it with the Vityaz, while `weapon_stats` and the README call it a .45 at 280 m/s; a `Round::FortyFive` (11.5 x 17 mm, round nose) is one profile and one `round_for` arm.
- The native crosshair is a pair of world-space cubes 3 mm thick at 1.2 m (two pixels on a 1080-line screen, one on 600, four at 4K; the first cut's 1.5 mm fell between pixel centres at 800x600 and drew nothing), occluded by anything nearer than 1.2 m; a screen-space overlay pass would fix both properly, and the web page's DOM crosshair has neither problem.
- The streak's base is 0.9 of the round's drawn heel (12 to 26 mm across) tapering to a point over up to 8 m, so past 10 to 20 m at 1080p it is under a pixel and the observer in the first capture saw a 1 to 2 px line at 5 m; if the second capture shows it faint or sparkling at range, floor the base radius (about 15 mm) or raise `STREAK_LEAD`, and keep `ROUND_SCALE`.
- The round meshes are lit like every other surface (one base colour, sun and ambient), so a copper jacket does not glint; the jacket's picture is the whole material, and its colour (`rounds::COPPER`) was reasoned from the sRGB upload, never judged on screen.
- The capture harness (`tools/v18/capture.ps1`) fires the sidearm in every run whatever `EMBER_WEAPON` says, so only the 9 mm round and the blue sidearm streak have ever been drawn; the AK, M4, Casull and Lapua meshes and their tracer colours are unseen, and no spawn has a line of fire long enough to catch the round itself in flight (it crosses 5 m in one frame).
- In the first capture, client A's view jumped by tens of degrees after the focus click in both `-Weapon 1` runs and never in the `-Weapon 3/5/6` runs, and in one `trench-city` run A's websocket closed 1.2 s after its first shot with nothing logged on either side; neither was investigated.
- The v20 rounds pass left the muzzle plume, the impact bursts (sparks, dust, splinters, the sandbag's puff, the body flash) and the casings as small cubes by design (`feel::Puff` is a cube with a velocity, and a puff is too small and too brief for its shape to read); only the round, its streak, the hole and the flash star have a shape of their own. The rocket's blast mark is the same flat disc as a bullet hole at 0.5 m, with no scorch or crater.
- `EMBER_ROUNDS=1` on the native client hangs every v20 shape (the five rounds, a Lapua with its streak, the AK's hole, the rocket's mark at a fifth, a flash star) half a metre in front of the eye every frame (`online::push_showcase`); it has been built and unit-tested but no capture has been taken under it yet, so the copper jacket, the streak's taper, the hole's size and the star's proportions are still unjudged on screen. `tools/v18/capture.ps1` has no switch for it: set the variable on client A by hand.
