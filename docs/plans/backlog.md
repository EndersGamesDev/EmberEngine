# ember backlog

Follow-ups and known gaps, one line each; pull into a plan before they are worked. Frictions with the orchestration itself belong to the orchestration repository's backlog, not here.

Presenter and input work is planned in `docs/presenter-architecture.md` and `docs/input-latch.md` and is not listed here; the engine chain's own follow-ups live in `docs/plans/engine-chain-backlog.md`.

Each line says what is wrong or missing, where it can be seen, and what closes it. A line that cannot be settled from the tree begins its remedy with `needs:` and names the browser, host, device or measurement that would settle it.

## engine-renderer

- The scene and present command buffers reach the queue in one `submit` (`crates/ember-engine/src/renderer.rs:1313`) under a comment claiming the ATW sliced-submission rule (`crates/ember-engine/src/renderer.rs:1144`); splitting the call is a one-line fix for the stage boundary but delivers no slicing on one in-order queue, so fix the comment with the code.
- Pointer capture fires on any mouse press with no button filter: `crates/ember-engine/src/app.rs:290` binds `button` and then tests only `state == ElementState::Pressed`.
- A rejected pointer-lock grab is dropped unhandled at `crates/ember-engine/src/app.rs:302`, and the capture path has no rejection arm; needs: a sandboxed-iframe run to confirm whether a denied grab still kills the wasm client.
- `cursor_ndc` goes stale rather than `None` under pointer lock: `crates/ember-engine/src/app.rs:282` clears it on `CursorLeft` and nothing clears it on grab.
- `crates/ember-engine/src/assets.rs:60` returns `None` for any non-8-bit glTF image with no log and no error, so a 16-bit export ships an untextured model; the hazard is written down twice (CLAUDE.md's renderer table, `docs/asset-pipeline.md:129`) and diagnosed nowhere.
- `docs/minimum-requirements.md:9` states `EXT_color_buffer_float` and `Depth24PlusStencil8` as the project's device floor, but that is the labs' floor: the engine that draws all four games asks `Limits::downlevel_webgl2_defaults()` and `Features::empty()` and uses `Depth32Float` with no stencil (`crates/ember-engine/src/renderer.rs:600`, `:481`). CLAUDE.md repeats the doc's floor as the project's.
- Harbor roof-bridge and indoor contact shading still shows the coarse voxel interpolation bands visible in the v29 WebGL2 captures; measure a budget-preserving reconstruction or filter improvement separately from map collision and parkour. No new lighting feature was claimed in v29.
- `scene_scale` is pinned at 1.0 (`crates/ember-engine/src/renderer.rs:647`) against a backing store sized from `client_width() * dpr` with no cap (`crates/ember-engine/src/app.rs:336`).
- The ATW rig is compiled out on wasm, the only genuinely weak target: `crates/ember-engine/src/renderer.rs:1039` forces `scene_pass_due = true` there.
- The scene-Hz throttle lives inside the renderer (`crates/ember-engine/src/renderer.rs:1033`, set by `set_scene_hz_cap` at `:965`), so the renderer decides whether the renderer runs.
- Shader hot-reload rebuilds the scene and present pipelines from one 60-frame clock (`crates/ember-engine/src/renderer.rs:1322`); it needs re-homing before the presenter split.
- Scene depth is stored every frame (`crates/ember-engine/src/renderer.rs:1199`) and sampled by nothing; the comment names warp stage C and future SSAO/TAA as the readers.
- Backface culling is off on both scene pipelines (`crates/ember-engine/src/renderer.rs:1676`), so the interiors of open shapes render solid.
- One base-colour texture per mesh: per-face detail and normal/roughness maps need a second texture binding in the scene pass, and `Instance` (`crates/ember-engine/src/renderer.rs:149`) has neither that nor a UV field, so v13 cover boxes bake a nominal tiling a per-instance UV scale would remove. No test pins either absence.
- One GPU texture per mesh id: twelve gloves that share a single image in the file still consume about 16 MiB with mipmaps (`docs/weapon-grips.md`); sharing a texture between mesh ids is the fix, and the same rule sets the viewmodel's floor.
- The scene pass has no additive pipeline — opaque is `BlendState::REPLACE` (`crates/ember-engine/src/renderer.rs:1779`) and the particle pass is alpha (`:1668`) — so a depth-read-only additive arm is a device-floor decision to record in `docs/minimum-requirements.md` first.
- No decal atlas: an impact mark is an untextured near-black disc wearing `feel::MARK_COLOR` (`crates/arena/src/rounds.rs:431`).
- The hole disc has 12 sides (`crates/arena/src/rounds.rs:168`) and reads as a dodecagon at a metre or through the scope; 16 or 20 would fix it.
- The native crosshair is world-space geometry pushed as instances (`crates/arena/src/online.rs:476`, `:481`) with no screen-space overlay pass, so it goes sub-pixel at common resolutions.
- `ember-engine` exposes no fallible offscreen seam, so what-is-this reports `unavailable_reason: Some("no safe offscreen seam")` (`crates/what-is-this/src/lib.rs:680`); the winit-owned surface loop and the panicking renderer init cannot satisfy a continue-on-failure contract.
- There is no viewmodel pass, so first-person geometry clips into walls; a wall-contact retraction or a second pass is the fix.
- Characters, gloves and mixed-material meshes need authored masks and normal detail the scalar-only `Surface` cannot carry (`crates/ember-engine/src/renderer.rs:149`); upgrading the operator assets to use them is a measured project of its own.
- Thin geometry aliases with no antialiasing path in the scene pass (`crates/ember-engine/src/renderer.rs:1712`, one sample per pixel on every pipeline), which railings, cables and grips show first.
- The sky term is a pure function of direction (`crates/ember-engine/src/shader.wgsl:96`) with no occlusion input, so a roofed interior receives the same sky light as open ground; occluding it under roofs is a separate feature.
- Reflections are the analytic sky, sun and clouds alone and never scene geometry, as the shader states (`crates/ember-engine/src/shader.wgsl:95`); local reflection probes are a separate feature with a device-floor cost to establish first.
- Harbor's moored ship and sea sit outside the playable terminal; boarding, water physics, animated cargo and water reflection have no renderer path, v26 having added only the analytic sky reflection.
- `aspect()` and `cursor_ndc()` measure winit `inner_size` (`crates/ember-engine/src/app.rs:374`) while the wasm surface is sized from `client_width() * dpr` (`:336`); only the native editor exercises them, so the divergence stays latent until the editor gets a web shell. needs: a picking measurement at three window sizes.
- `crates/labs/heap/src/page_contract.rs:120` asserts that English sentences appear verbatim in `docs/gpu-heap-lattice.md` and that a sliced range of `lattice_gpu.rs` source text contains no `.unwrap()` — a prose lint and a source lint carried as tests.
- v27 contact shading: an authored normal/roughness detail set for a representative material set is unstarted, as is precomputing the map-join bake and assessing sub-cell gaps (`docs/plans/arena-v27-contact.md`); needs: a GPU for the mip/aliasing and frame-cost gates that admit it.
- Round meshes are lit like every other surface, so the copper jacket constant (`crates/arena/src/rounds.rs:190`) is a base colour that cannot glint; needs: a capture to judge it on screen.
- needs: a pad on native and in each browser to exercise gamepad input and rumble, verified so far by compile and unit test only (`crates/ember-engine/Cargo.toml:33`, backend notes at `crates/ember-engine/src/app.rs:517`); gilrs's own dead zone stacks under the engine's 0.18 zone, the web focus gating and the actuator probe are unexercised, and the page probe reports at `crates/arena/src/online.rs:1755`.
- needs: a GPU and a frame-cost measurement to assess edge-aware filtering or higher resolution for the small stepped contact band where spawn walls meet the ground.
- `EngineConfig` is exhaustive (`crates/ember-engine/src/app.rs:50`), so a new field forces edits at three unrelated call sites; `#[non_exhaustive]` with a defaulted constructor contains it.
- Procedural primitive construction is repeated across `crates/kings/src/meshes.rs:25-198`, `crates/league/src/scene.rs:262-404`, and `crates/arena/src/rounds.rs:456-477`; there is no engine primitive module to receive it, so add behavior-pinned builders before migrating only byte-equivalent shapes.
- Level 1 of `docs/state-model.md` has no checkable artefact: the scene concept is hand-written three times, in the sim structs, the protocol enums and the instance builder (`crates/arena/src/lib.rs:285`), with nothing generated or shared between them.

## platform-web-deploy

- Four lab pages import bundles nothing builds, ships or links: `web/labs/heap/index.html:96`, `web/labs/heap/bench.html:90`, `web/labs/heap/spike.html:41` and `web/labs/layer/index.html:78` load cdylib bundles `deploy/deploy-pages.sh` never builds, `web/games.json` never lists and no page links, so nobody can reach them. Ship them or delete the pages.
- `copy_pkg` carries only the `.js` and `_bg.wasm` artefacts (`deploy/deploy-pages.sh:181`), so a wasm-bindgen `inline_js` snippet never ships; the consequence is recorded at `crates/ember-engine/Cargo.toml:44` and worked around at `crates/ember-engine/src/app.rs:922`.
- A name-bound mirror can be chosen on every page by self-reporting an absurd version: `web/hosts.js:308` lets the live value win, the returned view carries no source tag, and the ranking sorts on it at `:371`. The fix carries a source tag from the book merge through the view into the sort.
- `deploy/publish-host.sh:174` enters mirror mode on any `--file` whose basename is not `server.json`, so a typo silently writes a one-entry mirror over the book; accept `host.json` explicitly.
- `deploy/publish-host.sh:418` recomputes a legacy top-level address key only for games whose protocol key is already in the document, so a new game's address key stays stale until `deploy-pages.sh` writes that protocol key once.
- Frozen arena pages below v11 carry no `proto` in `web/games.json` (entries from `:169` down), so the hub cannot route a lobby to them; `handover` is what makes filling them in safe.
- Four Kings publishes its address and protocol at the top level through its own merge (`deploy/deploy-kings-online.sh:358`), so a kings host writes no `hosts[]` entry at all, and the script records the switch as future work at `deploy/deploy-kings-online.sh:33`; `deploy/publish-host.sh` has landed and derives those keys with no code change.
- The kings page honours `ember-pending` for action, lobby and password but not the pending address (`web/games/kings/v1/index.html:799`), and the kings entry in `web/games.json` carries no `handover`.
- The hub's live-lobby showcase resolves only the arena's live path (`web/index.html:332`), so kings lobbies are listed on the kings page alone.
- The commit a host builds a game from is recorded nowhere the repository carries: the shipper takes it from a deployed `version.json` the tree does not hold (`deploy/ship-host.sh:7`), so a host built off main can serve a game protocol its live page was never checked against, Fire included. Record the built commit per game beside the catalog.
- `games/hosted.toml:96` still marks arena v12 `latest` and `games/arena/` ends at `v012`; freezing v13 and later into the one-server registry is its own lane, and `crates/ember-server/src/registry.rs:391` pins one latest per game.
- Main pushes do not build Pages: the only main-push job beyond tests moves the `ci-passed` pointer (`.github/workflows/ci.yml:73`) and `deploy/deploy-pages.sh` is invoked by hand, so every release needs a named publisher, a matched five-bundle rebuild and a server-first rollout.
- `deploy/tests/run.sh:26` defines ten suites and `.github/workflows/ci.yml:69` runs nine by name; the omitted `host-loopback` is the only suite that drives `host.sh up/status/update/down` for real, and neither file says it is excluded or why.
- Nothing runs the two JavaScript suites: no gate script, CI job or deploy suite invokes `web/hosts.test.mjs:1` or `tools/v28/settings.test.cjs:2`, so they are commands a person has to remember.
- `deploy/tests/test-pages.sh:163` hardcodes the fire, kings and arena-v0 live paths while deriving the arena one from `deploy-pages.sh`, so a Fire or Kings version bump changes the script and the catalog together and leaves the test proving the previous path.
- The hub's Join gating and the hosts strip's merge are asserted only by reading and by `games.json` invariants: `web/hosts.test.mjs` has no DOM harness and the badge-and-no-button rendering at `web/index.html:651` is untested. needs: a browser smoke test over the three pages against a named server.
- needs: a real browser to create one team lobby and one hill lobby from `web/games/arena/v19/index.html`, verified so far by reading and by `games.json` parsing.
- needs: a real browser to press F or the fullscreen button on a public arena page; the handler and its page furniture exist (`web/games/arena/v28/index.html:97`) and nothing records a press.
- needs: a Windows workstation to run `deploy/tests/run.sh host-loopback`, which fails there because `deploy/host.sh:646` starts servers detached and then tests a pid-and-start-time liveness check (`:249`).
- needs: a wasm build to re-measure the arena bundle, last recorded at 39.1 MB after `wasm-bindgen`; the levers named are the hands' 2048 picture and the shield and AK/revolver 1024s inside `crates/arena/assets/viewmodel.glb`.
- needs: two `wasm-bindgen` builds on a server lane to pin the cause of two bundles four bytes apart from one commit, or to accept the non-determinism.
- `deploy/deploy-pages.sh:75` runs cargo and wasm-bindgen directly on the host and resolves a local python at `:38`; routing them through a build distro at idle priority is a separate change.
- The root compatibility copies of the arena bundle for cached pre-rename pages are still minted on every deploy (`deploy/deploy-pages.sh:195`); retire them once no cached page imports from the site root.
- Nothing prunes the address book on a schedule: `deploy/publish-host.sh:25` `--remove` is the only deletion path, and the render-time filter at `web/hosts.js:361` drops a vanished host from the view but never from the book.
- Hosts do not talk to each other — no lobby sharing, migration or cross-host matchmaking — and the client-side merge at `web/hosts.js:137` is the whole discovery story; recorded as deliberate at `docs/hosts.md:177`.
- The workspace defines no release profile for the wasm payload: `Cargo.toml` carries only the development profile and its per-package override.
- `web/games.json` describes fire v2 as protocol 1 with the castle-circuit note, but the published release is the protocol-2 GT Circuit V2 cut (`gh-pages` `80cf00ef`), so the launcher advertises a build the site does not serve; `CHANGELOG.md` records the published truth until the entry is corrected.

## infrastructure-servers

- `arena-server` can lose its main event loop while the listener stays up: the failed send at `crates/arena-server/src/lib.rs:361` returns silently and logs nothing. Off-process restart now answers the death, not the silence.
- Quick tunnels mint a new random hostname on every restart, which is the sole reason republishing and the off-host watchdog exist: `deploy/host.sh:307` records that a fresh one arrives on every restart, scraped at `:316` and started at `:349`, and `deploy/wsl/kings-run-tunnel.sh:14` says the same for the kings pair. A named tunnel per host needs an account, a domain and a browser consent flow, and would make a restart self-healing.
- `deploy/install-watchdog.sh:101` writes units hardcoded to the ssh-deploy layout — two source directories and two fixed ports — so on a `host.sh`-managed host they start the wrong binaries.
- The watchdog's never-redeploy-over-players guard greps a single hardcoded arena log path (`deploy/watchdog.sh:250`), so a Fire race does not defer a redeploy and a `host.sh`-managed host always reads as empty.
- `deploy/watchdog.sh:68` hardcodes the arena and fire game list with per-game deploy scripts at `:70` and knows nothing of kings; deriving both lists from `games.json` is the fix.
- The kings pair has no supervisor on its own host: the build distro carries no systemd, so nothing outlives the deploy that started it (`deploy/wsl/kings-run-server.sh:9`), `wsl --shutdown` kills the server outright (`deploy/deploy-kings-online.sh:25`, `:27`), and `deploy/watchdog.sh` is not ported to kings.
- The host updater has no bounded persisted backoff for a provider's 429 and 1015 refusals and does not tell a failed public-tunnel launch from a dead game server, so the two-minute repair timer can extend a provider block or restart healthy local games; reproduce it with isolated shims before changing production behaviour (`deploy/host-tick.sh:9`, minting rationed at `deploy/host.sh:340`).
- Nothing schedules the shipper's check, deploy or republish for the dedicated node, whose quick-tunnel addresses rotate on every restart (`deploy/host.sh:307`), so its entry in the book goes stale unattended; a workstation timer is the interim and a bound mirror holding its own credential is the destination.
- `deploy/host.sh:179` accepts a prebuilt product directory on a host that cannot build, and the stamp beside it is the only statement of identity (`deploy/host.sh:148`) with nothing verifying the binaries are the ones it names; a sha256 manifest written by the shipper and checked there closes the gap.
- `deploy/watchdog.sh:70` maintains a host by running the per-game deploy scripts, which build, so a host that cannot build needs a second supervision shape; folding the shipper in behind a per-host "ships, does not build" flag gives one watchdog rather than two.
- The shipper's prune keeps the three newest product directories by modification time (`deploy/ship-host.sh:389`, `:391`), so a rollback is "the second newest" rather than a named commit; rolling back to an explicitly named commit would make it deterministic.
- The shipper's arena health probe creates a lobby (`deploy/ship-host.sh:444` runs the bot's `create` action), so a scheduler cannot poll a host without touching lobby state; a read-only healthcheck action closes it.
- The bot's welcome line prints the host name, version and commit and drops the protocol the server reports in the same message (`crates/arena-server/examples/wsbot.rs:248`), so a host's arena protocol is proven from outside only by the handshake being accepted; printing it there makes it observable.
- `deploy/deploy-kings-online.sh` has no "is anyone mid-game" guard anywhere, so a redeploy drops every seated player.
- The public health check in `deploy/deploy-pong-online.sh:298` runs the probe from the workstation's checkout while the host runs its own pinned ref (`:233`), so a host whose protocol version differs never publishes; the script states the defect in its own comment at `:84`.
- `deploy/host.sh:489` now builds one published entry carrying every game's own address and protocol, but it is still one process and one tunnel per game, so the sole evergreen host that parses a legacy game selector (`crates/ember-server/src/connection.rs:409`) has no deploy path pointing several games at one address.
- `deploy/deploy-pong-online.sh:34` explains its port choice by an SELinux denial on a decommissioned machine while the script binds a different port at `:37`; the note is stale in a new way and wants rewriting, not reconciling.
- No supervisor restarts `host.sh` services on a pod without systemd: `deploy/host-tick.sh:9` does supervise servers and tunnels, but it is driven by a system timer (`deploy/install-host-timer.sh:2`).
- The off-host deployment watchdog is still driven from wherever it is invoked (`deploy/watchdog.sh:9` reads its host list from the environment); it belongs on durable automation independent of the workstation.
- The remote side of both ssh deploys assumes a build toolbox, a home-directory tunnel binary and `ss` (`deploy/deploy-pong-online.sh:233`, `:259`); `deploy/bootstrap-host.sh` prepares a fresh box for the `host.sh` path only.
- Four Kings is not registered in `games/hosted.toml`, which carries arena and fire versions only, and has no frozen hosted-contract crate of its own beside theirs under `games/`.
- One-server evergreen: nobody owns delisting approval, the player notice, publication of supported versions or the emergency security-removal path (`docs/one-server-evergreen.md:227`).
- One-server evergreen: every hosted entry carries a placeholder limits profile (`games/hosted.toml:1`), so Arena and Fire CPU, memory, frame, message, connection, lobby, outbound-byte and step-time limits must be measured and set before shared hosting.
- One-server evergreen: publication is still quick-tunnel based (`deploy/host.sh:349`) and no check proves a tunnel and proxy preserve the legacy game-selector query through the WebSocket upgrade (`docs/one-server-evergreen.md:229`).
- One-server evergreen: no hash manifest records the retained client artifacts beside each hosted version's frozen fixtures (`docs/one-server-evergreen.md:230`).
- The whole remote side of the two ssh deploys is exercised by PATH shims alone (`deploy/tests/shims/` holds ssh, scp, cargo, sleep, curl, git and wasm-bindgen plus stub servers and probes); the git side is real and nothing about the remote side is.
- Nothing tracked records a live run of `deploy/deploy-pong-online.sh`, `deploy/deploy-fire-online.sh`, `deploy/install-watchdog.sh` or `deploy/watchdog.sh` — including the fire script's port guard, toolbox build path and remote probe invocation; by design that proof lives in an untracked lane report rather than in the tree.
- needs: the Windows workstation to re-test the hidden detached launch at `deploy/wsl-detach.ps1:13` with the host process under watch, whose first launch coincided with a machine-wide wedge; the alternative is redirected stdio or a minimized window.
- needs: the Windows workstation to test the 5 ms socket read timeout (`crates/arena-server/src/lib.rs:357`) against tungstenite framing as the cause of the residual message loss; lengthening it costs latency and async I/O is the clean answer.
- The tee rule for logging a wsl run — a redirect from Git Bash interleaves at separate file offsets — is carried only by the kings deploy header (`deploy/deploy-kings-online.sh:48`), now the only script invoking wsl; what still lacks it is the build helper under `deploy/wsl/`.
- Installing the credentialed workstation's `deploy/republish-host.sh` on a scheduler is an operator action the repository deliberately does not perform; the script exists and is tested, and no schedule or key-copy step is installed anywhere.

## arena-simulation

- Client prediction and snapshot interpolation both pace from the measured render `dt` (`crates/arena/src/online.rs:3675`): there is no fixed-step accumulator and no held delay buffer, so interpolation degenerates to snap-to-latest at or below 60 fps.
- At render rates above 60 Hz the predicted HUD spread uses per-render displacement and can briefly narrow on frames carrying no movement tick, before authoritative spread arrives; derive its moving flag from the latest fixed-tick motion without marking a blocked player moving. Server accuracy is authoritative and unchanged.
- `Bullet.hit_mask` (`crates/arena-core/src/shooter.rs:2216`) keeps a leaver's bit for the rest of the round's flight time, so a same-id joiner cannot be hit; `remove_player` (`:2715`) purges players, owned bullets and rewind history and never touches it.
- The arena client connects before the window opens (`crates/arena/src/lib.rs:289` runs `connect` ahead of the engine at `:295`), so an unreachable server pauses launch about four seconds before the offline fallback.
- `PState` (`crates/arena-core/src/proto.rs:290`) carries no cosmetic event stamps, so a remote body plays no melee, no bonk, no hit reaction or death cue and never raises the gun it fires from; each is a defaulted field whose absence changes what an old peer sees, which is the join-gate question to answer before adding them.
- Cover boxes are axis-aligned in all four `base` rules: `crates/arena-core/src/shooter.rs:1161` clamps against the box minimum and maximum and `Obstacle` (`:1070`) carries no orientation, so rotated cover needs an oriented-box contract.
- `blocked_in` takes one scalar half-extent (`crates/arena-core/src/shooter.rs:1848`), so a non-square arena boundary needs a separate collision contract; nothing tests the absence.
- Block tops are perches because `support_height` (`crates/arena-core/src/shooter.rs:1151`) reads `base` and never `kind`; a support-skipping rule for loot boxes is a protocol bump, accepted rather than made blind (`docs/plans/arena-v18-freight-yard.md`).
- Teams balance only on join: `remove_player` (`crates/arena-core/src/shooter.rs:2715`) leaves the survivors where they are, and no test pins the resulting imbalance.
- King of the hill shows hill points where the scoreboard wants frags: the wire carries only the mode's score (`crates/arena/src/online.rs:2412`) though the server already counts frags per player; a defaulted `PState.frags` shows both without a bump.
- `LOOT_RESPAWN_SECS` is one number for every mode (`crates/arena-core/src/shooter.rs:383`, read unconditionally at `:3171`).
- A lobby plays one mode for its whole life: the mode is fixed at lobby creation and the client-to-server message set (`crates/arena-core/src/proto.rs:410`) carries no vote or rotation message.
- No team ping: a cosmetic client message the server rebroadcasts is the cheap form, and the message set (`crates/arena-core/src/proto.rs:410`) has no room for one today.
- No round penetrates thin cover — a shot stops on the first box face and the term occurs nowhere in `crates/arena-core/src/shooter.rs`.
- No wind and no drop table for the sniper beyond 120 m; neither term occurs in `crates/arena-core/src/shooter.rs`.
- Frozen `games/arena/v012/src/shooter.rs:48` ships a 0.30 head height while its own rationale at `:2823` and `:2846` argues from 0.22; reconcile the words without changing the frozen balance rule. The live copy in `crates/arena-core` was reconciled independently.
- `crates/arena-core` has no defaulted-fields decode sweep, though `crates/fire-core/src/proto.rs:352` and `crates/kings-core/src/proto.rs:760` each carry one; with more defaulted wire fields than either of them and the strictest rule in CLAUDE.md, a field added with a default and no test lands silently only here.
- The cross-host handling golden is compared at millimetre, milliradian and millisecond resolution (`crates/arena-core/src/shooter.rs:7641`), a tolerance decision recorded only in that comment and in `.github/workflows/ci.yml`: record in `docs/weapon-handling.md` what platform divergence it absorbs, and note that the Linux run of the re-enabled `free_for_all_handling_fingerprint_and_mode_equivalence` (`crates/arena-core/src/shooter.rs:9808`) is argued from the code and not yet observed green.
- `no_rng_state_lives_on_the_sim` (`crates/arena-core/src/shooter.rs:4405`) asserts that a literal source string occurs exactly twice in its own file, so it pins wording rather than state: a per-tick generator bound under any other name passes, and renaming a world generator's binding fails a test about RNG state.
- The rocket flight-time exit is reached only by `a_rocket_detonates_at_its_ttl_at_the_floor_and_at_the_wall` (`crates/arena-core/src/shooter.rs:8808`); at 120 to 300 m/s a rocket crosses the largest map well inside its five seconds, so no play reaches it.
- The wsbot's bonk hunt has no pathfinding (`crates/arena-server/examples/wsbot.rs:199`): a straight line to the target, a hop a second, and eight seconds before it gives up on a block.
- The authority-and-time law's wire clauses are unstarted: every timestamp, event-sequence, history-revision, acknowledgement and duration field is still tick-shaped (`crates/arena-core/src/proto.rs:372`, `:456`, `crates/fire-core/src/proto.rs:211`), and batching them into deliberate net, arena and fire bumps is the work.
- The authority-and-time law's rate clause is unstarted: `crates/arena-server/src/lib.rs:777` still charges a per-tick message counter, and no elapsed-time token bucket or bandwidth-and-QoS snapshot scheduler exists.
- The authority-and-time law's ordering clauses are unstarted: no timestamp validator, 12 ms reorder cohort, simultaneous-event reducer, event-keyed randomness or 300 ms revision-capable rewind exists; the only rewind is tick-indexed (`crates/arena-core/src/shooter.rs:968`).
- The authority-and-time law's world clause is unstarted: the exact arena, pad, track and body descriptions are neither versioned nor cooked, and the filter-then-widen predicate fixtures it asks for before changing shared world generation do not exist.
- Reflected rounds count against the reflector's bullet cap (`crates/arena-core/src/shooter.rs:962`) because `Bullet` (`:2187`) carries no caught-or-fired flag; recorded at the constant as a deliberate tradeoff.
- The shield is judged in the present, on this tick's flag and facing, while the body test stays lag-compensated (`crates/arena-core/src/shooter.rs:3511`), so a rewound shot can reflect off empty air; recorded there as a deliberate consequence rather than a defect.
- The seeded arena's hill is a placeholder square on the open floor (`crates/arena-core/src/shooter.rs:1403`), enough to drive the mode tests and not a place to fight over.
- The scutum's protected arc is the sim's 120-degree cone (`crates/arena-core/src/shooter.rs:35`, pinned at `:5564`), not the drawn board's outline; the cone is tested and its agreement with the board is not.

## arena-modes-realism

- The M4 is a weapon table row with no mesh (`crates/arena-core/src/shooter.rs:641`), so the loot pool excludes it (`:376`) and it draws a fallback (`crates/arena/src/ads.rs:18`); its source is in an unextracted archive kept out of git and `tools/v18/build_weapons.py` has no builder for it. The unblock is Windows-only.
- One hip hold offset serves every weapon (`crates/arena/src/online.rs:4501`, only the aimed point being per-weapon), so the RPG's tube reaches 0.78 m behind the grip and sits beside the eye.
- The camera bob is a time-based sine (`crates/arena/src/online.rs:3408`) while the leg cycle is distance-based, so two clocks drive one gait; drive the bob from the leg phase.
- A reflection plays the remote-shot cue and can raise a false hitmarker; the fix is a reflect variant on the sound enum, which has none today (`crates/arena/src/sound.rs:29`).
- One pair of footstep cues serves every surface (`crates/arena/src/sound.rs:112`: three walk and three run variants, no material dimension) though impacts already vary by material.
- The out-of-bounds boundary at the v25 terminal is geometry with no feedback channel — no cue exists in the feel or sound tables — and authored feedback should land before taller climbing or boarding routes are offered.
- The Tdm status line names my team first but cannot colour it: the status element is plain text by design (`crates/arena/src/online.rs:812`) with no team element beside it in the page.
- The Tab scoreboard does not say who holds the hill: `scoreboard_text` writes player, score and deaths rows only (`crates/arena/src/online.rs:2414`) while the king is produced in `mode_line` (`:2374`) for the status line.
- The sidearm's round is drawn as a 9x19 for every weapon that reaches the fallback arm (`crates/arena/src/rounds.rs:209`); a forty-five variant on the round enum and one arm in `round_for` closes it.
- The casing is still a box (`crates/arena/src/online.rs:4358`) and the rocket's blast mark shares the bullet hole's flat disc (`crates/arena/src/rounds.rs:431`) with no scorch or crater.
- The streak's base radius is 0.9 of the drawn heel with no floor (`crates/arena/src/rounds.rs:145`, applied at `:652`), which goes sub-pixel past 10 to 20 m; floor the radius or raise the lead once a capture shows it faint.
- The muzzle plume is four fixed-size balls and `WeaponFeel` (`crates/arena/src/feel.rs:37`) has no plume row, so the flash clearance constant (`crates/arena/src/online.rs:1120`) takes its floor from one shared reach.
- The sniper's bolt-cycle keyframe and the landing dip are not in the feel pass: `crates/arena/src/feel.rs:468` names a bolt part and nothing keyframes it, and the only camera dips are the bonk and reload ones.
- The hands are static posed gloves: no finger channel animates with the reload dip or the recoil (the dip moves the gun transform alone, `crates/arena/src/online.rs:1651`), and per-weapon magazine and reload interactions are unbuilt (`assets/models/swat-rig.json`, `assets/models/swat-parts.glb`).
- The sword hand is the rifle fist rotated onto the grip axis (`tools/v17/build_viewmodel.py:190`), so its posed right index is a trigger finger lying along the blade's grip; the authored gloves cover the guns only.
- `crates/arena/assets/viewmodel.glb` is 17.4 MB against the plan's 16 MB line, and every byte ships in the one bundle every web player downloads.
- `crates/arena/assets/sfx/` ships empty (a README alone) though the reader and its test are in place, so a recorded WAV replaces the synth with no code change (`crates/arena/src/sound.rs:1423`).
- Capture takes a read of the screen (`tools/v18/capture.ps1:313`), so anything dragged over the raised windows lands in the picture and a client's view can stop changing partway through a long run while its log proves it is still playing; off-screen capture is the real fix, and until then shots stay short.
- The captured rectangle is the whole window frame (`tools/v18/capture.ps1:192`, used at `:310`); crop to the client area with the client-rect calls, which are not imported.
- `tools/v18/capture.ps1:397` photographs client A and then client B inside the shot loop, so B lands 60 to 100 ms after the press and a remote muzzle flash cannot be caught; one pass grabbing both rectangles closes it.
- The drawn-weapon override is draw-only in both directions: the client documents it as the drawn weapon alone (`crates/arena/src/online.rs:376`) and the harness sets it without touching the sim weapon (`tools/v18/capture.ps1:108`, `:278`), so a capture fires the sidearm whatever the override says and no script can hand a chosen weapon to a player.
- `tools/v18/capture.ps1` has no switch for the round showcase (its parameter block at `:119` carries none) though the showcase exists and is unit-tested (`crates/arena/src/online.rs:1321`, test at `:7973`).
- `tools/v18/capture.ps1:174` parses two shot forms; the wall-clock seconds form runs on the harness clock and loses script time below 10 fps where the step form does not drift, so the wall-clock form should go once every recipe uses steps.
- A capture cannot tell from its own output that a connection dropped (`tools/v18/capture.ps1` reads the client status lines only); the honest check is the server log and the evidence not moving, and an earlier unexplained close a second after a shot was never re-run.
- A client's stdout reaches the harness through idle-only handlers, lagging about two seconds at startup; the clean fix is the client writing its own marker file, which `crates/arena/src/script.rs` does not do.
- The script language cannot aim at another player or a world point, only at an absolute or relative heading (`crates/arena/src/script.rs:221`), and cannot wait on a game fact, only on seconds (`crates/arena/src/script.rs:147`).
- `ShooterGame::with_chan` runs 122 lines under a suppressed line-count lint (`crates/arena/src/online.rs:1809`, the function from `:1810` to `:1931`); the cheap split into a script-run sub-struct is still unmade.
- Capture the flag is the natural fourth mode and a bigger design than the three that shipped (`crates/arena-core/src/shooter.rs:1303` enumerates them).
- There is no roof-only reward beyond the two blocks over the freight yard's C2, so the second storey has no loop of its own.
- The freight yard's train-zone alley is a 48 m level sightline as authored; cutting it needs a new container, and the contrast the geometry does deliver is pinned by its own test.
- needs: a live client with pointer lock and a second observer to judge the shield plate under real mouse-look and the sizes at several ranges.
- needs: a browser on a deployed bundle, plus an ear or an envelope plot, to see a tracer and hear a cue from the v20 page; nothing in the tree can settle it.
- needs: a capture run with a real rocket loot roll and a built web bundle to see the launch smoke, blast shards and every scope, shield and melee frame, none of which has been observed.
- needs: an eye-height capture in front of an authored facade to settle whether generated props face the direction the decor yaw claims.
- needs: a controller on the workstation to measure the script language's gamepad path, reasoned and unit-tested against a synthetic pad state and never driven by hardware.
- needs: human players to settle the freight yard's numbers, first its alley length, its sandbags and the loot respawn clock; the shipped behaviour is pinned by tests, not by play.
- needs: human players on both maps to settle the shared per-gun cone, recovery and zoom table.
- needs: eight human players to settle Harbor's spawn exits, rotations, quay angles, hill exposure and reward placement.

## games-other

- The editor's placement ignores the palette's base height (`crates/ember-editor/src/lib.rs:295` puts a part at half its own scale), so a placed roof lands on the floor although the roof entry sets a base (`crates/ember-editor/src/palette.rs:119`); the gap is named in a comment at `crates/ember-editor/src/level.rs:395` and no test pins it.
- The editor palette holds crate, container, pillar, plate, wall, roof and spawn only (`crates/ember-editor/src/palette.rs`), so ammo, sandbag, rubble, plinth and loot cover cannot be placed at all.
- `send_to` in fire-server returns a bool every caller ignores (`crates/fire-server/src/lib.rs:340`, stated in its own comment at `:348`), and the one-shot lobby events carry no sequence number (`crates/fire-core/src/proto.rs:199`, `:202`, `:215`), so a peer that misses one stays out of sync with no redelivery.
- The fire lobby has no chat, no kick and no way to change laps or car count without a redeploy: the server config is built from its default (`crates/fire-server/src/main.rs:70`).
- Kings v1 treats a lost socket as a lost seat during play and admits nobody unseated: no reconnect or spectator path exists in the server or the core, seat state being the only membership concept.
- Kings AFK escalation stops at elimination after three consecutive own-turn timeouts (`crates/kings-core/src/proto.rs:206`); no kick or per-player budget is designed beyond it.
- Kings promotion is automatic and always to Queen (`crates/kings-core/src/rules.rs:405`, pinned at `:839`) because the wire carries only a promoted flag (`crates/kings-core/src/proto.rs:231`); underpromotion needs a field.
- `Track` half-width is a single scalar (`crates/fire-core/src/track.rs:41`, read at `crates/fire-core/src/sim.rs:143`, `:196`, `:205`, `:463`), so kerbs, walls, off-track grip and the wall clamp are all constant-width; widening it is a protocol question.
- `Car::step` takes one surface-grip scalar (`crates/fire-core/src/car.rs:187`, applied at `:237`, `:244`, `:260`); a surface descriptor is a shared-sim change.
- Fire has no gamepad or touch handling anywhere in `crates/fire/src/`, and its page assumes a pointer; arena has since gained pad support, which makes this fire-only.
- Fire's handling numbers are argued from the model and checked by tests rather than tuned by feel; the grip, drift-grip and maximum-slip constants (`crates/fire-core/src/car.rs:43`, `:46`, `:65`) are where tuning starts.
- Fire's generated props are position-only, so `crates/fire/src/meshes.rs:71` fakes normals per face and projects planar UVs; the helpers moved into the engine (`:21`) rather than being removed, and a painted car would remove the fudge.
- Kings pieces jump between tiles though their ids are stable across promotion and awakening (`crates/kings-core/src/proto.rs:182`); `crates/kings/src/online_game.rs` carries no tween or slide state.
- The kings wasm canvas is a view only: no ray pick exists anywhere in `crates/kings/src/`, so the page's 2D grid stays the pointer input.
- Kings card variants and cross-player card trading are each a protocol bump: no trade message or per-piece rule-text variant exists in `crates/kings-core/src/proto.rs`.
- The kings pawn-capture set and the joker step are playtest knobs to be settled by playing (`crates/kings-core/src/rules.rs:19`, `:28`, read at `:174` and `:198`).
- The fire netcode has only been exercised on loopback, so the 25 m convergence bound in `crates/fire/tests/online_e2e.rs:207` is meaningless until it runs across real latency; no latency-injecting harness exists.
- The kings wasm client processes incoming messages only on animation frames (`crates/kings/src/net.rs:14`, `:163`, echoed at `crates/kings/src/online_game.rs:80`), so a hidden tab keeps its seat and sees the board only when it is shown again; deliberate as recorded.
- what-is-this emits accepted reports through the host's structured log stream and gains a durable report sink only when a second hosted consumer needs one; a deliberate deferral awaiting a trigger that has not occurred.
- needs: the Windows workstation and hundreds of native runs to reproduce the residual loss of a single post-connection message in the fire e2e suite (about one run in 45 to 60, surfacing as four symptoms and one bug); every application-level drop path is instrumented and was silent across about 250 runs, so the loss is below the application layer, Windows-only and tungstenite-only.
- needs: the Windows workstation to reproduce the fire e2e flake caused by four tests sharing a process and racing the scheduler (`crates/fire/tests/online_e2e.rs`); the suggested single-threaded mitigation was never applied, so the structure that produced it is unchanged.

## julibrot-math-kernels

- `forward_homography` maps the plane basis with a camera row against the ambient basis (`crates/labs/julibrot/math/src/screen.rs:86`), the transpose of what `apply_camera_rotation` applies (`crates/labs/julibrot/math/src/reprojection.rs:286`), and the fail-safe reconstruction absorbs it; the recorded round trip reaches 6.4e-7 px and never builds the flat-object configuration the 3.96 px claim names, so reproduce that figure there or strike it. needs: a browser check at a nonzero ambient camera rotation.
- The kernel's horizon test is the denominator's sign alone (`crates/labs/julibrot/math/src/screen.rs`), which misses the band where two sign flips cancel and the flat map and the relief pass disagree; no second horizon test exists on the 2D path and the decision is unrecorded.
- The Interactive refinement level is built unconditionally for both precision modes (`crates/labs/julibrot/kernels/src/refinement.rs:129`) and still ranked by the scheduler (`crates/labs/julibrot/app/src/frame/schedule.rs:448`), though the fast picture policy asks to omit it.
- `nonzero_z_zero_rebase_uses_the_correct_delta` (`crates/labs/julibrot/kernels/src/perturb.rs:675`) loops over every precision mode but never passes the mode to the function under test, so every iteration computes the same sample, and its opening assertion cannot separate a correct delta from a wrong one; the two neighbouring loops do pass it.
- `DETERMINISTIC_VERIFICATION_DIGITS` is kernels-owned (`crates/labs/julibrot/kernels/src/lib.rs:4`) and consumed by the app's conformance test; move it into math and replace math's duplicate orbit literal.
- `origins_share_slice` is defined twice, in the app (`crates/labs/julibrot/app/src/state.rs:1539`) and in present (`crates/labs/julibrot/present/src/state.rs:274`); promote it into math and make app and present agree which pose supplies zoom and width (`docs/julibrot/app.md:444`).
- The final exact-versus-fast corpus covers the deep paired arm alone (`crates/labs/julibrot/app/tests/final_conformance.rs`): the cross-product across zooms, caps, planes, palettes, rescale and rebase — with exact terminals, at most one code of colour error, and deep cases forcing both rescale directions — is unbuilt, the shared-by-contract boundary being what is retained instead (`docs/julibrot/kernels.md`).
- The observation oracle asserts its tightness bound on single-record rows only (`crates/labs/julibrot/math/src/perturb.rs`); extending it across multiple recurrence steps is what makes the later-escape and rebase row carry a bound.
- No wasm32-wasip1 test lane exists, so the codec's word-size parity is source inspection rather than a test (`docs/julibrot/precision-ledger.md:7`).
- The five-dimensional near clamp is a projection-space guard, not a model-space clipping distance (`docs/julibrot/present.md:95`), and replacing the closed-surface safeguard with a true model-space distance belongs to the study named at `docs/julibrot/present.md:600`.
- No mesh-side sensitivity sweep exists for coverage share and candidate monotonicity (`crates/labs/julibrot/math/src/footprint.rs` and its tests), which is what would justify replacing the reviewed discrete apron policy.
- Deterministic zoom-14 pixel sampling has no bignum ground truth to compare against and no accumulated-cancellation detector: the shipped single-step test is retained in `crates/labs/julibrot/kernels/src/perturb.rs`, Zhuoran-style rebasing is undesigned, and one sampled pixel still reports a sampled cap. needs: a server lane for the measurement half.
- needs: a GPU-completion timing path to measure the 16-byte zero-padded texel load against a two-points-per-texel accessor; none exists in the native lane (`crates/labs/heap/src/executor.rs`), and the heap address arithmetic should not change before it does.
- needs: both browser backends to validate the valid-rows span upload with a non-page-aligned orbit and prove no consumer reads the residue; the padded-pages default (`crates/labs/heap/src/executor.rs:635`) is what makes waiting safe.

## julibrot-present

- Present GPU tests can deadlock intermittently under parallel execution on the software Vulkan driver; serialize this shard until shared device and lock isolation is corrected. At c95fba62 the crate's test binary sat 43 minutes at 0.4% CPU with four gpu::device::tests threads blocked on futex and rt_mutex beside lavapipe threads, while the serialized run passes in 46 s.
- No check relates a scene's submitted extent to the pose it is submitted against: the pose comes from the hot slot and the extent from the grid in `crates/labs/julibrot/present/src/gpu/device/scene/submit.rs`, and the two validators check each alone.
- The scale invariant is skipped whenever a plan declares exposure (`crates/labs/julibrot/present/src/gpu/device/ledger.rs:221`, the flag decided by `crates/labs/julibrot/present/src/planner.rs:305`), so any pan past half a pose pixel leaves the coverage check; the decision belongs in `docs/julibrot/present.md` and is recorded nowhere.
- `record_height` returns the neutral 0 for glitch, horizon, malformed and non-finite records (`crates/labs/julibrot/present/src/shader.rs:51`, mirrored at `crates/labs/julibrot/present/src/mesh.rs:145`) — the middle of the height range, not its floor, where the interior returns the bottom; the decision belongs in `docs/julibrot/present.md` and is recorded nowhere.
- `project_vertex` clamps the fifth coordinate and keeps projecting (`crates/labs/julibrot/math/src/footprint.rs:418`) while counting the vertex as projected (`:293`), so the census counts a vertex the shader refuses; only the drawn rule applies the shader's test (`:333`).
- The zero-lift shortcut in `project_scene_vertex` returns a clip w of 1.0 where the chain returns the pose's own (`crates/labs/julibrot/present/src/planner.rs:689` against `:665`), and the record variant takes the same shortcut at `:571`.
- The warp corpus abandons the whole measurement when either pose refuses one sampled height (`crates/labs/julibrot/present/src/planner.rs:411` propagates the refusal out of the height loop); the two-direction corpus that would replace the fail-closed choice does not exist.
- The units repair in the exposed-fraction test is a side effect of another change and nothing pins it: the mapped point is tested against texture units (`crates/labs/julibrot/present/src/gpu/device/ledger.rs:65`) and both call sites use a held frame whose extent matches its pose, so neither separates the unit systems.
- `an_in_set_vertex_is_placed_by_the_chain_like_any_other_record` ends on an assertion that cannot fail (`crates/labs/julibrot/present/tests/steep_row.rs:426`): both placements reduce to the same height, so one call is compared with itself.
- The frame oracle's differing-pixel and first-row numbers are properties of its own record field rather than of the delivered frame, as its own doc comment says (`crates/labs/julibrot/present/tests/steep_frame.rs:811`, asserted at `:838`); driving it from the delivered escape grid is what would make them transfer.
- The frame oracle keeps the shader's operation order under a lint reason that contradicts the file's 25 uses of a fused multiply-add (`crates/labs/julibrot/present/tests/steep_frame.rs:9`), and whether it should narrow to binary32 and clip is undecided — its depth clears to infinity (`:535`) where the GPU clears to one.
- The frame oracle models the scene pass alone (`crates/labs/julibrot/present/tests/steep_frame.rs`), so it clears 25,393 pixels the served frame paints from the backdrop grid; a composed-backdrop oracle does not exist.
- The dynamic-offset census names six files and asserts an exact count (`crates/labs/julibrot/present/src/gpu/device/tests.rs:848`), so readback, ledger, poll and uniforms obey the law without being checked by it, and a new file in the module joins them.
- `Warp::reproject` takes a validation argument and a precision mode that are unread through the whole function (`crates/labs/julibrot/present/src/planner.rs:45`); remove them or make them affect planning again.
- The scene pass's clear-colour call site is unpinned: the site is `crates/labs/julibrot/present/src/gpu/device/scene.rs:249` and the only test naming the function exercises the function alone (`crates/labs/julibrot/present/src/gpu/device/tests.rs:506`).
- The quarter-pixel inverse-row invariant is proven at neutral view controls only (`crates/labs/julibrot/present/src/planner.rs:989`, both poses built from the neutral controls), so rotated and translated height-zero poses are untested.
- `texture_reallocations` increments on each changed scene extent with no policy ceiling (`crates/labs/julibrot/present/src/gpu/device/scene/submit.rs:116`).
- The tile vocabulary is split between kernels and present across eleven names — residency and quality defined twice (`crates/labs/julibrot/kernels/src/tile_job.rs:345`, `:352` against `crates/labs/julibrot/present/src/tile.rs:368`, `:403`), the rung and the descriptor sample pair present-only, the source screen rectangle kernels-only — and centralizing them in kernels means recording the decisions in `docs/julibrot/tiled-reprojection.md`.
- Tiled reprojection stage 1 is unstarted: no depth-bearing retained view exists in `crates/labs/julibrot/present/src/state.rs`, and direct target-depth reconstruction must be proven before multi-tile composition.
- The generalized relief-redraw admission floor is calibrated from one 960 by 540 measured relief zoom row (7.1952% conservative risk at `+0.1`, 5.3596% at `+0.5`); a broader native corpus across pans, zoom-out, camera cross terms and other saved rows is needed before replacing the fixed 7.1952% risk with a pose-specific predictor (`crates/labs/julibrot/present/src/planner.rs`).
- A records apron at final resolution with an explicit budget clamp remains an unexplored alternative to the coarse backdrop that ships (`crates/labs/julibrot/app/src/frame/loop/browser/backdrop.rs:64`; decision doc `docs/julibrot/present.md:267`).
- Whether a reachable saturated-relief state should keep presenting the flat floor chart, refuse, or publish and visualize the saturation is a study question (`docs/julibrot/present.md:600`), derived at `docs/julibrot/present.md:95`.
- The value-to-colour shade source and its uniform lane are pinned natively, but a browser readback has not yet confirmed the served zoomed row's interior pixels after the palette moved out of the scene pass.
- The existing `warp_wall_ms` fence wall now measures value reprojection plus the fullscreen shade pass and browser scheduling together; isolating shade-only GPU cost would require a supported timestamp-query measurement that the WebGL2 floor does not expose.
- A relief redraw leaves the destination-horizon ring at Clear because horizon vertices are rejected before rasterization and therefore cannot write a status; matching the Final's Horizon without placing a false slab requires a fragment-producing destination-status path beyond the one mesh draw, measured as 17,556 pixels (3.39%) on the measured relief zoom row (`crates/labs/julibrot/present/src/shader.rs`, `crates/labs/julibrot/present/src/gpu/device/redraw.rs`).
- needs: a device capture of a partly-refused primitive to measure the hairline a refused vertex leaves along its edge, before the mirror or the shader's discard threshold moves (`crates/labs/julibrot/present/src/shader.rs`).
- needs: a browser census of a slower morph, recording the warp kind and the held frame's extent per step; the anchor path's extent correctness is confirmed in code (`crates/labs/julibrot/present/src/planner.rs:247`) and the hold path is `crates/labs/julibrot/app/src/frame/schedule.rs:676`.
- needs: a reproduction on a device for the diagonal black streak artefacts; the candidate sites are the fixed cell diagonal (`crates/labs/julibrot/present/src/mesh.rs`) and the screen-derivative normals (`crates/labs/julibrot/present/src/shader.rs`).

## julibrot-app-web

- R2-01b now asserts the refresh submission order: both drivers run one ordered refresh executor whose typed stages the scene and warp effects consume. The browser-mode `BROWSER_ACTION_TRACE_FIXTURE` asserts scene-before-warp and fence-observation order per turn for the short, zoom, height, capture, and finished-picture scenarios. The R2-01b frame trace is byte-unchanged, so R2-04a–c may start.
- The browser-model fixture emits one `HotWritten` per turn while production keeps three conditional `presenter.write_hot` sites around the arrival and accepted-warp branches; browser-model cases covering those branches and freezing every conditional `HotWritten { slot_order }` action are follow-up work.
- R2-02 must reconcile or deliberately preserve the worker transport divergence: the same logical ownership scenario dispatches and returns request slot 0 on the same-thread queue versus slot 1 on the browser stack, and same-thread credit return advances the final fact epoch to 7 versus 6 in the browser owner core.
- R2-04d is the scheduled home for a poll-only browser boundary; until then the served whole-grid oracle reports `owner_epoch`, `presentation_ledger`, `capture_count`, `scene_polls`, `warp_polls`, and `second_frame_policy` as load-dependent evidence because `app_refresh()` both polls GPU fences and runs presentation turns, and the second-frame policy is selected from the timing-derived PolicyProbe wall.
- The integrated `Cargo.lock` change is the workspace-only `pollster.workspace = true` dev-dependency edge for the present crate's native palette tests; it adds no registry crate.
- A clear-only present stamps the requested view as presented (`crates/labs/julibrot/app/src/frame/loop.rs:466`), so the finished-picture reading can be true over a blank surface; the companion restart rule is settled and this stamp is not.
- `retire_main_grid_pair` frees a span and leaves the field naming it (`crates/labs/julibrot/app/src/frame/loop/browser/submit.rs:270` against the bare field at `crates/labs/julibrot/app/src/frame/loop.rs:745`); an optional grid would make the recovery invariant structural.
- The backdrop replan's early return keys on requested extent, cap and precision mode (`crates/labs/julibrot/app/src/frame/loop/browser/backdrop.rs:72`), so a replan is skipped when the executor would derive a different delivered extent.
- A screen pan becomes inexact once the surface is lifted — 1.67 px error at height 0.6 with camera yaw 0.25, above the one-pixel bar — because the navigation delta is taken at a literal zero height (`crates/labs/julibrot/app/src/state.rs:603`).
- A corner box zoom of `+1.95` log2 whose destination lies partly outside the source settles in about 12 s against 2.6 s for a centred box in the same tab because the coverage-first backdrop render precedes the redraw; the tile design's demand-ordered scheduling is where that gets cheaper.
- The delivered precision reading is sourced from the main state (`crates/labs/julibrot/app/src/facts.rs:281`), which carries no delivered value until the next orbit lands, so it reads unavailable for a turn after a centre revision while zoom and depth stay populated, indistinguishable from a value never asked for.
- The rebase count fields are hard-coded absent strings (`crates/labs/julibrot/app/src/facts.rs:330`), so no census can read them.
- The five newer present facts — the destination and warp source extents and the lattice refusal (`crates/labs/julibrot/present/src/contract.rs:454`) — are forwarded by no app fact, so a browser census cannot read the presented scale.
- The held-frame partition and held-since scene id exist in present (`crates/labs/julibrot/present/src/contract.rs:439`, `:441`) and are forwarded nowhere in `crates/labs/julibrot/app/src/facts.rs`, which also does not label the delivered fields as held.
- The timing ledger publishes a null dispatch wall unconditionally (`crates/labs/julibrot/app/src/timing.rs:137`) and carries no transfer, packing or upload wall at all, and the scene wall it reports is the fence observation wall, which reads like a poll-cadence cap rather than GPU work; publishing the reference-request count and the discarded scene ids belongs to the same change.
- Credit admission time cannot be separated from the observed round trip because neither frozen request nor response layout carries an admission timestamp (`crates/labs/julibrot/worker/src/codec.rs:19`), so the ledger can only forward what a worker sample happens to carry (`crates/labs/julibrot/app/src/timing.rs:144`).
- The frozen orbit response carries a length but no escaped flag (`crates/labs/julibrot/worker/src/wire.rs` and its codec), so a short escaping orbit cannot prove it is complete; the recent work addressed the symptom in the app's discard handling and left the wire untouched.
- A level whose cap outlasts the accepted orbit asks for a longer reference every time (`crates/labs/julibrot/app/src/frame/loop.rs:112` gates on cap against accepted length and the outstanding request only); a cheap candidate escape-count pre-test would refuse it before the transfer.
- Nothing carries a census candidate across a centre or origin move (`crates/labs/julibrot/app/src/frame/loop.rs:395`; the request count is per accepted navigation at `crates/labs/julibrot/app/src/facts.rs:124`), so a deep view pays for its corrections again after navigating.
- The census correction's request is a navigation with a zero delta, spending a generation and a centre revision the picture does not need; the retain-across-discard workaround states it verbatim (`crates/labs/julibrot/app/src/frame/loop/browser/reference.rs:160`) and the class fix is unmade.
- Regional second-reference glitch correction is deferred whole: cluster selection, one extra worker orbit per cluster, the masked rerender, the merge by ownership and the two counts it would publish occur nowhere in the tree.
- Settled deterministic promotion is built and switched off (`crates/labs/julibrot/app/src/frame/schedule.rs:64`, the state machine at `:111`): staging the effective mode, driving it from the browser frame loop, one effective-mode source, forwarding held facts, publishing a presented precision tier in place of the completed-scene policy still at `crates/labs/julibrot/app/src/facts.rs:232`, and showing the tier are what activation needs (`docs/julibrot/app.md:446`).
- The presenter poll still returns an allocating vector (`crates/labs/julibrot/app/src/frame/schedule.rs:280`, implemented at `:850`) though the two-slot fixed poll exists (`crates/labs/julibrot/present/src/gpu/device/poll.rs:22`).
- The worker header write zeroes every mutable byte for a non-orbit header (`crates/labs/julibrot/worker/src/browser.rs:431`) though a canonical request initializes at most 628, and with a large-cap transport that becomes an always-on clearing cost — about 7.9 times — on ordinary small returns; tracking the initialized extent is the fix.
- A large-cap orbit is written through tens of thousands of individual byte stores (`crates/labs/julibrot/worker/src/browser.rs:983`, called once per record from `:406`).
- A boot crosshair makes the crosshair plane recompute a big-precision displacement and two centre clones every refresh (`crates/labs/julibrot/app/src/state.rs:667`, driven from `web/labs/julibrot/main.js:227`, target set at `crates/labs/julibrot/app/src/state.rs:522`); cache it per accepted reference. needs: a browser profile for the cost itself.
- `lab.set(field, value)` parses the whole saved-view row to rebuild a grouped setter (`web/labs/julibrot/lab.js:170`), one round trip per control move, because no per-scalar setter exists.
- The page reads facts by JSON name (`web/labs/julibrot/lab.js:350`, `:418`), so a rename becomes a timeout rather than a compile error; the Rust-side field list (`crates/labs/julibrot/app/tests/page_contract.rs:32`) and the page's pinned predicate string (`:1071`) are not tied together, and the frame-capture names sit outside the list.
- The page contract is 1,405 lines and 263 text-containment assertions over 20 source and page files, two of them in another crate (`crates/labs/julibrot/app/tests/page_contract.rs:1`); it passes on text presence and reds on any rename, standing in for browser facts that need visible replay.
- The maximum page iteration cap is a constant the page contract never names (`crates/labs/julibrot/app/src/frame/loop.rs:47`); pin it against the largest cap option the page offers.
- The surface horizon share is published nowhere: `crates/labs/julibrot/app/src/facts.rs:181` publishes the uncovered share alone, and carrying the horizon share from the scene footprint through the page contract is the work.
- The page publishes a mesh fact alone, so a quarter-interior frame reports a small uncovered fraction (`crates/labs/julibrot/app/src/facts.rs:182`); whether a dark share or a refused share belongs on the page is the app's call, undecided and unrecorded in `docs/julibrot/app.md`.
- The drive page counts its census in JavaScript on the main thread (`web/labs/julibrot/drive.html:33`, four totals at `:91`); whether that belongs in the app as a wasm entry is undecided and unrecorded in `docs/julibrot/app.md`.
- A hidden tab runs on the 250 ms fallback floor (`web/labs/julibrot/lab.js:38`, waking only when the document is visible at `:122`) and no timeout adjusts for a hidden document; whether the module raises its own default or publishes turns per second is undecided and unrecorded in `docs/julibrot/app.md`.
- The arm guard and the two capture encodes are pinned by a truth table and source-text ordering assertions (`crates/labs/julibrot/app/tests/page_contract.rs:1322`, `:1174`), and the finished-picture predicate by a truth table over its own booleans (`:1224`); a fake presenter now exists (`crates/labs/julibrot/app/src/frame/loop/tests.rs:703`) and nothing drives an armed copy or the predicate through it.
- The wasm transfer bridge re-implements the codec's request layout (`crates/labs/julibrot/worker/src/browser.rs:17`) so every layout change must land in both; the hazard is pinned by a byte-identity test (`crates/labs/julibrot/worker/src/codec.rs:686`) and not removed.
- The worker's plane axis constants are pinned to the Mandelbrot seed (`crates/labs/julibrot/worker/src/owner.rs:49`, layout-pinned at `:685`) while the app writes only the seed's axes (`crates/labs/julibrot/app/src/state.rs:482`); retiring them is a byte-layout revision waiting for a worker lane.
- needs: a browser run on the served hub and on live Pages to fill the timing ledger's live values for preview, interactive and final at 960x540, and to confirm the corrected callback and step walls after the fact-key and bundle-helper repairs; the harness and the repairs are in history and no live figure has replaced the placeholder observations.
- needs: a browser run on both origins for the present-side rows — the per-scenario clear-only counts, a suspended-tab warp-fence deadline, device loss and the relief diagonal streak — retaining reports and receipts.
- needs: a browser replay of a deep reference to decide whether the bounded admission price wants a coarser budget policy; a reference that reliably costs several seconds still spends most of its wall waiting a second at a time.
- needs: a browser measurement of the saving a viewer actually sees from the shallow-refinement bypass, which is in the code and measured natively alone (`docs/julibrot/audit-compute.md:21`).
- needs: a browser to count scene ids over settled seconds in the exact edge-on state, which resubmits scenes indefinitely; the state is tracked (`crates/labs/julibrot/app/src/frame/loop.rs:239`, set at `crates/labs/julibrot/app/src/frame/loop/browser/submit.rs:93`) and no guard stops the resubmission.
- needs: a browser restore from a stored view above four to prove the height-scale refusal surfaces as a typed refusal rather than wedging the control loop; the refusal is in the app validation path and no native test exercises the page side.
- needs: a served-build census or a native two-draw GPU test to exercise the offscreen frame copy (`crates/labs/julibrot/present/src/gpu/device/readback.rs`), proven structurally and never run on a device.

## docs-process

- `docs/authority-and-time.md:11` and 25 further lines cite crates renamed the day after the document was last touched; the rename updated the other documents and skipped this one, so every one of those citations resolves to nothing.
- `docs/one-server-evergreen.md:12` states a protocol number and a line that are both wrong, `:17` cites a deploy script deleted when the cube demo surface was retired, and `:226` still poses as open the recovery and admission of the historical arena versions, which have since landed as contract crates with fixtures.
- `docs/input-latch.md:161` repeats a claim the arena client's own header has since corrected: the client aims with relative mouse deltas under pointer lock and unprojects no cursor.
- `docs/plans/milestone-1.md:215` and three further lines cite an engine scene-frame module, a presenter module, a flycam crate and a warp test, none of which exists; the plan is closed, so the fix is a note that its citations are historical.
- `docs/julibrot/tiled-reprojection.md:31` and `docs/gpu-heap-lattice.md:23` state normative claims by citing files that exist only on other branches, so a reader of the main line cannot check the boundary or the ladder's fixed steps against anything in the tree.
- `assets/models/` holds the SWAT operator model, its parts and its rig with no license, README or provenance file of any kind; record the author's source and license beside the asset and its v23 derivatives.
- Eight labelled open TODO notices live in the vendored linter (`tools/linter/src/assembly.rs:147`, `:158`, `:468`, `tools/linter/src/carrier.rs:147`, `tools/linter/src/claim.rs:304`, `tools/linter/src/comment.rs:124`, `tools/linter/src/coverage.rs:65`, `tools/linter/src/graph.rs:95`); a local fix is lost at the next re-vendor, so they route upstream rather than being edited here.
- Two lint attributes carried in vendored code (`tools/cli-common/src/publication.rs:75`, `:159`) are unupstreamed and lost at the next re-vendor.
- Nothing in the tree records where `tools/linter` is vendored from or how it is re-imported, so a local patch over it is lost silently at the next re-vendor; record the upstream and the re-vendor step beside the code, since every commit touching it so far is a local one.
- `tools/make_hands.py` remains the hand geometry implementation imported at `tools/make_assets.py:60` and called at `:163`; its six unfitted constants need measurement when the helper next runs.
- `cargo test --workspace` stops at the first failing binary, so one tripped flake reads like a mass regression; the guidance is to run it without fail-fast and report the suite count, whose landmark figure must be re-measured against the workspace's current member list rather than the eleven members it was written for.
- The browser frame loop is compiled only for wasm (`crates/labs/julibrot/app/src/frame/loop.rs:719`, re-exported at `:1610`), so a native check compiles none of it; the wasm check belongs beside the native one in lane order.
- `docs/julibrot/app.md` should carry the recorded property that the frame-loop fallback is a floor and not a rate, so a backgrounded tab completes at roughly 1 Hz; it is a documented trade with no code change asked for and does not belong in this file.
- `docs/weapon-handling.md` should carry the recorded property that the footstep cue cadence is floored per gait against a 6 rad/m leg cycle (`crates/arena/src/feel.rs:1556`, `:1564`, selected at `:1570`), so cues plant about eight a second walking and twelve sprinting; a slower leg cycle is a separate animation change.
- needs: a build server to run a Windows-target workspace check with all targets and enumerate the residual errors under the denied-warnings law; the commit that closed the underlying breakage records the same debt in its own message.

## UltimateLegue

- League scene: an attack order has no target mark in the world (the sim does not put `Unit.target` on the wire); a red ring under the unit my champion is chasing would tell a player their click landed.
- League scene: the camera follows the own champion only; edge-pan and a space-to-recentre toggle are the MOBA norm and both need `world.cam` (client) rather than `scene.rs`.
- League review harness: `tools/league/review.ps1` photographs the SCREEN like the arena harness, so a window dragged over the client lands in the picture; an off-screen capture written by the client itself would need no screen space.
- League review harness: the human seat idles at its fountain in a practice run because the harness sends no input by rule; a `LEAGUE_BOT_ME=1` that hands the own seat to the bot brain would put the review camera in a real 3v3 instead of a 3v2 plus a bystander.
