# Arena v29: vertical Breakwater Harbor and parkour

## Recovery attempt — 2026-09-06 16:14 UTC

User renewed the request to publish. Main/ci-passed remains129bcac4 and Pages3dc66e0a. Root temporarily checked out the exact clean runtime revision and rebuilt in9.237s: WASM remains0200c883d7f228221b91346740f7985527082abbdd3f6aa735f90823058a80ae; stamped Windows Arena executable SHA256 is `f228e5d96c8b0674a041673e4be4569e1827b9b5f9f617c2a5c493f6c5a0fe63`. After over35minutes of cooldown, the resumed natural Knecht tick at16:08:41 again encountered all-three-tunnel HTTP429/code1015 at16:08:52. Root paused only the timer at16:09:45. Local game processes survive, but no public rollout occurred. Do not make further rapid tunnel-creation attempts.

An asynchronous user approval request is pending for Windows Arena-only deployment behind its existing tunnel; no answer had arrived when recording this checkpoint. At16:11UTC that server reported one player/one lobby. ArenaPID32676 and tunnelPID4072 still match their prior creation times; no Windows process, task, binary, durable checkout, stamp or address-book entry has been changed. Old executable SHA256 is `db89418494659961a950ce78911c73583736b5f977d12acae57a8cf0297b97cc`. The durable checkout is a clean ancestor of129bcac4, so a normal fast-forward is possible after approval. New task nameember-arena-server-v29 and proposed durable launcher path were verified absent.

Prepared `tools/v29/server-only-windows.ps1` for the fallback. It is specific to the already-audited durable Arena path/port/host, refuses wrong binary SHA256 or any existing7780listener, launches hidden/Idle with separate logs and waits for the child. It never stops processes, creates tunnels, registers tasks or publishes. Only offline Windows PowerShell syntax is checked so far; the launcher has NOT been run. Suggested task: separateember-arena-server-v29, same Interactive/Limited user, no trigger, IgnoreNew, Priority10, ExecutionTimeLimitZero, battery start allowed/no battery stop; launch explicitly once after approved maintenance. Do not override execution policy: LocalMachineRemoteSigned already permits this locally authored script. Back up the old executable before replacement; prove exact server/tunnel identities and source/artifact stamps, start through durable Task Scheduler ownership, then verify loopback/public Welcome and unchanged tunnel lifetime. Source main remains the release pin; this utility/handoff stays on the development branch until deployment is verified.

## Release checkpoint — 2026-09-06 15:34 UTC

All v29 code and the updater/publisher repairs are on main and ci-passed at `129bcac43d0968613aa5359b802f01c04f98b28b` (`r1490`). Main CI [34041967229](https://github.com/EndersGamesDev/EmberEngine/actions/runs/34041967229) passed both jobs and automatic promotion at15:21:46UTC; its deployment suite includes23/23 host-timer checks and real Linux flock coverage. The user's screenshot shows the historical failed `b97b87bf` run34039530509, not this release. Its cross-platform movement fingerprint and shell-heredoc test extraction failures are resolved; the handling test now runs unskipped. Node20 checkout-action notices are warnings, not those failures.

The clean129bcac4 server/WASM rebuild took5.102s. WASM remained byte-identical to the fully tested final gameplay/art candidate:43,202,283bytes, SHA256 `0200c883d7f228221b91346740f7985527082abbdd3f6aa735f90823058a80ae`. The actual-page browser gate passed43checks again in8.632s, and six isolated publisher discovery/preservation tests passed in0.067s. The eight-peer gate also ran against Knecht's actual Linux129bcac/r1490 server through a disposable owned SSH forward: FFA/TDM/Hill,4v4/rejoin, protocol/capacity rejection, handling, slide and uninterrupted opposing-wall kicks all passed in23.347s,5,290states, no errors. Forward startup/cleanup included:25.079s. It left no public rooms or forwarding process behind.

The user-approved Knecht repair was installed in2s at15:22:22UTC: old timer/service stopped under the old control-group policy, clean managed checkout advanced to exact green ci-passed, then the fixed installer enabled the natural timer. No manual host.sh up was invoked. Automatic build/local health passed. All three game servers survived successful oneshot exit for over7minutes with unchanged PID/start-time identities and no inherited tick-lock descriptor, proving the core lifecycle repair. Full six-child/next-quiet-tick proof is NOT complete because public tunnels failed to start.

PUBLICATION IS BLOCKED by Cloudflare quick-tunnel HTTP429/code1015, not CI or game tests. Existing logs record failures at15:18:40,15:22:45 and15:33:17UTC; no Retry-After duration was recorded. Root paused only the timer at15:23:29, waited over10minutes after the last rejection, then resumed it at15:33:06 for one natural retry. Local probes passed again, but all public tunnel creation still failed. The timer was paused again at15:34:19 to avoid extending the rate limit; do not repeatedly restart it or mint replacement tunnels. Game servers remain running locally, source129bcac4; the run/deployed marker remainsfa0bfcb5 because the public-health/publish stage has not succeeded. Current retained game PIDs/start stamps: Arena1547256/18577041, Fire1547272/18577145, Kings1547288/18577248. Recheck identities before any later operation. Cloudflare's documented1015 guidance is to wait rather than repeatedly retry: https://developers.cloudflare.com/support/troubleshooting/http-status-codes/cloudflare-1xxx-errors/error-1015/ .

Pages is deliberately unchanged at `3dc66e0ae3b3a625b5afa49151c59a8d92c650a5`; v29 is NOT publicly published. Frozen v28 and all peer releases are untouched. At15:34UTC the existing Windows dusky-osprey public Arena answered protocol21/r1469/f28a145f with0players/0lobbies in0.187s. Root did not change its processes, scheduled task, durable checkout or tunnel. The handoff documentation is committed on the development branch only, leaving main/ci-passed at the exact tested runtime revision; publish from a clean checkout of that main revision, not from this later documentation HEAD.

Next decision: ask the user whether to update only Windows Arena behind its already-running public tunnel if Knecht stays rate-limited. This is a server-location change, not yet authorized. Read-only audit found ArenaPID32676 (created2026-09-06T13:13:26.623227Z), listener127.0.0.1:7780, binary `C:/Users/end/dev/ember-environment/target/release/arena-server.exe`; tunnelPID4072 (created13:13:36.370658Z) forwards that port at `wss://python-firefox-marine-atmospheric.trycloudflare.com`. The durable checkout is cleanf28a145f, and scheduled taskember-arena-host is logon-only. Existing Windows deployment commands restart/mint the tunnel too: do NOT call their up/down/install path for a server-only fallback. If authorized, first recheck occupancy and exact identities, implement/use a durable owned hidden/Idle server-only launch, preserve the tunnel, prove actual protocol22/version/commit through that unchanged address, and update only Arena metadata through the existing host-book writer without downgrading FireV2. No alternate identity, IP or credentials should be used to evade the provider limit.

After a publicly reachable exact129bcac4/r1490 protocol22 host exists, run the eight-path publisher prepare and explicit push with the recorded clean build SHA/WASM hash, then public-release.cjs. Require the actual publication baseline, peer/frozen byte preservation, public Welcome and successful Pages deployment. For Knecht, resume the fixed timer only after cooldown/recovery and prove all six child identities survive exit and a later quiet tick before claiming full automatic-update health. No recurring follow-up automation has been created.

## Rollout approval (supersedes historical hold below)

The user explicitly approved proceeding after the final confirmation named automatic Arena, Fire and Kings restarts. Publication is now authorized. A fresh deployment check found the pre-existing Knecht oneshot killing all six server/tunnel children immediately after successful publication, and an inherited lock descriptor that would prevent later ticks once those children survive. The release includes the bounded installer/tick repair and its isolated tests. Pages publication now changes eight paths atomically: the original seven Arena artifacts/catalog/stamp/launcher paths plus only top-level Arena proto/ws/cache keys in server.json. Hosts, mirrors and all peer fields remain untouched; this avoids masking Knecht's rotating mirror. The historical seven-path/server-first-book requirement below is superseded by tools/v29/README.md and the tested current publisher.

## Scope and ownership

Start from main `b97b87bf` in clean worktree `C:/Users/end/dev/ember-parkour`, branch `codex/harbor-parkour-v29`. Preserve original checkout's unrelated Arena/League changes and every independently published peer release. Root owns protocol/server snapshot/client prediction integration, gates and coordinated release. Workers own shared parkour simulation, authored Harbor geometry/art, and browser page/test preparation plus read-only deployment audit. Only root runs Cargo at Idle priority; no process may operate the user's keyboard, cursor, focus, fullscreen or pointer lock.

## Requested behavior

Make Harbor vertical with enterable industrial houses/buildings, accessible roofs, paired walls for repeat wall jumps, alternate ground routes and tactical cover for eight players. Keep the authored 96 m harbor footprint and industrial landmarks. Add sprint-then-crouch ground sliding, jump-out slide momentum, crouch-held wall sliding and fresh-jump wall kicks retaining tangential momentum. Allow chains between opposing walls, not unlimited climbs against one wall or unbounded speed. Existing remappable sprint/crouch/jump controls and gamepad equivalents remain. Walking stays 4 m/s. Preserve shield, weapon accuracy, hit volumes and multiplayer authority.

## Shared simulation and release

One movement routine and serializable parkour state must serve authoritative simulation, client prediction and reconciliation. Jump remains a consumed PRESS so short taps survive the network send window. Obstacle bases, body/ceiling clamps, loot bonks, roof support, boundaries and spawn/round resets must remain valid. New motion and map collision are semantic changes: protocol 22 and frozen web v29, never silently swap v28's map/WASM. Test deterministic state replay, momentum bounds, no collision tunneling, ordinary map routes, held-jump behavior and authoritative snapshot transfer before publishing.

The new CI/ci-passed host timer introduced by a peer must be audited before a main push because main may now trigger automatic restarts. Do not run the broad Pages publisher while main's Fire source is older than the live Fire V2. Use an Arena-only release preserving all other public paths and catalog entries. Check live player counts and obtain current restart approval if anyone will be disconnected. Historical v25 approval does not apply to this release.

## Verification and handoff

Implementation is in progress; not yet published. Shared movement, protocol22 snapshots, Harbor collision/art and v29 menus are implemented. Root's lower-layer gate passes219 core tests (one intentionally ignored), two server unit tests and16 real-WebSocket integration tests. Strict core/server Clippy and the preliminary private QA server build pass in5.25s combined. The earlier full client/core/server gate passed376 tests before final narrow-wall and prediction-review corrections; it is not final-release proof.

The shared handling golden now hashes numeric values at1/1000 resolution while the independent same-platform replay still compares every raw bit. This addresses the pre-existing Windows/Linux libm last-bit failure instead of retaining main's skipped test. Excluding narrow wall faces changed only the final two100-tick checkpoints; a1.90s A/B run with the face-width filter disabled recovered the prior golden exactly. Linux CI must still verify the new unskipped pin. Broad walls support parkour;1.2m crane posts remain solid but cannot be climbed into decorative gantries.

Independent client review found fractional replay steps, render-rate shield expiry and a duplicate predicted jump latch after reconciliation. All three are repaired using a shared fixed-tick motion/shield helper, carried replay remainder and consumed-edge state; five focused regressions also cover death/respawn input clearing. The final native/strict-Clippy gate passes383 tests (146 client,219 core,18 server; one ignored core benchmark) in22.57s combined. Replay still estimates the server's input timeline from sent_at/ack_age, so normal network corrections are not eliminated.

Preliminary private network QA passed ground slides and capped wall descent but timed out on its alternating-kick driver (30.55s,7235 states); the driver changed direction before server confirmation and did not retain failure frames. The corrected controller follows confirmed last-wall normals, retains failure diagnostics, and requires one uninterrupted airborne chain. Its rerun passes all three eight-peer modes,4v4/rejoin, capacity/version rejection, walk/crouch/sprint and precision rules, sliding, capped wall descent and opposing-wall momentum observed by all seven peers:23.148s,5285 states, three shot events. This used the current core/server release binary before its final clean-commit build stamp; rerun after that stamped build for final artifact provenance.

Remaining gates: final native/strict-Clippy tests, clean committed release server and Arena-only WASM builds, private headless controls and eight-peer movement tests, and actual WebGL2 capture/review of the vertical routes. Record wall times, exact tested revisions, public artifact hashes and backend protocol. Do not report trusted user-gesture permission or physical controller validation from stubs. Public release must preserve frozen v28 and other games byte-for-byte.

Checkpoint `67869ea5` was built clean as `r1477` in 11.41 seconds, including server/wsbot, Arena WASM and bindings. WASM: 43,201,995 bytes, SHA256 `c9cc8a2e8bd3c24e3b61fdf6d42c1025f8590655f873910430ae666dc98e74b4`. Its actual-page headless gate passed 43 checks in 8.131 seconds; all 21 Harbor rendering views passed in 25.601 seconds with 101 draw calls per frame and no WebGL errors. These are correctness/capture figures, not an FPS benchmark. Screenshot review prompted a concrete interior floor finish to hide road stripes and a clearer pump-house camera; final artifacts must be rebuilt after that visual correction.

Linux CI [34040925969](https://github.com/EndersGamesDev/EmberEngine/actions/runs/34040925969) passed on `67869ea5`, including the restored handling golden and all eight deployment shim suites. Created 15:00:23 UTC and complete 15:01:56 UTC (93 seconds); promotion was correctly skipped for workflow_dispatch. GitHub branch push and CI dispatch work, but draft PR creation was denied by the current token's CreatePullRequest permission. No alternate credentials or peer were used to bypass that restriction. Barza message 80 records the checkpoint and release hold.

At14:55UTC main/ci-passed remain37d27e90 and Pages3dc66e0a. Knecht24's every-two-minute timer is active and restarts Arena, Fire and Kings together when ci-passed moves. The other three audited hosts have no timer. All inspected public hosts were empty at that checkpoint. The user has been asked whether publishing may trigger these three-game restarts; no approval has arrived yet. Hold main promotion and deployments pending that answer; branch checkpoints and workflow_dispatch CI are safe. Use the scoped publisher in tools/v29, never the broad all-game publisher while main's Fire protocol1 is older than the live FireV2.

## Final tested candidate and release hold

The final code/art candidate is `e58f7a47305089b869320998e9743488a810a4f0`, built from a clean worktree as `r1478`. Its 383 native tests passed again; the combined native test, stamped server/wsbot build, Arena WASM build and binding generation took 17.35 seconds. The subsequent merge `e0b8250f` incorporates the peer's Julibrot-only main update `fa0bfcb5` without conflicts. A direct diff confirms it changes none of Arena, arena-core, arena-server, ember-engine, the v29 page, Cargo.toml or Cargo.lock. Later documentation commits do not change tested runtime code, but the publishing script requires a fresh clean build stamp matching the eventual main HEAD.

| Final gate | Result | Measured wall time |
|---|---|---|
| Native tests | 146 client + 219 core + 18 server; one ignored benchmark | Included in 17.35 s test/build run |
| Strict Clippy | Passed for all Arena/core/server targets; final interior-art pass also clean | 22.57 s earlier combined gate; 11.90 s final art-test/Clippy pass |
| Settings configuration | 69 checks passed | 0.06 s |
| Actual v29 page/WASM controls | 43 checks passed, including remapping, sensitivity, pause and fullscreen-state stubs | 8.071 s |
| Private real-server networking | Eight peers in FFA/TDM/Hill, 4v4/rejoin, capacity/protocol rejection, movement/precision, slide and continuous opposing-wall kicks; all seven observers saw the actor's movement | 22.841 s, 5,226 states |
| Actual WebGL2 rendering | 21 Harbor viewpoints, 101 draws/frame, zero errors; corrected indoor floors and pump-house view inspected | 33.048 s |
| Linux CI | Both jobs passed; promotion correctly skipped | 85 s, 15:04:56–15:06:21 UTC |

Final WASM is 43,202,283 bytes with SHA256 `0200c883d7f228221b91346740f7985527082abbdd3f6aa735f90823058a80ae`. Both final browser reports record that hash. The private server Welcome reports protocol 22, `r1478`, `e58f7a47`. The uninterrupted wall-kick run recorded opposing normals at ticks 1231 and 1257 and reached 2.4800003 m before ending its bounded check; it is not a maximum-height claim. Linux CI: [34041179796](https://github.com/EndersGamesDev/EmberEngine/actions/runs/34041179796). Local raw reports and screenshots are under `target/parkour-browser`, `target/parkour-network` and `target/parkour-harbor`; they are regenerable and ignored by Git.

Not verified or claimed: trusted browser fullscreen/pointer-lock permissions, physical controller hardware, exhaustive combat balance, every possible roof-edge angle, or a public v29 deployment. Existing coarse contact-shadow filtering and sub-tick predicted HUD spread are recorded in the backlog. No operating-system input was synthesized. Each private server was stopped using its owned process handle; no live service was restarted by this task.

The user's follow-up asks whether the server will update automatically; it does not explicitly approve the three-game restart. A fresh SSH check confirms `ember-host.timer` is active, last triggered at 15:06:09 UTC, and has deployed the peer's `fa0bfcb5`. Thus main + green CI advances ci-passed, and Knecht's two-minute poll starts the build/restart automatically. GitHub Pages assets still need separate publication; a development-branch push does neither. Keep v29 on its branch until restart/publication approval arrives. Then recheck occupancy and concurrent work, promote normally, rebuild/stamp the exact clean main revision, verify a protocol-22 host/address book, publish only the seven allowed Arena paths with `tools/v29/publish-arena.cjs`, and run the public byte-preservation proof against the publisher's exact Pages base. Do not broadly rebuild/publish the other games.
